# docs/

Supplementary documentation that supports — but is not — the canonical
discipline documents. The canonical documents (`SPEC.md`, `DECISIONS.md`,
`CODE_SPEC.md`, `ROADMAP.md`, `CHECKLIST.md`, `SESSION_START.md`) live at the
repo root and take precedence over anything here.

This README is a directory placeholder so the otherwise-empty directory is
tracked by git.
