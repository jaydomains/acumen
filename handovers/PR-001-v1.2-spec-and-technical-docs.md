# Handover — PR-001 v1.2 spec clarification + technical docs

## PR identifier and link

- PR: PR-001 — v1.2 spec clarification + Acumen technical docs
- Link: <filled at PR open>
- Author / session: Claude Code session (audit-and-plan-acumen)
- Date closed: 2026-05-18

## Phase reference

- ROADMAP phase closed by this PR: pre-P0 (spec clarification + the
  ROADMAP/CODE_SPEC/CHECKLIST/SESSION_START documents themselves).
- Does this PR fully close the phase? Yes for the documentation
  deliverable. No application code; the next session starts at P0.

## What was built

- Files changed: `DECISIONS.md`, `SPEC.md` (v1.1 -> v1.2).
- Files added/replaced: `CODE_SPEC.md`, `ROADMAP.md`, `CHECKLIST.md`,
  `SESSION_START.md` (placeholders -> full canonical documents);
  this handover.
- Summary: v1.2 resolved the four under-specified statistical/operational
  decisions blocking technical design (AC-D9 competence formula,
  AC-D20/AC-D27 calibration math, AC-D22 embedding model, AC-D25 benchmark
  carve-out), then the technical document set was authored against v1.2,
  informed by a read-only SiteMesh audit so the platform port is
  mechanical.

## What was decided in this PR

- New product anchor: **AC-D27** — anchor calibration mathematics
  (Bayesian-shrinkage `effective_difficulty`, fresh-question delta,
  cold-start gate; `k=20` trade-off documented in its rationale).
- Amended: **AC-D9** (IRT-style competence + recency decay + loop
  target + null handling), **AC-D22 / SPEC §7.3** (embedding model =
  OpenAI `text-embedding-3-small`), **AC-D25** (benchmark sequential,
  carved out of JIT).
- New technical anchors: **AC-CD1–AC-CD18** in `CODE_SPEC.md`.
  AC-CD9/10/12/13 are confident (resolved by v1.2). **AC-CD11** is the
  sole `needs user input` anchor.
- Existing anchors depended on: AC-D1–AC-D26 throughout.

## Drift flags raised and how they were resolved

- v1.1 narrated benchmark mode as using AC-D25 JIT parallel streaming,
  which the v1.2 AC-D25 amendment contradicts. **Resolved inline** in the
  spec-clarification commit: SPEC §4.3/§4.7/§4.12/§6.1 and DECISIONS
  AC-D13 rewritten to sequential adaptive benchmark generation. Per user
  direction this was fixed now, not deferred, and is deliberately *not* a
  CHECKLIST drift question.
- AC-D22 "embedding model from the primary provider" was not
  implementable (Anthropic exposes no embeddings API). Resolved: fixed to
  OpenAI `text-embedding-3-small` (OpenAI already in stack for AC-D19).

## Open questions deferred to a later phase

- **AC-CD11 — cross-family review latency rule.** Per-response vs
  batched vs parallel, and the hard ceiling before the fail-soft "review
  pending" path triggers. Deferred with an explicit **pre-build gate at
  P6**; recorded in CODE_SPEC §11/AC-CD11 and as the sole CHECKLIST drift
  question.

## Build state vs spec

- Complete: the v1.2 spec set and the four technical documents.
- Partial: none.
- Stubbed: none. No application code exists by design.

## Test coverage and CI results

- Tests added/changed: none (documentation-only PR).
- Coverage delta: not applicable.
- CI result at merge: not applicable (no code).
- Manual verification: cross-reference sweep — every AC-D1–27 and
  AC-CD1–18 is cited by ≥1 ROADMAP phase and ≥1 CHECKLIST row; banned
  -string hygiene pass on CODE_SPEC; v1.1/v1.2 status markers reconciled
  across SPEC + DECISIONS.

## Anything a fresh Claude Code session needs to pick up cleanly

- Required reading: `SESSION_START.md`, then `CHECKLIST.md`, then
  `ROADMAP.md` P0.
- Next action: **start ROADMAP P0** (scaffold & stack lock).
- Trap: do not build the AC-D19 blocking submit path before the **P6
  gate** resolves AC-CD11.
- Note: `tenant_id` exists on tenant-scoped tables from P1 but v1 is
  single-tenant; RLS is a documented SiteMesh port seam, not a v1 gap.
