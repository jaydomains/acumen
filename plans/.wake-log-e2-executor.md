# Wake-log — Slice E2 executor (rollback matrix + AC-D21 override + demoted_sources)

Mechanical append-only action log for the sequenced code-execution audit loop on the
E2 slice PR (autonomous-content-generation workstream, slices A1→F1). Each line:
`<UTC date> · <action> · <last-seen remote HEAD> · <set-diff> · <round-trips>`.
E1 merged @ main `72624b6` (slice 12/14, AC-CD26 read half). E2 is the **rein-in**
write half — the rollback matrix + the relocated AC-D21 override + the DS13-a
`demoted_sources` source-override layer. Branched off `72624b6`. **Migration-class.**

- 2026-06-13 · E2 implemented — **verify-before-write against MERGED main (anchors govern)**: read the
  ratified **AC-CD26 rollback half** (`CODE_SPEC.md:1365-1384`) + SPEC §4.11:246 + AC-D14/D21/D28 +
  §290. **Key verify-before-write catch:** detail §13.4 says "no new model," but the **ratified AC-CD26
  governs** and rules the `demoted_sources` DB table (DS13-a option (i)) — the stale detail body predates
  the OV-64 ratification; the overseer's E1-merge marker confirms "E2 … migration class." So E2 IS
  migration-class. Built: migration `0013_e2_demoted_sources` (+ `DemotedSource` model); the DS13-a DB
  source-override layer in `source_authority.py` (`effective_authority_tier`/`effective_is_allowlisted`/
  `denied_hosts`/`filter_demoted` — code seed + DB overrides) wired into `corpus_builder.acquire_for_topic`
  (skips denied hosts); the four rollback fns in `oversight.py` (`rollback_pill` retire / `rollback_question`
  exclude AnchorQuestion / `rollback_batch` via PublishRecord.batch_id / `rollback_source` via per-item
  draft_ref→pill + writes a durable `denied` demotion) + `override_safety_relevant` (reuses
  catalogue.override_pill_safety) + the admin-gated write router (commits explicitly). **retract-not-delete
  (AC-D14 retire / AC-D23 exclude), idempotent, audited `pill_generation.rollback_*`.** Two flagged
  interpretations (for reviewers): (a) `rollback_question` targets **AnchorQuestion** (the `excluded`
  soft-retract; `Question` has no retire field) — defensible per the content-oversight scope; (b)
  `rollback_source` per-item precision uses the draft_ref→pill payload resolution (the CA-E1-2b pattern,
  here on an infrequent write). NORMAL class for spec (empty anchor diff; AC-CD26 ratified). Tests:
  `test_oversight_rollback.py` (6) + `test_source_override.py` (5) + `test_oversight_rollback_api.py` (4)
  + e2e `test_demoted_sources.py` (override join across sessions) + p1_schema table-set/trigger-count/0013
  round-trip updated. **1059/1059 unit+integration pass; ruff 0.6.9 (CI parity) + format + mypy + structure
  + unpinned clean.** · last-seen remote HEAD `72624b6` · set-diff `0 dropped / 0 added (baseline — no reviewer findings yet)` · round-trips: none.

- 2026-06-13 · **CI fix — mypy app tree-wide** @ push `0e145f3` · `checks` red on `b413cfa`/`a149294`:
  `mypy app` (75 files, CI parity) flagged `oversight.py:546` `target_id=pill_ids and next(iter(...)) or
  SEED_TENANT_ID` as `set[UUID] | UUID`. **Lesson:** per-file `mypy <files>` gave a FALSE-NEGATIVE; run
  `mypy app` (whole-package) to match CI. Fixed: rollback_source summary audit `target_id=SEED_TENANT_ID`
  (no single entity id — per-pill audits carry pill targets; source in target_entity + detail). **Test-only
  trivial value change; logic unchanged.** mypy app clean; rollback tests green. · last-seen remote HEAD
  `72624b6` · set-diff `0 dropped / 0 added (CI-only fix)` · round-trips: none (pre-review CI).

- 2026-06-13 · **round 1 fold (amend-once)** @ push `83a710b` · auditor `3d9382d` (content CLEAN, **0
  findings**; both flagged interpretations CONCUR; hold to sign at migration-chain green) + overseer
  `1a6d1b4` (OV-E2-1..9 clean; 1 FINDING = `_pill_ids_for_source` tenant-isolation, CA-E1-1 recurrence;
  gate held) + Gitar (👍 approved-with-suggestions, 4×💡). Folded: **tenant filter** on the gen-task scan
  (overseer + Gitar #4); **audit target_id**→`demotion.id` after flush (Gitar #1); **Tier() guard** on
  invalid tier_override→seed+WARN (Gitar #3). **Deferred Gitar #2** (select-then-insert race — ON CONFLICT
  upsert breaks the AC-CD15 fake; admin-infrequent/single-tenant; unique constraint protects integrity;
  offered in-slice if reviewers want). +1 unit (invalid tier_override). **1060 pass; ruff/format/mypy app/
  structure/unpinned clean; anchor diff empty.** · last-seen remote HEAD main `72624b6` · set-diff `overseer-finding + Gitar#1/#3/#4 dropped (folded), #2 deferred; 0 added` · round-trips: CA 1/5.

- 2026-06-13 · pairing — auditor `484883c` round 1b (OWNED MISS CA-E2-1 tenant scope = overseer-caught;
  withdrew 'content clean'; held pending fix — **fix already in `83a710b`**, will re-verify + re-sign) +
  overseer `d5fa146` (tenant finding + Tier-guard + audit-id **resolved**; **Gitar GREEN**; race = residual
  non-blocking, D1-D2 #3 class; **gov lane CLEAN** — accepts the race deferral). All fixes pushed; auditor
  re-verifying existing SHA. **No-action pairing.** · last-seen remote HEAD main `72624b6` · set-diff `0/0`
  · round-trips: CA 1/5 (awaiting re-verify + migration-chain green).
