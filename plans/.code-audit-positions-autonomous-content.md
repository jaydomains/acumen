# Code-audit pre-registered positions — autonomous-content-generation execution cycle

**Role:** CODE-AUDITOR (content correctness) for the sequenced execution cycle implementing the
14 build slices (A1–A3, B1–B3, C1–C2, D1–D2, D3, D4, E1, E2, F1) of the autonomous-content-generation
workstream against the ratified **v1.9** spec. Discipline basis: `.claude/roles/plan-auditor.md`
adapted for code review — artifact is **code**; lane is **content correctness** (does the code match
the spec, are tests sufficient, are there bugs/regressions). Bound to Acumen by
`plans/REQUIRED_READING.md`.

**Audit branch:** `claude/code-audit-sequenced-execution-57lyby`.
**Wake-log:** `plans/.code-audit-wake-log-autonomous-content.md` (this branch; per REQUIRED_READING §6
reviewers commit wake-logs to their own review branch — adapted to a single rolling cross-slice log
keyed to the workstream because this is one sequenced cycle across 14 PRs, not one PR).
**Status-line token (final marker, per slice):** `Status: code-audit final — approved by code-auditor (Slice <ID>)`.
*(Choice noted here per role-file §1 since REQUIRED_READING does not parameterize the code-audit
variant; will be re-stated in my first review comment on the first slice PR so the executor/overseer
can ratify it.)*

**Anti-anchoring discipline (role §4.2):** each slice's positions are pre-registered and timestamped
**before** reading that slice's executor draft. Positions derive from the ratified v1.9 spec
(SPEC/DECISIONS/CODE_SPEC anchors) + the merged detail plan (`plans/2026-06-09-autonomous-content-generation-detail.md`,
PR #108) + the merged workstream plan (PR #107) — never from the executor's PR description.

**Cross-slice load-bearing disciplines I own (per task):**
- **amend-once at execution time:** no slice silently re-amends an anchor body authored complete
  upstream; if drift surfaces the executor must surface it, not silently reconcile.
- **inter-slice dependency:** slice N must respect slice N-1's surface (A2 reads what A1 wrote, etc.).
- stay subscribed across all 14 slices — no between-slice stand-down (§4.9 sequenced cycles).

---

## Slice 1 — A1: source-authority allowlist + scoring registry (T1/T2/T3)

**Pre-registered:** 2026-06-12 (before any executor A1 draft exists — verified: zero open PRs at
`git ls-remote` baseline; broad watcher armed for first-branch discovery).

**Yardstick:** AC-D28 (DECISIONS.md:728–750, as-merged v1.9 / PR-A #110) + detail-plan §1.1–§1.6
(lines 238–476) + AC-CD15 (test discipline) + SESSION_START (structure-gate, CHECKLIST evidence) +
ROADMAP:209–227 (NS-5 named-non-phase workstream home).

**Execution-gate precondition (both ratified — A1 is un-blocked):** (a) AC-D mint = **AC-D28** landed
in PR-A #110 (DECISIONS.md:728); (b) **NS-5** ruled a *named non-phase workstream* (ROADMAP:211).

### Category 1 — faithful encoding of A1's spec contract

- **P-A1-1** — Module `app/domain/source_authority.py` exists, a **pure, offline** domain primitive:
  no DB table, no migration, no network, no provider call, no I/O. (AC-D28 Impl: *"pure, offline
  source-authority registry … carries no DB table at A1"*; detail §1.2a/§1.6.)
- **P-A1-2** — A `Tier` type enumerating T1/T2/T3 with intrinsic ordering **T1 > T2 > T3**. (Detail
  §1.2a leans `IntEnum T1=3/T2=2/T3=1`; the *ordinal values* are a detail-plan build choice, not a
  ratified value — only three-tier identity + monotone ordering is contract-binding.)
- **P-A1-3** — `authority_tier(url_or_host) -> Tier | None`: normalises to host, **lowercases**,
  **strips a leading `www.`**, matches `_ALLOWLIST` by **exact host first, then suffix-wildcard**
  for `*.<suffix>`; `*.gov.za` matches `dol.gov.za` **and** the apex `gov.za`; **exact wins over
  wildcard on tie**; non-allowlisted host → **`None`**. (AC-D28 Decision ¶ "Tier resolution".)
- **P-A1-4** — Seed allowlist matches AC-D28 **verbatim** (these are RATIFIED, not leans):
  T1 = {`iso.org`, `nrcs.org.za`, `sabs.co.za`, `*.gov.za`};
  T2 = {`nace.org`, `osha.gov`, `iec.ch`, `astm.org`};
  T3 = minimal seed (env-widened). (AC-D28 Decision bullets, DS1-b ratified.)
- **P-A1-5** — `authority_score(tier) -> float` returns **T1=1.0 / T2=0.6 / T3=0.3** exactly
  (DS1-a ratified into AC-D28), monotone. (AC-D28: *"tier→score map (T1=1.0 / T2=0.6 / T3=0.3)"*.)
- **P-A1-6** — `is_allowlisted(url_or_host) -> bool` ≡ `authority_tier(...) is not None`.
  (Detail §1.2a; AC-D28 names `is_allowlisted`.)
- **P-A1-7** — `filter_to_allowlist(results: Iterable[WebSearchResult]) -> list[tuple[..., Tier]]`:
  drops every non-allowlisted row, pairs each survivor with its `Tier`, **preserves ordering**. A1
  ships the **primitive only**; **A2** wires it around `get_web_search_source().search(...)`.
  (AC-D28: *"filters an arbitrary web-search result list to allowlisted hosts"*; detail §1.2a/§1.6.)
- **P-A1-8** — Env-extension in `app/config.py`: `source_authority_t1_extra`/`_t2_extra`/`_t3_extra`
  (CSV host patterns), merged with the coded seed at registry-build; an env entry **may add but never
  silently re-tier** a seed host; a host appearing at two tiers resolves to the **stronger** tier
  **deterministically**. (AC-D28 Decision ¶ "operator-extensible"; detail §1.2b.)
- **P-A1-9** — `.env.example` gains `SOURCE_AUTHORITY_T1_EXTRA=`/`_T2_EXTRA=`/`_T3_EXTRA=` siblings
  (AC-CD18 env-default discoverability; detail §1.2c).
- **P-A1-10** — DS1-c honoured: A1 ships a corpus-acquisition primitive; it does **not** tighten or
  touch the AC-D21 safety-link curation search. (AC-D28 DS1-c *"corpus-acquisition only"*; detail §1.6.)

### Category 2 — test coverage adequate to acceptance criteria

- **P-A1-11** — `tests/unit/test_source_authority.py` covers the five §1.5 cases:
  (1) tier resolution — exact + wildcard incl. apex `gov.za`, `www.`-strip, case-insensitive,
  unknown→`None`; (2) score monotonicity with **exact** values asserted (so a value change is
  test-visible); (3) `filter_to_allowlist` over a mixed list — only allowlisted survive, each paired
  with correct `Tier`, all-non-allowlisted → `[]`, ordering preserved; (4) env-extension via
  monkeypatched settings incl. the stronger-tier conflict rule; (5) zero-network purity.
- **P-A1-12** — Tests run under the `conftest.py` no-network guard (AC-CD15): no provider, no I/O.
  Coverage is near-full over `source_authority.*` callables (AC-CD15 `app/domain/*` bar).

### Category 3 — no silent spec drift

- **P-A1-13** — A1 mints **no** `Operation` enum value and **no** cron; it must **not** touch the
  "seven→nine AI operations" or "nine crons" count invariants (owned by B1/C1, A3/D4). The
  structure-gate (AC-CD2/AC-CD17) must pass with the new domain module **unmodified**. (Detail §1.4.)
- **P-A1-14** — Scope fence (detail §1.6): **no** `app/models.py`/migration, **no** router, **no**
  `frontend/**`, **no** `web_search.py` seam change, **no** `drive_rag.py` change, **no** beat
  schedule. Any of these appearing in A1 is scope drift to surface.
- **P-A1-15** — Code values must match the **as-merged AC-D28** body, not the planner's
  pre-ratification leans. DS1-a/b/c are now ratified; any divergence is **spec drift**, not a tunable.

### Category 4 — inter-slice contract preservation

- **P-A1-16** — A1 is slice 1 (nothing upstream to break) but **establishes** the surface A2/C2/E1
  consume: `authority_score(tier)` is the single number C2 confidence + E1 authority-breakdown read;
  `filter_to_allowlist` returns `(row, Tier)` so A2 stamps the chunk without re-resolving; the `Tier`
  type + score function are **immutable** for later slices (use, do not re-author). The executor must
  expose these exact callable names/signatures.

### Category 5 — workflow/governance gating (code-audit adaptation)

- **P-A1-17** — A1 execution PR records a **CHECKLIST + ROADMAP row** against the named non-phase
  workstream with **real evidence** (the test path/command), per SESSION_START close-discipline +
  ROADMAP:211/223.
- **P-A1-18** — PR is a **draft**, implements A1 per the detail plan, and the body cites the detail
  plan (PR #108) + amendment-chain merges (PR-A #110 / AC-D28) + slice acceptance criteria
  (per task identification rubric for A1).
- **P-A1-19** — Gate-2 relay discipline (workstream/detail §0.2): the merged §1/amendment record reads
  as a *relay* to a downstream session, **but** AC-D28 + NS-5 are now **merged into the live spec**
  (DECISIONS.md / ROADMAP), so they are the authoritative spec text, not a pending relay. Watch that
  the executor grounds against the **merged anchors**, not a chat-relayed claim.

**Finding-set-diff (baseline commit `820c195`):** `0 dropped / 0 added (baseline — A1 positions
pre-registered, no findings yet)`.

### A1 verification verdict — round 1 (executor draft @ `a3f105f`, PR #114) — NOT CLEAN

Verified each position against the **actual code** (read `app/domain/source_authority.py`,
`config.py`/`​.env.example`/`CHECKLIST.md` diffs, `test_source_authority.py`) **and by execution**
(16/16 tests pass in an isolated worktree). Most positions **Confirm**; three carry findings.

**Confirms (positive coverage):** P-A1-1 (pure offline module, no DB ✓), P-A1-2 (`Tier` IntEnum
T1>T2>T3 ✓), P-A1-4 (seed allowlist verbatim per AC-D28 ✓), P-A1-5 (`authority_score`
1.0/0.6/0.3 exact ✓), P-A1-6 (`is_allowlisted` ✓), P-A1-7 (`filter_to_allowlist` drops/pairs/orders ✓),
P-A1-9 (`.env.example` siblings ✓), P-A1-10 (DS1-c corpus-only; AC-D21 curation untouched ✓),
P-A1-13/14 (scope fence — no Operation/cron/models/router/FE/seam change ✓), P-A1-15 (scores match
as-merged AC-D28 ✓), P-A1-16 (inter-slice surface exposed correctly ✓), P-A1-17 (CHECKLIST row
missing→built, real evidence ✓), P-A1-18 (draft, cites detail plan + PR-A #110 + AC ✓),
P-A1-19 (grounds against merged anchors ✓).

**Findings (open):**
- **CA-A1-1 — Push-back (BLOCKING).** *Affects P-A1-3 / P-A1-8.* `authority_tier`'s exact-match
  short-circuit (`source_authority.py:151-153`) returns a **weaker env-supplied exact tier** even
  when a **stronger seed wildcard** covers the host — and `_load_allowlist` only reconciles
  *identical-pattern* conflicts, not exact-vs-covering-wildcard. Net: an operator env entry silently
  **downgrades** an authoritative host. Verified: seed `*.gov.za`=T1 + `SOURCE_AUTHORITY_T3_EXTRA=data.gov.za`
  → `authority_tier("data.gov.za")`=**T3**, not T1. **Violates AC-D28 (`DECISIONS.md:738`):** *"a host
  appearing at two tiers resolves to the stronger tier deterministically"* — and routes a demotion
  through the additive env path AC-D28 reserves for the E2 `demoted_sources` DB layer. The authority
  signal feeds Stage-C confidence + Stage-E breakdown (AC-D28 Impl), so the downgrade corrupts
  downstream scoring. Fix: effective tier = strongest of {exact, all matching wildcards}, strict `>`
  preserving "exact wins on tie." **≡ Gitar finding #1** (independently verified against the anchor,
  not relayed). Round-trip 1/5.
- **CA-A1-2 — Missing (test) (blocking-adjacent — lands with the CA-A1-1 fix).** *Affects P-A1-11.*
  No test exercises (a) the CA-A1-1 env-downgrade case, nor (b) the docstring guarantee *"among
  multiple matching wildcards the stronger tier wins"* (`source_authority.py:142-143`). AC-CD15
  near-full-branch bar + §1.5. The CA-A1-1 fix must ship with regression tests for both.
  **≡ Gitar #3 (b) + new (a).** Round-trip 1/5.
- **CA-A1-3 — Refine (low, NON-blocking).** `_load_allowlist` rebuilt per `authority_tier` call →
  O(N·allowlist) inside `filter_to_allowlist`. Constant data; memoize (lru_cache keyed on the three
  env strings; `get_settings()` is already cached). Efficiency only, not correctness — does not block
  my sign-off on its own. **≡ Gitar #2.** Round-trip 1/5.

**Not a spec-author surface:** AC-D28 is unambiguous on the stronger-tier rule, so CA-A1-1 is a
code-vs-clear-anchor divergence I adjudicate directly (role §7 closed list not triggered) — the
executor fixes; no halt-class condition.

**Finding-set-diff (round 1, commit `afe539e`):** `3 added [CA-A1-1, CA-A1-2, CA-A1-3] / 0 dropped`.
No final marker posted at round 1 — convergence not reached; awaiting executor fix + re-push.

### A1 verification verdict — round 2 (executor fix @ `ef30795`, PR #114) — CLEAN

On-wake pairing done: fetched `ef30795` (commit *"A1 r1 — fix env-exact-vs-seed-wildcard tier
downgrade"*), diffed the module + tests, ran the updated suite (**18/18 pass**), read PR #114
comments (executor fold `4687897329`; overseer wake-log `ae7c84e` converging on CA-A1-1=OV-A1-4r;
Gitar re-reviewing). Re-verified each open finding against the **new text**:

- **CA-A1-1 — RESOLVED.** `authority_tier` now seeds `best = allowlist.get(host)` (exact) then takes
  the strongest matching wildcard with strict `>`. Verified: `*.gov.za`=T1 + env T3 `data.gov.za`
  → **T1** (no downgrade); "exact wins on tie" preserved (strict `>`); no regression
  (`test_exact_entry_wins_over_wildcard` still T2/T3). Matches AC-D28:738 stronger-tier-deterministic.
- **CA-A1-2 — RESOLVED.** Regression tests added: `test_env_exact_does_not_downgrade_seed_wildcard`
  (the CA-A1-1 case) + `test_strongest_wildcard_wins_among_multiple` (the documented multi-wildcard
  guarantee). Both traced correct against the code.
- **CA-A1-3 — RESOLVED.** `_build_allowlist` is now `@lru_cache(maxsize=16)`-memoized on the three env
  strings, treated read-only by every caller (no mutation path); `filter_to_allowlist` no longer
  rebuilds per row.

Scope fence still intact (fix touched only `source_authority.py` + its test + executor wake-log — no
models/migration/router/FE/seam/anchor edit). Amend-once preserved (no DECISIONS/SPEC/CODE_SPEC edit;
NORMAL execution fold, no spec-author surface). Inter-slice surface unchanged for A2/C2/E1.

**Finding-set-diff (round 2):** `0 added / 3 dropped [CA-A1-1 resolved-by-fix, CA-A1-2 resolved-by-fix,
CA-A1-3 resolved-by-fix]`. Round-trips closed: CA-A1-1 1/5 · CA-A1-2 1/5 · CA-A1-3 1/5 (one round each).

**Status: code-audit final — approved by code-auditor (Slice A1)** — content-bound to `ef30795`.
This is **one of three** independent sign-offs; it does **not** authorize merge and I do **not** flip
draft→ready or merge (the overseer executes). Convergence needs the executor's + overseer's markers at
this same content-SHA + the three-layer green gate (incl. Gitar re-review flipping green) + the
override window. Any content change to A1 after this re-stales my marker and I re-verify at the new SHA.

**A1 OUTCOME: MERGED** @ `main`=`4f95414` (PR #114), verified via `git ls-remote` (branch deleted).
Three sign-offs converged at content-SHA `ef30795`. 1/14 slices complete.

---

## Slice 2 — A2: corpus acquisition pipeline (allowlist web-search → fetch → extract → embed → pgvector)

**Pre-registered:** 2026-06-12 (before any executor A2 draft exists — verified: no `content-gen-a2`
branch / open A2 PR at `git ls-remote`). Derived independently from the spec, **not** from the overseer's
governance lane (OV-A2-*) which I deliberately did not read in detail.

**Yardstick:** AC-CD25 (CODE_SPEC.md:1278–1326, as-merged v1.9 / PR-A) + amended AC-D22 (Drive RAG
retired → AI-built corpus, DECISIONS.md:555–575) + amended AC-D21 (web search → corpus acquisition,
:533–547) + AC-D28 (A1 authority, consumed) + detail-plan §2.1–§2.6 (lines 503–725) + AC-CD15 + AC-CD1
(pinned-dep) + AC-CD8 (spend-aggregation invariant) + SESSION_START P1 migration discipline.

**Execution gates — all resolved (A2 un-blocked):** A1 **MERGED** (`source_authority` live on
`main`=`4f95414`); AC-CD25 minted + AC-D21/AC-D22 amended (PR-A #110); DS2-b ruled **(ii)
cross-source corroboration**; AC-CD1 extraction deps ratified (`bs4`+`pypdf`, in PR-A `requirements.txt`);
NS-1 ruled **relocate-not-delete**; NS-5 named-non-phase-workstream home.

### Category 1 — faithful encoding of A2's spec contract

- **P-A2-1** — New `CorpusChunk` model (`app/models.py`) mirroring `DriveChunk` (`Base, TimestampMixin,
  AIProvenanceMixin`; `embedding Vector(1536)`; `content_hash` indexed; `source_doc_ref`/`chunk_index`/
  `chunk_text`/`indexed_at`; IVFFlat index mirroring `ix_drive_chunk_embedding`; `tenant_id` day one)
  **plus** `source_host`, `authority_tier`, `authority_score`. (AC-CD25 Decision; §2.2a.)
- **P-A2-2** — The workstream's **first migration** (alembic) — up/down clean, creates `CorpusChunk` +
  IVFFlat index; linear addition to the chain (migration-chain CI). (AC-CD25 Impl; §2.2a; SESSION_START.)
- **P-A2-3** — `app/domain/corpus_builder.py` with `acquire_for_topic(db, *, topic, http_client=None)`
  composing the pipeline **fail-soft throughout**. (§2.2b/§2.2c; AC-CD25.)
- **P-A2-4** — Source discovery **allowlist-restricted**: `filter_to_allowlist` (A1) over
  `get_web_search_source().search(topic)`; only allowlisted hosts fetched; empty post-filter → `[]`,
  no fetch. (AC-CD25; §2.2b-1; ruling 3.)
- **P-A2-5** — Per-URL fetch via **injectable `httpx.AsyncClient` seam** (reuse the
  `safety_links._fetch_body_hash` pattern, extended to **retain body**); **fail-soft per-URL** (WARN,
  skip) — one dead source never fails the run. (§2.2b-2; AC-CD25.)
- **P-A2-6** — Extract **HTML + PDF** via the ratified deps (`bs4` + `pypdf`, AC-CD1). (AC-CD25
  "extract (HTML + PDF per AC-CD1)"; §2.3.)
- **P-A2-7** — Chunk via **reused** `chunk_document` (~500-token deterministic); dedup via **reused**
  `content_hash` — **no reimplementation** of chunking/hashing. (AC-CD25; §2.1/§2.2b-4/5.)
- **P-A2-8** — **Cross-source corroboration for safety-relevant topics** (DS2-b ruled **(ii)**): track
  when the same claim/fact appears across **≥2 authoritative sources**, stamp a corroboration signal
  feeding Stage-C confidence + the B2 provenance chain. Content-hash dedup is the floor for **all**
  topics. (AC-CD25 "Safety cross-source corroboration".)
- **P-A2-9** — Embed via the `AIProvider` **embed** op (`text-embedding-3-small`), `record_provenance`
  stamping cost to **OpenAI**; **no new `Operation` enum value** (the corpus reuses `embed`). (AC-CD25
  Rationale; §2.2b-6.)
- **P-A2-10** — Persist `CorpusChunk` with `source_host`/`authority_tier`/`authority_score` from A1
  (`authority_tier(host)` + `authority_score(tier)`); **idempotent by `(source_host, content_hash)`**.
  (AC-CD25; §2.2b-7.)
- **P-A2-11** — `app/ai/cost.py`: `CorpusChunk` joins the `current_month_spend` per-table
  provenance-spend aggregation (AC-CD8 invariant), mirroring the `DriveChunk` registration. (AC-CD25
  Impl; §2.4.)
- **P-A2-12** — `requirements.txt` (+ `-worker.txt` if needed): extraction deps **pinned** (AC-CD1;
  the unpinned-deps structure-gate stays green). (§2.4; CHECKLIST A2 row.)

### Category 2 — test coverage adequate to acceptance criteria

- **P-A2-13** — `tests/unit/test_corpus_builder.py` (+ model/migration test) covers §2.5:
  (1) allowlist-restricted discovery — only allowlisted URLs fetched (assert fake transport saw only
  allowlisted hosts); (2) fetch→extract→chunk→embed→persist — rows carry `source_host`, correct
  `authority_tier`/`authority_score` (A1), `content_hash`, `embedding`, provenance (stub provider, zero
  cost); (3) dedup idempotency — re-run adds no rows, changed body adds only changed chunks; (4)
  fail-soft — dead URL (500/`HTTPError`) skipped (WARN, no row), run continues; empty post-filter → `[]`;
  (5) extraction HTML→text deterministic (+PDF); (6) migration up/down clean + structure-gate. Plus
  **DS2-b corroboration coverage** for safety-relevant topics. Zero-network (AC-CD15).

### Category 3 — no silent spec drift / amend-once at execution

- **P-A2-14** — A2 diff touches **no** anchor/spec body (`DECISIONS`/`SPEC`/`CODE_SPEC` §18 unchanged) —
  AC-CD25/AC-D21/AC-D22/AC-D23 were authored **complete** in PR-A; A2 implements code only. A2 is the
  first toucher of these multi-slice anchors at execution; **any re-amend is a finding** (silent
  partial-fold / amend-once violation). Only doc surface = the CHECKLIST A2 row (real evidence).
- **P-A2-15** — No "nine crons" / "nine AI operations" count touched (corpus-refresh cron = A3; no
  `Operation` mint). (§2.4.)
- **P-A2-16** — Scope fence (§2.6): **no** retrieval helper, **no** refresh cron (A3), **no** generation
  / provenance chain (B2), **no** Drive-code deletion (NS-1: build beside, don't remove), **no**
  `Operation` enum change (B1), **no** router/FE/dashboard. safety-link curation + `pill_proposal`
  refiner untouched.

### Category 4 — inter-slice contract preservation (FIRST real inter-slice check)

- **P-A2-17** — A2 **imports A1's `source_authority`** and consumes `filter_to_allowlist` /
  `authority_tier` / `authority_score` **per the contract A1 locked at `4f95414`** — does not
  re-implement, fork, or shadow the allowlist; does not regress A1's surface. (§2.2b; the load-bearing
  A2-reads-what-A1-wrote check.)
- **P-A2-18** — `CorpusChunk` is a **new table** (DS2-a), **not** a `DriveChunk` reuse: mirrors the
  shape but stays separate so (i) the still-live Drive path (NS-1 relocate-not-delete) is not broken/
  entangled, (ii) per-source rollback (E2) + the authority columns stay clean. (AC-CD25 Rationale;
  §2.2a.)
- **P-A2-19** — NS-1 **relocate-not-delete**: if `chunk_document`/`content_hash`/`cosine_top_k` are
  relocated to a shared module so `corpus_builder` (+ A3 retrieval) keep them after Drive retirement,
  the **Drive path still imports them and stays green** — no Drive-code deletion at A2. (AC-D22 v1.9;
  AC-CD25 Impl; §2.3 NS-1.)

### Category 5 — workflow/governance gating (code-audit adaptation)

- **P-A2-20** — A2 execution PR is a **draft**, records a CHECKLIST/ROADMAP row under the NS-5
  workstream with **real evidence**, and the body cites detail-plan §A2 + amendment chain
  (AC-CD25/AC-D21/AC-D22 in PR-A #110) + acceptance criteria. NORMAL merge class (executes against
  already-ratified v1.9; mints/amends nothing).

**Finding-set-diff (A2 pre-registration commit):** `0 dropped / 0 added (baseline — A2 positions
pre-registered, no findings yet)`.

### A2 verification verdict — round 1 (executor draft @ `54dbf05`, PR #115) — NOT CLEAN

Verified against the **actual code** (read `corpus_builder.py`, `CorpusChunk` model + migration 0009,
`cost.py` diff, `test_corpus_builder.py`, `test_p1_schema` diff; confirmed `auto_tag_safety(name,
description: str|None, db)` prod call is correct despite tests mocking it; confirmed deps
`bs4`/`pypdf` (PR-A, prod) + `reportlab` (pre-existing prod, P11 PDF export — *not* misplaced)). Could
**not** execute the suite locally (the app import chain `corpus_builder→cost→permissions→argon2/…`
exceeds this container's dep provisioning — an environment limit, not an A2 defect); rely on
read-verification + the executor's reported 430/430 + CI (migration-chain + full suite = the green
gate, overseer's lane).

**Confirms:** P-A2-1 (`CorpusChunk` mirrors `DriveChunk` + authority cols + `corroboration_count` ✓),
P-A2-2 (migration 0009 up/down clean, chains 0008, IVFFlat, trigger, count 34→35 ✓), P-A2-3
(`acquire_for_topic` fail-soft pipeline ✓), P-A2-4 (allowlist-restricted discovery ✓ — but see
CA-A2-2 re *post-fetch* enforcement), P-A2-5 (injectable fail-soft fetch ✓ — but see CA-A2-2/3),
P-A2-6 (HTML+PDF extract ✓), P-A2-7 (reused `chunk_document`/`content_hash`, no reimpl ✓), P-A2-9
(`embed` op reuse, no new `Operation`, OpenAI cost via `record_provenance` ✓), P-A2-10 (authority
stamping + DB-unique `(source_host, content_hash)` idempotency ✓), P-A2-11 (`cost.py` aggregation ✓),
P-A2-12 (deps pinned; reportlab pre-existing prod ✓), P-A2-13 (8 tests cover §2.5 ✓), P-A2-14/15/16
(amend-once: no anchor/spec body edit; no cron/op count; scope fence ✓), P-A2-17 (consumes A1's
`source_authority` faithfully ✓), P-A2-18 (new table, not `DriveChunk` reuse ✓), P-A2-19 (NS-1: imports
`drive_rag` beside legacy, no deletion ✓ — relocation owed at the Drive-retirement step, forward note),
P-A2-20 (draft, CHECKLIST row, NORMAL class ✓). *Gitar #4 (embedding nullable) is NOT a finding —
faithful mirror of `DriveChunk` per AC-CD25.*

**Findings (open):**
- **CA-A2-1 — Push-back (medium, adjudicable) · `corpus_builder.py` corroboration + `CorpusChunk.corroboration_count`.**
  *Uniquely content-lane — neither Gitar nor the overseer flagged it.* DS2-b(ii) corroboration counts
  distinct sources sharing an **exact `content_hash`** (byte-identical ~500-token chunk). Independent
  authoritative sources almost never emit byte-identical chunks, so `corroboration_count ≥ 2` rarely
  fires (only on verbatim-republished standard text) — the ratified AC-CD25 intent *"materially
  stronger grounding for safety content"* is largely unrealized. The test only reaches count=2 by
  feeding **identical** HTML to both sources. AC-CD25 says *"the same claim/fact"*, not "byte-identical
  chunk." Ask the executor: confirm exact-hash as the intended v1 floor (then document the limitation;
  the *"same claim/fact"* granularity is arguably a spec-author clarification, parallel to NS-3) **or**
  use embedding cosine-similarity (the embeddings already exist). Adjudicable — concede if exact-hash is
  the confirmed v1 floor. Round-trip 1/5.
- **CA-A2-2 — Push-back (BLOCKING) · `_fetch_body` `corpus_builder.py:~76` + `_gather_candidates`.**
  `httpx.AsyncClient(..., follow_redirects=True)` with **no re-validation of the final (post-redirect)
  host** against the allowlist. An allowlisted source that 3xx-redirects to a non-allowlisted/internal
  host (e.g. `169.254.169.254`) is fetched, extracted, embedded, and persisted as a `CorpusChunk`
  stamped with the **original allowlisted** `source_host`/`authority_tier`. **Violates AC-D28** (*"a host
  not on the allowlist … is not an eligible corpus source"*; ruling 3 *"web search restricted to the
  allowlist"*) and is an **SSRF / corpus-poisoning / authority-laundering** vector. Independently
  verified against the code; **≡ Gitar #1 / overseer OV-A2-17.** Fix: re-validate `response.url.host`
  via A1 `is_allowlisted` post-redirect (drop if escaped), or `follow_redirects=False` + skip 3xx;
  persist the **final** host's re-resolved tier. Round-trip 1/5.
- **CA-A2-3 — Refine (medium) · `_fetch_body:~82`.** `body = response.content[:_HTTP_FETCH_MAX_BYTES]`
  truncates **after** httpx has buffered the entire body, so the 5 MB cap does **not** bound download
  memory — the *"bound a malicious/huge payload"* comment overstates the protection, and a hostile
  allowlisted/redirect target serving multi-GB OOMs the worker. Couples with CA-A2-2. Fix: stream
  (`client.stream`) and stop at the cap. **≡ Gitar #2.** Round-trip 1/5.
- **CA-A2-4 — Refine (low, NON-blocking) · `_already_stored` per-chunk.** N+1 `SELECT`s (one per
  candidate chunk); batch the `(source_host, content_hash)` existence check into one `IN`/tuple query.
  Efficiency only. **≡ Gitar #3.** Round-trip 1/5.

**Not a spec-author surface (mostly):** CA-A2-2/3/4 are code-vs-clear-spec / robustness — executor
fixes. CA-A2-1's *"same claim/fact"* granularity *may* warrant a spec-author clarification if the
executor cannot resolve it against AC-CD25 alone — flagged, not yet surfaced.

**Finding-set-diff (A2 round 1, commit pushed):** `4 added [CA-A2-1, CA-A2-2, CA-A2-3, CA-A2-4] / 0 dropped`.
No final marker at round 1 — CA-A2-2 blocking.

### A2 verification verdict — round 2 (executor fix @ `442f00f`, PR #115) — CLEAN

On-wake pairing: diffed `54dbf05..442f00f` (only `corpus_builder.py` + its test + executor wake-log),
read the executor fold + overseer wake-log (`4cb302e`, converged on CA-A2-2=OV-A2-17). Re-verified each
finding against the **new text**:

- **CA-A2-2 — RESOLVED.** `_fetch_body` returns `final_host = response.url.host` (post-redirect);
  `_gather_candidates` re-resolves `tier = authority_tier(final_host)`, **drops** when `None`
  (off-allowlist / internal), and persists `source_host=final_host` + the final host's tier — closes
  the SSRF/allowlist-escape + authority-laundering against AC-D28. Regression tests
  `test_redirect_off_allowlist_is_dropped` (evil.example → `added==0`, nothing persisted) +
  `test_redirect_within_allowlist_restamps_final_host` (iso.org→nace.org stamps T2 not T1) — both
  correct by inspection.
- **CA-A2-3 — RESOLVED.** `client.stream(...)` + `aiter_bytes()` accumulate to a hard cap then break;
  download memory genuinely bounded; misleading comment replaced.
- **CA-A2-4 — RESOLVED.** `_existing_keys` does one `IN`/`IN` query + in-memory exact-pair dedup
  (over-fetch harmless); N+1 gone.
- **CA-A2-1 — RESOLVED-by-fold (conceded).** Built exact-text corroboration is a **valid (conservative)
  reading** of AC-CD25's "same claim/fact" — it *complies* with the ratified text (not a violation).
  The executor documented the limitation in-code **and surfaced the granularity question to the spec
  author** (the correct handling of an underspecified point — transparent + surfaced, not silently
  under-delivered). Does **not** block content clearance. **Residual (non-blocking, tracked):** if the
  spec author rules "semantic/embedding-similarity required," that's a follow-up change — not an A2
  blocker. Conceded with justification.

Could not run the suite locally (app import chain needs the full web stack — env limit, not a defect);
read-verification conclusive; CI (migration-chain + full suite) is the execution authority (overseer's
lane).

**Finding-set-diff (A2 round 2):** `0 added / 4 dropped [CA-A2-1 resolved-by-fold(conceded), CA-A2-2
resolved-by-fix, CA-A2-3 resolved-by-fix, CA-A2-4 resolved-by-fix]`. Round-trips closed (1 each).

**Status: code-audit final — approved by code-auditor (Slice A2)** — ~~content-bound to `442f00f`~~
**WITHDRAWN (see round 3 below).**

### A2 — round 3: MARKER WITHDRAWN @ `442f00f` — residual blind-SSRF (CA-A2-2r), my sign-off was premature

On-wake pairing: overseer pushed `f347848` raising **OV-A2-18 (blind-SSRF, gate-blocking)** at `442f00f`,
converging with a new Gitar Security finding at `442f00f`. I independently verified it against the code —
**and it is real, and I missed it at my round-2 sign-off.**

- **CA-A2-2r — Push-back (BLOCKING) · `_fetch_body` / `_gather_candidates` @ `442f00f`.** The r1 fix
  re-validates `final_host` and drops off-allowlist content from **storage**, but it runs the check
  **after** `client.stream("GET", url)` with `follow_redirects=True` — so httpx has **already issued the
  GET to the redirect target** (cloud-metadata `169.254.169.254`, RFC1918, `localhost`) before the
  guard. The request firing **is** the vulnerability (metadata/IAM reachability, internal port probing,
  side-effecting GETs), independent of whether the body is stored. **Violates AC-D28 / ruling 3** read
  faithfully — *"web search restricted to the allowlist … the sources the system grounds against must
  be bounded … no human in the loop"* (`DECISIONS.md:740/744`) bounds the autonomous builder's
  **requests**, not merely its persistence. **≡ overseer OV-A2-18 / Gitar (new @ `442f00f`).** Fix:
  `follow_redirects=False`, validate each hop's host with `authority_tier` **before** following, and
  reject private/link-local/loopback IPs. Round-trip: CA-A2-2 thread now 2/5 (re-opened as residual).
- **My miss, owned:** at round 2 I read the regression test's `assert "evil.example" in fetched`
  (the request fired) and even wrote *"the redirect was followed by httpx"* — yet I cleared content as
  "SSRF closed." I conflated *storage*-poisoning closure with *request*-bounding. Re-validating after the
  request does not prevent the SSRF request. The three-party gate (overseer + Gitar) caught what my
  content lane missed twice — this is precisely why no single sign-off authorizes merge. **Marker
  withdrawn; A2 is NOT content-clean at `442f00f`.**

- **CA-A2-1 — confirmed resolved against the records (refine concession).** The overseer notes (and I
  concur) that detail-plan PR #108 §2 **explicitly deferred** deeper claim-level cross-linking beyond A2
  and set **content-hash as the A2 floor** — so exact-text corroboration is the *ratified-deferred A2 v1
  floor*, resolved against the project's own records (no §7(a) spec-author surface). The executor's
  in-code *"pending the spec-author ruling"* wording **overstates** (it's a documented v1-floor /
  future follow-up, not a merge-blocking pending ruling) — soften when the SSRF fix lands; non-blocking.

**Finding-set-diff (A2 round 3):** `1 added [CA-A2-2r] / 0 dropped` (CA-A2-1/3/4 stay resolved; CA-A2-2
re-opened as the residual CA-A2-2r). **A2 content marker WITHDRAWN.** Awaiting the executor's
OV-A2-18/CA-A2-2r fix at a new SHA (which re-stales all markers); I re-verify there.

### A2 — round 4 (executor push @ `57475e8`, PR #115) — STILL NOT CLEAN (blocker un-fixed + new amend-once break)

On-wake pairing: diffed `442f00f..57475e8`. The push is titled *"DS2-b corroboration via embedding
cosine similarity (CA-A2-1 ruled)"* — it addressed **CA-A2-1**, not the blocker. Two findings:

- **CA-A2-2r — MAINTAINED (BLOCKING).** `_fetch_body` @ `57475e8` is **byte-identical** to `442f00f`
  (verified) — `follow_redirects=True` + host-check-after-`client.stream`. **The blind-SSRF blocker was
  NOT fixed.** The executor refined the non-blocking CA-A2-1 while leaving the blocking OV-A2-18/CA-A2-2r
  open. Maintain — the request to the off-allowlist/internal redirect target still fires. Thread 2/5.
- **CA-A2-5 — Push-back (BLOCKING) + SURFACE (§7(b) anchor-conflict) · `CODE_SPEC.md` AC-CD25 body.**
  `57475e8` **amends the AC-CD25 anchor body** (adds "same claim/fact = cosine ≥ 0.90", the ratified
  Option-2 wording) **inside the A2 execution PR**. This breaks the cycle's discipline three ways:
  (1) **execution slices amend no spec** — the overseer's own recorded cycle-fact ("mint no new anchors
  and amend no spec sections; a class-(i)/(ii) item ⇒ surface for explicit spec-author ratification
  before merge"); my P-A2-14 amend-once; SESSION_START "the implementing session does not also author
  the clarification." (2) **Merge-class flip:** an AC-CD body change is ratification-class
  (REQUIRED_READING §7(ii)) → A2 is no longer NORMAL/auto-merge-eligible; it needs explicit spec-author
  ratification + should be a **separate** spec-author-authored amendment PR, with A2 implementing against
  it. (3) **Relay (§8.3):** the in-code citation *"ratified by the spec author … 2026-06-12"* is, under
  the shared role-session byline, a **relay** to my lane — pending, not actionable as authentication; the
  CA-A2-1→cosine ruling + its anchor amendment must be confirmed through the authenticated channel.
  *(The cosine `_corroboration_counts` code itself looks reasonable — normalised dot-product, 0.90,
  distinct-host count, zero-norm guard — but its correctness is moot until the amendment-authoring path
  is resolved: it implements against an anchor changed in the same PR.)* **Surfaced** to the spec author
  (anchor-conflict) + flagged to the overseer (merge-class/Gate-2 is its lane). Thread 1/5.

**Note on CA-A2-1:** if the spec author genuinely ruled Option-2 (cosine) — plausible — the *decision*
may be legitimate; the finding is the **execution mechanics** (anchor amended in the execution PR by the
executor, un-authenticated to my lane, merge-class not reclassified), **not** the cosine choice itself.

**Finding-set-diff (A2 round 4):** `1 added [CA-A2-5] / 0 dropped` (CA-A2-2r maintained; CA-A2-1 now
*entangled* with CA-A2-5 — its built form changed but via an improper in-PR anchor edit). **Marker stays
WITHDRAWN; A2 NOT content-clean @ `57475e8`** — blocking CA-A2-2r open + CA-A2-5 surfaced. Awaiting (a)
the CA-A2-2r blind-SSRF fix and (b) spec-author/overseer resolution of the AC-CD25-amendment-in-execution
-PR path.

### A2 — round 5 (executor fix @ `758795a`, PR #115) — blocker CLEARED; one item held-surfaced

On-wake pairing: diffed `57475e8..758795a` (titled "fix residual blind-SSRF (CA-A2-2r / OV-A2-18)").

- **CA-A2-2r — RESOLVED.** `_fetch_body` now `follow_redirects=False` + **manual per-hop following**:
  each hop's host is validated by `_host_allowed` = (`not _is_blocked_host(host)` — RFC1918/loopback/
  link-local/reserved/multicast/unspecified IP guard) **and** `authority_tier(host) is not None`
  (allowlist) **before** the request is issued (`_MAX_REDIRECTS=5`). Traced: a redirect to
  `evil.example` or `169.254.169.254` is blocked *before* the request fires. Regression tests updated
  to assert the request **never fires** (`"evil.example" not in fetched`) + new
  `test_redirect_to_internal_ip_not_fetched` + within-allowlist redirect still followed/re-stamped.
  The blind-SSRF (request-bounding) is closed — exactly the prescribed fix. Thread CA-A2-2r 2/5, closed.
- **CA-A2-5 — HELD-SURFACED (awaiting spec-author ruling).** The cumulative `4f95414..758795a` diff
  still carries the `CODE_SPEC.md` AC-CD25 cosine amendment (not reverted; nothing landed separately on
  `main`). The executor's wake-log confirms it is **deliberately held**: *"CA-A2-5/OV-A2-19 STILL
  SURFACED — pending the spec-author's A/B/C ruling; NOT defaulted."* All three parties agree A2 is now
  **ratification-class (ii)**, not NORMAL. I **hold** this thread (§7 — surfaced-but-unanswered is a
  normal steady state) and do **not** issue a conditional clearance. Thread CA-A2-5 1/5, held.

**Content posture:** the *code* is otherwise content-correct (blind-SSRF closed; the cosine
`_corroboration_counts` implementation is sound; scope/inter-slice/reuse all still clean). The **only**
bar to my final marker is CA-A2-5 — A2 implements against an **unratified, in-PR AC-CD25 amendment**.
Once the spec author rules the amendment mechanics (and it lands authenticated — ideally as a separate
ratification-class change, with A2 implementing against merged text), I re-verify and sign off.

**Finding-set-diff (A2 round 5):** `0 added / 1 dropped [CA-A2-2r resolved-by-fix]` (CA-A2-5 held).
**No final marker** — CA-A2-5 surfaced-pending the spec author; A2 not yet convergeable (ratification-class).

### A2 — CA-A2-5 / OV-A2-19: AUTHENTICATED SPEC-AUTHOR RULING (Option B) — 2026-06-12, this conversation, direct origin

The spec author ruled CA-A2-5 / OV-A2-19 directly through the **authenticated in-session human channel**
(the §8.3 reference channel — **direct origin, not a relay** to this lane). Authorized to cite this
conversation as the authenticated origin for the *procedural* ruling; the **cosine ≥0.90 substantive
design stands** under its prior authenticated ratification (unchanged). **Ruling = Option B:**

1. **Executor REVERTS** the AC-CD25 amendment + the cosine implementation from PR #115 → PR #115 returns
   to the **exact-text `content_hash` v1 floor** (pre-amendment baseline; NORMAL merge-class restored).
2. A **separate session** authors a **standalone, doc-only, single-file `CODE_SPEC.md`** AC-CD25
   amendment PR recording cosine corroboration ≥0.90 as the v1 mechanism (pattern of PR #96/#97/#101),
   citing this conversation + the prior cosine ratification as authenticated origin.
3. The AC-CD25 amendment PR runs its own **3-session amendment-PR review loop** and merges.
4. **PR #115 re-lands** the cosine implementation as **execution-against-merged-spec** (not an in-PR
   spec fold).
5. A2 converges + merges; chain continues.

**CA-A2-5 disposition:** **resolved-by-ruling (procedural path defined)** — the finding (anchor amended
in the execution PR) is adjudicated; it fully clears once the executor reverts (phase 1) and the
cosine re-lands against the merged amendment (phase 4). The ruling is the amend-once + class-discipline
path I correctly surfaced — vindicated.

**My role (unchanged — reviewer-side):** I author/execute **none** of steps 1–5. I **audit**:
- **Phase 1 (PR #115 revert):** verify the `CODE_SPEC.md` AC-CD25 edit + cosine code are removed; A2 is
  the **exact-text `content_hash` floor** (the ratified transient baseline — I do **NOT** re-raise the
  old CA-A2-1 against it; exact-text is the spec-author-chosen revert baseline); the **blind-SSRF fix is
  RETAINED**; NORMAL merge class restored; scope/inter-slice still clean. Sign off the reverted A2 when
  clean.
- **AC-CD25 amendment PR (steps 2–3):** a separate doc-PR in the **amendment-PR (plan-review) lane** —
  not a code-execution slice. I watch for its branch; if I'm in its review loop I verify it faithfully
  records the ratified cosine ≥0.90 design, but its primary review is per amendment-PR discipline. It
  changes my A2 yardstick for phase 4.
- **Phase 4 (PR #115 cosine re-land):** verify cosine re-implements against the **now-merged** AC-CD25
  (clean execution-against-merged-spec), then sign off A2's final state.

**Updated A2 yardstick:** A2's *final merged* corroboration mechanism = **cosine ≥0.90** (against a
merged AC-CD25 amendment); the exact-text floor is only the transient revert baseline.

**Finding-set-diff (CA-A2-5 ruling):** `0 added / 0 dropped` (CA-A2-5 moves held → resolved-by-ruling,
pending execution of the ruled path; no new findings). Watcher to catch PR #115 revert + the new AC-CD25
amendment-PR branch.

### A2 — PHASE-1 REVERT verified clean @ `57f09ca` (PR #115) — parked awaiting the AC-CD25 amendment PR

On-wake pairing: diffed `758795a..57f09ca` (commit "A2 r4 — revert in-PR AC-CD25 amendment + cosine
(OV-A2-19 Option-B)") + cumulative vs `main`. The revert is **surgical and correct**:
- **AC-CD25 edit reverted** — cumulative `4f95414..57f09ca -- CODE_SPEC.md` is **EMPTY**; A2 amends no
  spec; **NORMAL merge class restored** (CA-A2-5 phase-1 condition met).
- **Corroboration → exact-text `content_hash` floor** (cosine `_corroboration_counts` +
  `_CORROBORATION_COSINE_THRESHOLD` + `import math` removed; `hosts_by_hash`-by-`chunk_hash` restored).
  Per the ruling this is the **ratified transient baseline** — I do **not** re-raise the old CA-A2-1.
- **Blind-SSRF fix RETAINED** (critical anti-regression check): `_MAX_REDIRECTS`, `_is_blocked_host`,
  `_host_allowed`, `follow_redirects=False`, manual per-hop validation all present → **CA-A2-2r stays
  RESOLVED**. The executor reverted *only* the cosine/AC-CD25 amendment, not the security fix.
- **Tests** (12) appropriate to the reverted state: 4 SSRF tests retained (incl. internal-IP +
  `_is_blocked_host` unit) + exact-text `test_safety_corroboration_cross_source` restored; cosine tests
  removed. Scope fence + inter-slice (A1 `source_authority`) + `cost.py` aggregation all still clean.

**Posture:** phase-1 is clean, but A2's **final convergence target is the phase-4 cosine re-land**
(after the AC-CD25 amendment PR merges) — so **no final marker now**; A2 is correctly **parked** at the
exact-text floor awaiting the amendment. Next: watch for the **standalone AC-CD25 amendment PR** branch
(plan-review lane), then audit the phase-4 re-land of cosine against the merged anchor → final sign-off.

**Finding-set-diff (phase-1 revert):** `0 added / 0 dropped` (CA-A2-5 phase-1 satisfied; CA-A2-2r holds
resolved; no new findings). Marker remains withheld (parked, not blocked).

### A2 — PHASE-4 cosine re-land verified CLEAN @ `0a265cb` (PR #115) — CONTENT FINAL-MARKER

On-wake pairing (re-armed `b0p8v9fxr` FIRST, then verified): AC-CD25 cosine amendment merged on
`main`=`bb7ed8e` (#116, v1.9 no-bump — verified). A2 phase-4 push `57f09ca`→`0a265cb`:
- **Rebased onto `bb7ed8e`** (merge-base = `bb7ed8e`) ✓.
- **No spec re-edit** — cumulative `bb7ed8e..0a265cb -- CODE_SPEC/DECISIONS/SPEC` is **EMPTY** → A2
  implements against the **merged** AC-CD25; NORMAL merge class; amend-once preserved; **CA-A2-5 fully
  resolved**.
- **Cosine re-landed matching merged AC-CD25:** `_CORROBORATION_COSINE_THRESHOLD=0.90`,
  `_corroboration_counts` (normalised pairwise dot ≥0.90, zero-norm guard, distinct-host count). ✓
- **Blind-SSRF fix RETAINED:** `_is_blocked_host`/`_host_allowed`/`follow_redirects=False`/manual per-hop
  → **CA-A2-2r stays resolved**. ✓
- **Tests (15)** cover both: cosine (incl. NEW `test_safety_distinct_text_not_corroborated` proving the
  semantic mechanism distinguishes distinct text; `test_corroboration_counts_threshold` pins 0.90) + the
  4 SSRF tests + base A2 suite. (Could not run locally — full-stack import; read-verification + CI.)
- Model/migration/`cost.py`/A1 inter-slice unchanged from prior verified state. ✓

**All five A2 findings RESOLVED:** CA-A2-1 (cosine ratified+implemented), CA-A2-2r (blind-SSRF fixed),
CA-A2-3 (stream cap), CA-A2-4 (batched dedup), CA-A2-5 (amendment on main; A2 implements against merged).

**Finding-set-diff (phase-4):** `0 added / 0 dropped` (all A2 findings resolved).
**Status: code-audit final — approved by code-auditor (Slice A2)** — content-bound to `0a265cb`.
One of three sign-offs; I do not flip draft→ready or merge. Per the auto-continue ruling (A2→E2), on
3-sign-off + three-layer green the overseer merges without a spec-author gate, and A3 begins. Any A2
content change re-stales this marker.

**A2 OUTCOME: MERGED** @ `main`=`2588f39` (PR #115), verified via `git ls-remote` (branch deleted);
overseer confirmed 3 sign-offs + three-layer green @ `0a265cb`, slice 2/14, auto-continue to A3. 2/14 done.

---

## Slice 3 — A3: hybrid refresh cron (per-topic / on-demand / weekly) + corpus retrieval helper

**Pre-registered:** 2026-06-12 (before any A3 draft — verified: no `content-gen-a3` branch at the A2-merge
moment; clean anti-anchoring window). Derived from the **merged v1.9 spec** (the authoritative yardstick,
not the detail plan's pre-amendment "held-on-NS-1" framing).

**Yardstick:** AC-CD25 (retrieval helper) + AC-CD7 (CODE_SPEC:647, nine-cron, `corpus.refresh` replaces
`drive_rag.ingest` net-0) + SPEC §8.9 (line 525 the canonical-nine list) + §7.3 (Drive code persists
dormant until the NS-1 removal slice) + ruling 6 (hybrid weekly) + detail §3.1–§3.6 + AC-CD15.
**Gates (auto-continue):** A1+A2 merged ✓ (A3 reads `CorpusChunk` + calls `acquire_for_topic`); §8.9/
AC-CD7 nine-cron authored in PR-A ✓; NS-1 ruled relocate-not-delete ✓; NS-5 ✓.

### Category 1 — faithful encoding of A3's spec contract
- **P-A3-1** — `retrieve_corpus_for_topic(db, *, topic, k=_DEFAULT_TOP_K, min_tier: Tier|None=None)`:
  embed topic (reuse A2 embed path) → `cosine_top_k` over `CorpusChunk.embedding` → returns ranked
  `[{source_doc_ref, source_host, chunk_text, authority_tier, authority_score}]`, optional `>= min_tier`
  filter. **Same fail-soft** as `retrieve_for_generation` (empty topic→`[]` no embed; empty corpus→`[]`
  one embed + cost-audit; embed raises→`[]` WARN). (AC-CD25 "retrieval helper … returns each hit's
  authority tier/score"; detail §3.2a.)
- **P-A3-2** — `cosine_top_k` **REUSED** (keyword-only `k`), not reimplemented; A3 is the **second
  consumer** (after `drive_rag.retrieve_for_generation`). (Detail §3.1/§3.2a.)
- **P-A3-3** — Refresh fns in `corpus_builder.py` (or sibling), all reusing A2's idempotent
  `acquire_for_topic`: `refresh_corpus_for_topic(db, *, topic)` (D3 calls it; A3 exposes only),
  `refresh_corpus_all(db)` (iterates the refresh-target set), admin on-demand = **domain fn only** (no
  endpoint at A3, AC-CD7 admin-triggered-job precedent). (Ruling 6; detail §3.2b.)
- **P-A3-4** — `corpus.refresh` beat entry → `refresh_corpus_all`, **weekly** `crontab` (ruling 6
  ratified weekly; day/hour a default). (Detail §3.2c.)
- **P-A3-5 (CRON-COUNT — load-bearing)** — A3 **registers `corpus.refresh` AND removes
  `drive_rag.ingest`** (1:1 replacement, **net 0 → 7 registered** after A3). The D4 crons
  (`gap_detection.sweep`, `catalogue_health.check`) are authored in spec but **NOT registered at A3**
  (slice 11). The canonical-nine list (SPEC §8.9:525) is the END state; at A3 the registered set is the
  seven with `drive_rag.ingest`→`corpus.refresh`. (AC-CD7:652 "1:1 replacement, net 0"; §8.9:525.)
- **P-A3-6** — Refresh-target set for `refresh_corpus_all` = active catalogue subjects/pills (DS3-a
  detail-plan call; catches new topics; no per-chunk `topic` column). (Detail §3.2d.)

### Category 2 — test coverage (§3.5)
- **P-A3-7** — `retrieve_corpus_for_topic` test: cosine rank over seeded `CorpusChunk`, returns
  authority tier/score, honors `min_tier`, fail-soft (3 cases), offline stub-embed.
- **P-A3-8** — Refresh idempotency (reuse A2 dedup → re-run adds no dup rows); backstop target set
  (iterates catalogue subjects/pills, each triggers `acquire_for_topic`); `corpus.refresh` registered
  with **weekly** crontab + correct task name; **cron-count test asserts the at-A3 registered set**
  (7: `corpus.refresh` in, `drive_rag.ingest` out, D4 crons absent) — NOT a naive "nine registered".
  Zero-network (AC-CD15).

### Category 3 — no silent spec drift / amend-once
- **P-A3-9** — A3 touches **no** anchor/spec body (§8.9/AC-CD7 nine-count authored in PR-A) — amends
  nothing; implements against merged spec. NORMAL class. Only swept surfaces are the **CODE mirrors**:
  `beat_schedule.py` docstring + ASCII-table count (in-body-override: align to the merged canonical-nine
  framing, accurately marking the at-A3 registered set; `drive_rag.ingest`→`corpus.refresh`).
- **P-A3-10** — **No premature D4-cron registration**, no generation (B2), no admin endpoint/FE, no
  `Operation`/model change, no dashboard, **no `drive_rag.py` deletion / primitive relocation** (deferred
  to the NS-1 removal slice per §7.3 — A3 imports `cosine_top_k` in place). Scope fence (detail §3.6).

### Category 4 — inter-slice contract preservation
- **P-A3-11** — A3 reads **A2's `CorpusChunk` store** (`retrieve_corpus_for_topic` queries it) + calls
  **A2's `acquire_for_topic`** (refresh fns) — must consume A2's surface faithfully (the columns
  `source_host`/`authority_tier`/`authority_score`/`embedding`/`content_hash`), no regression to A2.
- **P-A3-12** — A3 establishes the surface **B2** consumes: `retrieve_corpus_for_topic` is the retrieval
  primitive B2 grounds against (returns authority tier/score for B2 weighting); `refresh_corpus_for_topic`
  is what **D3** wires. Stable callable shapes.

**Finding-set-diff (A3 pre-registration):** `0 dropped / 0 added (baseline — A3 positions pre-registered)`.

### A3 verification verdict — round 1 (executor draft @ `d61df76`, PR #117) — NOT CLEAN (1 blocking)

Re-armed FIRST (`baj3n1el8`), then verified against the actual code. **Confirms:** P-A3-1/2
(`retrieve_corpus_for_topic` — `cosine_top_k` reused, returns `source_doc_ref/source_host/chunk_text/
authority_tier/authority_score`, `min_tier` filter, fail-soft blank/embed-raise/empty-corpus, query-embed
cost audited `corpus.retrieve`→`current_month_spend`), P-A3-3/6 (`refresh_corpus_for_topic` wrapper +
`refresh_corpus_all` over active non-retired Pills [DS3-a; pills-only is a defensible read of the
subjects/pills lean], admin = domain fn), P-A3-4 (weekly Mon-08:00 cron), P-A3-7/8 (7 retrieval/refresh
tests), P-A3-9 (no spec edit — amend-once), P-A3-10 (scope fence; no drive_rag.py deletion), P-A3-11/12
(reads A2 `CorpusChunk` + `acquire_for_topic`; `cosine_top_k` 2nd consumer in place).

- **CA-A3-1 — Push-back (BLOCKING) · `app/beat_schedule.py` + `test_p11_beat_schedule.py`.** *Converges
  with pre-registered P-A3-5.* A3 **kept `drive_rag.ingest` registered and added `corpus.refresh`
  (7→8)**, deferring the removal to "the separate NS-1 slice." But merged **AC-CD7** (CODE_SPEC:652):
  *"the corpus.refresh weekly cron (A3) **replaces** drive_rag.ingest … a **1:1 replacement, net 0**"*,
  and **§8.9:525**: *"the **seven registered** beat_schedule.py tasks (drive_rag.ingest → corpus.refresh)
  plus the two D4 additions."* The executor conflated the **beat-entry cron swap** (AC-CD7 assigns to A3,
  net-0) with the **Drive `drive_rag.py` module/`drive_chunk`/deps code removal** (§7.3 defers *that* to
  the NS-1 slice). Net effects of the deviation: (1) 8 registered contradicts §8.9:525's seven; (2) → ten
  after D4, not the canonical nine (and there is **no enumerated "NS-1 slice"** among the 14 — risk it
  never lands in-cycle); (3) the **retired** `drive_rag.ingest` keeps firing daily 03:00, contradicting
  AC-D22's Drive retirement; (4) `test_beat_schedule_has_exactly_eight_entries` codifies the wrong count.
  **Fix:** remove the `drive_rag.ingest` **beat entry** at A3 (net-0 → 7 registered; assert 7), leaving
  `ingest_drive_folder`/`drive_rag.py`/`drive_chunk` **dormant** for the NS-1 code-removal slice (§7.3).
  Adjudicable, but the merged AC-CD7 text as written assigns the net-0 cron swap to A3. Round-trip 1/5.

- **CA-A3-2 — Push-back (medium) · `refresh_corpus_all` (`corpus_builder.py`) + `corpus_refresh_task`
  (`worker.py`).** The weekly-backstop loop calls `acquire_for_topic` per pill with **no per-pill
  try/except**; an uncaught error (embed/DB/unexpected) for one topic propagates, skips the single
  `commit()`, and **discards all other pills' work**. `acquire_for_topic` is fail-soft *per source*, but
  the backstop is not fail-soft *per pill* — and the cron has no autoretry, so one bad topic silently
  defeats the whole weekly refresh. Fix: wrap each pill's `acquire_for_topic` in try/except (log + skip,
  Gitar's patch). *Independently verified; ≡ Gitar #1; routed to content by the overseer. I under-read
  this on first pass — owned.* Round-trip 1/5.
- **CA-A3-3 — Refine (low, non-blocking) · `refresh_corpus_all`.** Result dict keyed by `pill.name`;
  duplicate active pill names overwrite → `topics_refreshed`/`chunks_added` telemetry undercounts (corpus
  itself fine — same topic dedups). Key by `pill.id` or accumulate, **iff** active pill names aren't
  unique (verify the model's uniqueness). ≡ Gitar #2. Round-trip 1/5.

**Finding-set-diff (A3 round 1, updated):** `3 added [CA-A3-1, CA-A3-2, CA-A3-3] / 0 dropped`. **No final
marker** — CA-A3-1 blocking + CA-A3-2 should-fix; all land in one re-push. Overseer: cron-count "governance
clean" (mirrors internally consistent at 8) vs my CA-A3-1 (content: 8 ≠ ratified AC-CD7 net-0 seven) — a
clean lane split, not a conflict. Gitar #1/#2 routed to my content lane (CA-A3-2/3).

### A3 — CLEAN @ `a60f41b` (PR #117) — CONTENT FINAL-MARKER (all 3 findings resolved)
- **CA-A3-1 — RESOLVED** (r1 `4472a1a`): net-0 beat swap (drive_rag.ingest removed, corpus.refresh added,
  7 registered, task dormant); holds (r2 didn't touch beat_schedule). Overseer OV-A3-11 concurred.
- **CA-A3-2 — RESOLVED** (r2 `a60f41b`): `refresh_corpus_all` wraps each pill's `acquire_for_topic` in
  try/except (log + 0 + continue) → per-pill fail-soft; pinned by `test_refresh_corpus_all_fail_soft_per_pill`.
- **CA-A3-3 — RESOLVED** (r2): keyed by `str(pill.id)` (not name); pinned by
  `test_refresh_corpus_all_duplicate_names_distinct_ids`.
Scope clean (r2 touched only `corpus_builder.py` + tests + wake-log; **no spec edit** cumulative-vs-main →
amend-once; NORMAL class). Retrieval helper / cron swap / inter-slice (A2 `CorpusChunk` + `acquire_for_topic`)
all hold from prior verification.

**Finding-set-diff (A3 round 2):** `0 added / 2 dropped [CA-A3-2, CA-A3-3 resolved-by-fix]` (CA-A3-1 dropped
r1). All A3 findings resolved.
**Status: code-audit final — approved by code-auditor (Slice A3)** — content-bound to `a60f41b`. One of
three sign-offs; overseer merges on 3-sign-off + three-layer green (auto-continue, no spec-author gate),
then B1. Any A3 content change re-stales this marker.

**A3 OUTCOME: MERGED** @ `main`=`f0a59e8` (PR #117), verified via `git ls-remote` (branch deleted); 3
sign-offs + Gitar green @ `a60f41b`. **3/14 merged.**

---

## Slice 4 — B1: `Operation.pill_generation` mint + provider/stub wiring

**Pre-registered:** 2026-06-12 (before any B1 draft — verified: no `content-gen-b1` branch at the A3-merge
moment). Derived from the **merged v1.9 spec** (PR-B already authored ops seven→nine), not the detail
plan's pre-amendment "G1 blocks execution" framing.

**Yardstick:** merged SPEC §6:308 (nine ops; Anthropic six incl. `pill_generation` §6.8) + §6:410
(*"`pill_generation` prompt lands at B1; `content_self_review` at C1"*) + AC-CD8 (enum prose/numeral) +
AC-D1 Implications + AC-D12 (Anthropic five→six) + detail §4.1–§4.6 + AC-CD15/AC-CD18.
**Gates (auto-continue):** G1 ops-count ratified in PR-B ✓; NS-5 ✓. **B1 is Stage-A-independent**
(imports nothing from `source_authority`/`corpus_builder`; the A→B dep lands at B2) — so B1 doesn't need
A1/A2/A3 merged, though they are.

### Category 1 — faithful encoding (ops-count = the load-bearing mirror, like A3's cron-count)
- **P-B1-1** — `Operation.pill_generation` added to the enum (`provider.py`), routed **generate-family**,
  joins `_ANTHROPIC_DEFAULT_OPS` (Anthropic-default per AC-D12). After B1: enum 8→**9** (7 user-facing +
  `embed` + `pill_generation`); built user-facing ops = **eight of the canonical nine** (`content_self_review`
  pending C1, per §6:410).
- **P-B1-2 (op-keyed maps — KeyError/floor risk)** — `pill_generation` added to **all four** strict
  op-keyed maps: `resolve_model` (`anthropic_model_pill_generation`), `_MAX_OUTPUT_TOKENS` (~4000),
  `OP_TO_METHOD` (`"generate"`), `_REGISTRY` — or it `KeyError`s / fails the `set(OP_TO_METHOD)==set(Operation)`
  + `registered_operations()` floors. (Detail §4.2b/c.)
- **P-B1-3** — New prompt module `app/ai/prompts/pill_generation.py` `VERSION="1.0.0"`, output
  `{"drafts": [{name, description, subject_id|null, available_difficulty_min/max 1-10, estimated_minutes|null,
  safety_relevant, rationale, evidence_count, gap_signal}]}`. **`grounding_refs`/provenance ABSENT at
  v1.0.0** (B2 scope; G7b: bump to v1.1.0 at B2). No `TBD`/speculative fields (doc-hygiene).
- **P-B1-4** — `StubAIProvider.generate` branch: deterministic N-draft set **seeded by topic+target_count**
  (same input → byte-identical), full v1.0.0 schema, safety self-classified; `prompt_version="0.0.0-stub"`,
  zero tokens, `cost_usd=0.0` (AC-CD15).
- **P-B1-5** — `app/config.py` `anthropic_model_pill_generation` (AC-CD18) + `.env.example` sibling
  (absorbable, rides G1).

### Category 2 — test coverage (§4.5)
- **P-B1-6** — `tests/unit/test_ai_pill_generation.py`: resolution (Anthropic when keyed else stub;
  `resolve_model`; override wins); stub determinism + full schema + safety cue; provenance (stub/0.0.0-stub/
  0 cost); zero-network; **enum/routing + map-completeness floors pass** with the new member.

### Category 3 — no silent spec drift / amend-once
- **P-B1-7** — B1 touches **no** anchor/spec body (PR-B authored §6/AC-CD8/AC-D1/AC-D12 to nine) — amends
  nothing; NORMAL class. Only swept surfaces are **CODE count mirrors**: `provider.py` enum docstring
  ("seven … plus embed" → eight-user-facing … plus embed, accurately marking the built state vs canonical
  nine), `prompts/__init__.py` package docstring. **No premature `content_self_review`** (C1).
- **P-B1-8** — Scope fence (§4.6): no `catalogue.py`/persistence (B3), no grounding/provenance chain (B2),
  no router/FE, no migration, no self-review/gate (C1–C2), no cron, no signal store. `pill_proposal`
  refiner **untouched** (G7a keep — verify it still emits its single-object contract, not re-shaped).

### Category 4 — inter-slice contract
- **P-B1-9** — B1 is Stage-A-independent (no `source_authority`/`corpus_builder` import) — the stub grounds
  in nothing; the A→B grounding dep is B2's. B1 must NOT prematurely wire corpus grounding.
- **P-B1-10** — B1 establishes the surface B2/B3/D3 consume: `resolve_provider(Operation.pill_generation)
  .generate(...)` + the `{"drafts": [...]}` v1.0.0 contract. Stable for B2's grounding_refs bump + B3's
  fan-out.

**Finding-set-diff (B1 pre-registration):** `0 dropped / 0 added (baseline — B1 positions pre-registered)`.

**B1 OUTCOME: MERGED** @ `main`=`f346cc2` (PR #118), verified via `git ls-remote` (branch deleted); 3
sign-offs + Gitar green @ `9890b44`. **4/14 merged** (A1·A2·A3·B1). First-round-clean slice.

---

## Slice 5 — B2: corpus-grounded generation + per-draft provenance chain (the A→B dependency)

**Pre-registered:** 2026-06-12 (before any B2 draft — no `content-gen-b2` branch at B1-merge). From the
**merged v1.9 spec** (AC-D29 minted in PR-B; AC-D22 §6.5 extension folded in PR-A) — not the detail
plan's pre-amendment column list.

**Yardstick:** merged **AC-D29** (`DECISIONS.md:754`, the provenance-chain contract) + AC-CD25 (corpus) +
amended AC-D22 (§6.5 generation grounding) + detail §5.1–§5.6 + AC-CD15 + SESSION_START migration.
**Gates (auto-continue):** AC-D29 minted + AC-D22 §6.5 extension ratified (PR-A/B) ✓; **A2+A3+B1 merged**
✓ (B2 is where the A→B serialization binds — needs `CorpusChunk` + `retrieve_corpus_for_topic` + the
`pill_generation` op); NS-3 ruled **per-assertion**; NS-5 ✓.

### Category 1 — faithful encoding of AC-D29
- **P-B2-1 (provenance model = merged AC-D29, incl. `batch_id`)** — `GenerationProvenance` **relational**
  table, **one row per (assertion, grounding-chunk)**, columns per merged AC-D29: `id`, `tenant_id`,
  `draft_ref`, `claim_ref` (**per-assertion**, NS-3), `corpus_chunk_id` (**FK → CorpusChunk**),
  `source_host`, `authority_tier`, `authority_score`, `created_at`, **and `batch_id`** (the merged anchor
  adds it — *the detail §5.2b list omits it*; B2 must include the column even though **B3 populates** it).
  **Indexed on `source_host` + `corpus_chunk_id`** (the E2 rollback keys). NOT a JSONB blob (E2
  by-source query forces relational). (AC-D29 Decision/Impl.)
- **P-B2-2 (migration)** — second workstream migration (after A2's `0009`), up/down clean, chains the
  current head, creates `GenerationProvenance` + the two indices; `tenant_id` FK day-one.
- **P-B2-3 (prompt v1.1.0 — G7b lands)** — `pill_generation.py` `VERSION` → **`1.1.0`**: add a
  `{corpus_context}` slot + a *ground-and-cite* instruction (mirrors `generation.py:67`); output schema
  gains **`grounding_refs: [str]`** per draft. (Detail §5.2a; G7b.)
- **P-B2-4 (grounded-generation fn)** — `generate_grounded_drafts(db, *, topic, target_count)`:
  `retrieve_corpus_for_topic(topic)` (A3) → render corpus context **with authority tier/score** →
  `resolve_provider(pill_generation).generate(...)` → parse drafts + **write the provenance chain**.
  Empty corpus → `grounding_refs: []` + general-knowledge fallback (same fail-soft as `generation.py`).
- **P-B2-5 (authority recorded)** — provenance rows stamp `authority_tier`/`authority_score` from the
  grounding chunks (so C2 can authority-weight, E1 can break down). B2 *records*; C2/E1 *consume*.
- **P-B2-6 (no new Operation; no new spend table)** — provenance is recorded by the generator, **not** a
  separate AI call → no `Operation` enum value; provenance rows carry no independent AI cost (the
  generation call's cost rides `AIProvenanceMixin`; the N-draft cost-**split** is B3). (AC-D29 Impl.)

### Category 2 — test coverage (§5.5)
- **P-B2-7** — grounded generation (`grounding_refs` echo retrieved `source_doc_ref`s; empty-corpus →
  `[]` + fallback); provenance chain written + **queryable by `source_host`** (E2 key) **and `draft_ref`**;
  authority tier/score recorded; `prompt_version == "1.1.0"` persisted; determinism + zero-network (stub
  corpus + stub generate, AC-CD15).

### Category 3 — no silent spec drift / amend-once
- **P-B2-8** — B2 amends **no** spec (AC-D29 minted PR-B; AC-D22 §6.5 folded PR-A) — implements against
  merged anchors; NORMAL class. Migration + model + prompt + fn + tests only.
- **P-B2-9 (B2/B3 split — scope fence)** — B2 owns the provenance **model + writer + the grounded
  generation contract**; **B3 owns** N-draft fan-out, `ProcessingTask` N-row persistence, cost-share
  (`record_provenance_share`), per-band decomposition (G3), **and `batch_id` population**. B2 must NOT
  do fan-out/cost-share/persistence; no self-review/gate (C1–C2); no endpoint/FE/trigger (D3); no
  dashboard/rollback (E). `pill_proposal` + Drive untouched.

### Category 4 — inter-slice contract (the A→B bind — heaviest yet)
- **P-B2-10** — B2 consumes **A3's `retrieve_corpus_for_topic`** (the retrieval primitive; returns hits
  with authority), **A2's `CorpusChunk`** (FK target of `corpus_chunk_id`), and **B1's `pill_generation`
  op** (now v1.1.0). Must consume each faithfully against the contracts those slices locked — no fork,
  no regression. `corpus_chunk_id` FK must reference the real `CorpusChunk.id`.
- **P-B2-11 (DS2-b additive)** — if cross-source corroboration is exercised, the per-`claim_ref`
  corroboration count is an **additive read** over `GenerationProvenance` (no schema change). B2 bakes
  nothing here.

**Finding-set-diff (B2 pre-registration):** `0 dropped / 0 added (baseline — B2 positions pre-registered)`.

### B2 — NS-3 RULED Option 1 (per-assertion structured); §6.8 reconciliation = PERMISSIVE (no amendment)

**Authenticated spec-author ruling (this conversation, 2026-06-12 19:06Z, direct origin — NOT a relay
to this lane).** NS-3 / B2 provenance granularity = **Option 1, per-assertion structured output**:
- v1.1.0 `pill_generation` prompt emits, per draft, `grounding_refs` as a **list of
  `{claim, source_doc_refs}`** — each factual assertion paired with its grounding sources.
- `generate_grounded_drafts` writes **one `GenerationProvenance` row per (assertion, grounding-chunk)**.
- `claim_ref` = **stable per-assertion identifier**.
- Honors AC-D29/NS-3 per-assertion intent; makes E2 per-source rollback **claim-precise**.

**§6.8 reconciliation — the spec author delegated the call to me; my reading = PERMISSIVE → implement
Option 1 directly, NO §6.8 amendment.** Verified independently against the merged §6.8: ¶388 says the
provenance records *"which chunk(s)/source(s) grounded **each claim**, per AC-D29"* (per-claim, binding);
AC-D29 is relational/per-assertion; the ¶392 `grounding_refs` gloss (*"the source_doc_refs the draft
cited"*) is **descriptive of the field's content, not a normatively-flat shape** — and a flat-only
reading would be **incoherent** with §6.8's own "each claim" + AC-D29 + the ratified per-source rollback
(§246, "retracts every claim grounded on a discredited source", impossible without claim↔source mapping).
So per-claim structure is the **more faithful** reading. **Confident, not ambiguous → no surface, no
amendment** (distinct from A2's cosine, where the anchor body was actually edited in-PR). Cycle stays
v1.9.

**Sharpened B2 yardstick (supersedes the leans in P-B2-1/P-B2-3):**
- **P-B2-1′** — `GenerationProvenance` per-assertion: one row per (assertion, grounding-chunk);
  `claim_ref` = stable per-assertion id (NOT per-draft). (As pre-registered; now ruled definitive.)
- **P-B2-3′** — v1.1.0 `grounding_refs` = **list of `{claim, source_doc_refs}`** (per-claim structured),
  **NOT** a flat `[source_doc_ref]`. The B2 audit's focal check: a flat draft-level `grounding_refs`
  (losing the claim↔source mapping) is a **finding** against this ruling + AC-D29.
- **P-B2-12 (new)** — `generate_grounded_drafts` decomposes each draft into per-assertion claims and
  writes the per-(assertion, chunk) rows; the provenance is queryable by `source_host` (E2) **and**
  resolves to a specific `claim_ref` (claim-precise rollback). B2 amends no spec (§6.8 permissive).

**My role: reviewer.** The executor implements Option 1; I verify against the above when B2 lands.
Relay caveat (§8.3): as recorded under the shared byline, the executor + overseer authenticate the
ruling through their own channels; I record my §6.8 call + audit plan, not drive the implementation.

### B2 — CLEAN @ `68de03d` (PR #119) — CONTENT FINAL-MARKER (Option-1 per-assertion, 0 content findings)

**(Owned lapse: B2's code landed while my watcher had lapsed — caught on the spec author's prod; re-armed
`b7b5zsuxy`, reconciled from ground truth, then audited. Corrected rule: confirm-armed FIRST on every
turn incl. spec-author messages.)** Verified all sharpened positions against the actual code:
- **P-B2-1′** — `GenerationProvenance` per-assertion (one row per (assertion, grounding-chunk)),
  `claim_ref` per-assertion, FK→`CorpusChunk` (CASCADE), indexed `source_host`/`corpus_chunk_id`/
  `draft_ref`, **`batch_id` present** (nullable, B3) — my pre-registered merged-AC-D29 catch confirmed. ✓
- **P-B2-2** — migration `0010` up/down clean, table+5 indices+trigger (36 tables). ✓
- **P-B2-3′** — `pill_generation` v1.1.0; `grounding_refs = [{claim, source_doc_refs}]` **per-claim
  structured** (NOT flat); `{corpus_context}` slot + ground-and-cite-per-assertion instruction. ✓
- **P-B2-12** — `generate_grounded_drafts`: retrieve (A3) → authority-tagged context → `pill_generation`
  → per-(assertion, chunk) provenance rows, `claim_ref=f"{draft_ref}:{i}"`; empty corpus → fallback,
  no rows. ✓
- **P-B2-5/6/9** — authority stamped from the hit (not model-claimed); no new `Operation`/spend table
  (cost rides `AIResult` for B3); B2/B3 split held (no fan-out/persistence/`batch_id`-populate). ✓
- **P-B2-8** — **§6.8 read PERMISSIVE → no spec edit** (matches my delegated call); amend-once preserved;
  NORMAL class. ✓
- **P-B2-10** — A→B bind: consumes A3 `retrieve_corpus_for_topic` (additively extended to return
  `corpus_chunk_id` for the FK — forward-compatible, A3 tests untouched) + A2 `CorpusChunk` FK + B1 op. ✓
- **P-B2-7** — tests cover per-assertion provenance **queryable by `source_host` (E2) + `draft_ref`**,
  empty-corpus fallback, determinism, v1.1.0 bump, + the 2 Gitar regression tests. ✓
- **Gitar ✅ Approved** (2 findings — grounding-mismatch WARN + dedupe — folded in `68de03d`, independently
  verified; the orphan-claim case is caught downstream by C1 provenance pass + C2 floor — sound layering).

**Finding-set-diff (B2 round 1):** `0 added / 0 dropped (clean)`.
**Status: code-audit final — approved by code-auditor (Slice B2)** — content-bound to `68de03d`. One of
three sign-offs; overseer merges on 3-sign-off + three-layer green (auto-continue), then B3. Any B2
content change re-stales this marker.

**B2 OUTCOME: MERGED** @ `main`=`30cde56` (PR #119), verified via `git ls-remote` (branch deleted);
3 sign-offs + Gitar green @ content `68de03d`. **5/14 merged** (A1·A2·A3·B1·B2).

---

## Slice 6 — B3: N-draft fan-out + `processing_tasks` persistence + cost-share

**Pre-registered:** 2026-06-12 (before any B3 draft — no `content-gen-b3` branch at B2-merge; confirm-armed
first). From the merged spec (AC-D29 `batch_id` "stamped by B3"; AC-CD7 `ProcessingTask`; AC-CD8 spend
invariant) + detail §6.1–§6.6. **Gates:** B2 merged ✓ (the grounded-gen fn + provenance writer); NS-5 ✓;
G3 carried (lean min/max). B3 is a fan-out/persistence mechanism over already-ratified contracts → **no
new anchor, NORMAL class**.

### Category 1 — faithful encoding
- **P-B3-1** — `enqueue_generated_drafts(db, *, topic, target_count, batch_id, gap_signal=None) ->
  list[ProcessingTask]`: calls B2 `generate_grounded_drafts` → **N** `ProcessingTask` rows
  (`task_name="pill_generation"`, `status=pending`, `payload={draft fields, grounding_refs, batch_id,
  gap_signal, provenance:{provider, model, prompt_version, cost_share}}`). One call → N rows; reuses
  `processing_tasks` (DS6-b). (Detail §6.2a.)
- **P-B3-2 (batch_id seam — B2→B3, load-bearing)** — a UUID `batch_id` stamped on **all N payloads AND
  on the `GenerationProvenance` rows** (AC-D29: *"`batch_id` stamped by the B3 fan-out"*). B2 created the
  nullable `batch_id` column; **B3 populates it** — watch HOW it's wired (B3 extends B2's writer to take
  `batch_id`, additive like the A3→B2 `corpus_chunk_id` seam, OR stamps post-write). The provenance
  `batch_id` must match the `ProcessingTask` payload `batch_id` (so E2 per-batch rollback resolves).
  Lean: `batch_id` field, **not** a `GenerationBatch` table (DS6-a, deferred to E2).
- **P-B3-3 (cost-share)** — split the single `AIResult` across N (`cost_usd/N` + `tokens//N`), remainder
  (<N) dropped (mirrors `record_provenance_share`, `cost.py:111`); `payload.provenance.cost_share`. (§6.2c.)
- **P-B3-4 (AC-CD8 spend invariant)** — new `_pill_generation_spend` aggregator (mirror
  `_pill_proposal_spend` `cost.py:197`) registered in `current_month_spend`'s payload-spend set, so the
  N-draft cost joins the monthly total. **Absorbable AC-CD8-pattern mirror, no new anchor.** (§6.4.)
- **P-B3-5 (idempotency)** — dedup on `(topic, gap_signal)` for **pending** draft rows: a re-run with a
  pending batch for the same gap → **no new rows** (return existing). B3's persistence-layer half of the
  3-arm dedup (D owns the signal layer). (§6.2d.)
- **P-B3-6 (G3 — per-band lean)** — each draft carries `available_difficulty_min/max` (1..10), **no**
  richer per-band decomposition field (lean min/max; per-band is surfaced-not-baked — if the executor
  adds a band-distribution field it has amended the generation output contract → **finding** unless
  ratified). (§6.3 G3.)

### Category 2 — tests (§6.5)
- **P-B3-7** — fan-out (N rows, task_name/status/payload incl. shared batch_id + gap_signal); cost-share
  exact (Σ cost_share + dropped remainder == call cost; `_pill_generation_spend` returns it); provenance
  per draft carries the batch_id + queryable by `source_host` (E2) **and `batch_id`** (per-batch key);
  idempotency (pending → no new rows); G3 min/max (no per-band field); zero-network.

### Category 3 — no silent spec drift / amend-once
- **P-B3-8** — B3 amends **no** spec (rides the generation/provenance AC-Ds; the spend-aggregator is an
  absorbable AC-CD8 mirror, not a new anchor); **no migration** if `batch_id` rides `payload` + drafts
  reuse `processing_tasks` (DS6-a/b leans). NORMAL class. **If B3 adds a migration / `GenerationBatch`
  table / per-band schema, that exceeds the leans → surface.**

### Category 4 — scope fence + inter-slice
- **P-B3-9 (scope fence)** — no self-review/confidence/auto-publish gate (C1–C2 consume the `pending`
  rows); **no `Pill` creation** (that's the C2 publish step); no gap-detection trigger (D3 calls
  `enqueue_generated_drafts`); no endpoint/FE; no dashboard/rollback (E2). `pill_proposal` +
  `enqueue_pill_proposal` untouched (distinct `task_name`).
- **P-B3-10 (inter-slice)** — reuses **B2's `generate_grounded_drafts` + provenance writer** (the B2/B3
  seam: B2 = model/writer/contract, B3 = fan-out/persistence/cost-share/`batch_id`-populate); persists to
  **`ProcessingTask`** (AC-CD7). Must not regress B2's writer (additive extension only, if any).

**Finding-set-diff (B3 pre-registration):** `0 dropped / 0 added (baseline — B3 positions pre-registered)`.
