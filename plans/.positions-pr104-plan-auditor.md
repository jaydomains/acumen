# Pre-registered positions — PR #104 plan-auditor (framework self-install)

**Status: final — approved by plan-auditor** (content correctness) @ planner tip `957eda7`;
content-bound to the role-file + skill + REQUIRED_READING content (byte-identical since install
commit `7b3e531`). One of three required sign-offs — convergence + merge are the overseer's lane;
class-(iv) additionally needs authenticated spec-author ratification.

Role: **plan-auditor** (content correctness) for the recursive self-install of the
three-party plan-review framework (`.claude/roles/{planner,plan-auditor,plan-overseer}.md`
+ `.claude/skills/counterpart-change-detector/` + `plans/REQUIRED_READING.md`), draft PR #104,
branch `claude/blissful-babbage-HatwU`. Audit branch: `claude/nice-goldberg-fLQZt`.

## Anti-anchoring provenance (honest record)
These positions were formed and stated **in the session transcript before the draft PR text
was read** (the session reasoned about "what a correct framework installation must do" before
PR #104 even existed, then before fetching its files). The transcript is the timestamped
pre-registration proof. They are committed to this branch *after* the draft read only because
the rulebook (`plan-auditor.md`) and the PR itself did not exist on disk until the planner's PR
opened — so a literal "git-commit-before-read" was not physically possible this loop. I record
this rather than backdate a commit (§3.1 verify/honesty; §4.2 anti-anchoring intent preserved
via the transcript).

## Positions — what a correct framework install MUST satisfy
- **P1 — Role-file port fidelity.** The three role prompts must be faithful ports: byte-identical
  to their source, or any deviation explicitly documented. No silent rewording.
- **P2 — Skill-file port fidelity.** SKILL.md + operating-guide.md + watch-counterpart.sh faithful
  ports; the script especially (paths / poll logic / mode) unedited or documented.
- **P3 — Parameter-binding completeness.** Every project parameter the role files externalize must
  be *bound* in REQUIRED_READING.md — no dangling binding (role references a param REQUIRED_READING
  never defines) and no dead binding (REQUIRED_READING defines a param no role references).
- **P4 — Transportability preserved.** Acumen-specific divergence isolated to REQUIRED_READING.md,
  never edited into the role files themselves.
- **P5 — §8.3 classification.** Correctly classified class (iv) (durable project-level precedent),
  with no-auto-merge + explicit *authenticated* spec-author ratification required; a relayed /
  inferred ruling is pending, not actionable.
- **P6 (rulebook §6 add) — §8 topology invariant must not drift** across the in-scope role files,
  verified against the invariant (not byte-diff, since §8 is perspective-adapted per role).
