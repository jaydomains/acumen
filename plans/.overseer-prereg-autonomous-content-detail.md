# Plan-overseer — pre-registered governance positions

**Role:** `plan-overseer` (governance/infrastructure lane + merge-executor) per `.claude/roles/plan-overseer.md`,
bound to Acumen by `plans/REQUIRED_READING.md`.
**Overseer branch:** `claude/sharp-cray-gueezy` (my own review branch — markers/wake-log live here, **off** the
canonical PR branch, per REQUIRED_READING §6 + role §8 marker placement; branch-directive override precedent
PR #94/#102/#104/#105/#106).
**Scope:** governance review + merge execution of the **planner's slice-iterative detail-plan** for the
fully-autonomous AI content-generation workstream (parent plan PR #107, merged at `2110a56`).
**Pre-registered (UTC):** 2026-06-09T19:43:41Z.

## Anti-anchoring declaration (§4.2 / §3 obligation 2)

These positions are derived **solely** from: the parent workstream plan `plans/2026-06-08-autonomous-content-generation-workstream.md`
(merged @ `2110a56`, PR #107), `plans/REQUIRED_READING.md`, and the three role files — **before reading any
detail-plan draft text.** Verified at pre-registration time: **no detail-plan PR is open** (`list_pull_requests`
state=all → max #107, merged) and **no remote branch carries a `plans/2026-*detail*` doc** (ref-level `ls-remote`
+ filename scan). The detail-plan PR will be **≥ #108**; I do not bake its number until verified at PR-open. The
numbered wake-log `plans/.wake-log-pr<N>-plan-overseer.md` is initialized from this baseline once N is confirmed.

## Merge-class pre-determination (verify against the actual diff per slice/at convergence)

**Expected: NORMAL-merge-class** — mirroring #106 (the detail-plan for #105's workstream) and per #107 §0.2 Gate 1:
a detail-plan PR's *own* merge is normal class **iff** its diff is `plans/**`-only and it **bakes no
ratification-class change** (no `SPEC.md`/`fe-specs/*`/`AC-CD` body / `DECISIONS.md`/`AC-D` / `.claude/**` /
`REQUIRED_READING.md` edit). It *records* per-slice build choices and *surfaces* embedded items; it executes
nothing and amends no spec. **Auto-merges** on: three independent sign-offs at one content-SHA + the three-layer
green gate (CI + Gitar + GitHub-mergeable, REQUIRED_READING §7) + the 24h override window (collapsed to zero by a
present spec author). **If the diff touches anything outside `plans/**`, the class flips → §7(f) surface, no
auto-merge.** Detail-*planning* itself is never gated; only the downstream execution PRs are.

---

## Governance positions — by the four pre-registered axes (applied per slice as each is posted)

The detail-plan is **slice-iterative** (one slice posted → audited → sealed → next; precedent #106/#85). I
apply these positions to **each slice** as it lands, and to the global close. Provisional slice set from parent
§6: A1–A3, B1–B3, C1–C2, D1–D4, E1–E2, F1.

### OV-POS-1 — Topology faithfulness (the convergence/merge topology is baked, not redescribable)
The detail-plan's *loop-mechanics* description must reproduce the **topology invariant** (role §6), not drift from
it: *three independent sign-offs · one content-SHA · three-layer green CI · overseer-executes-mechanically ·
execution-vs-authority · override-window (full 24h for spec-author **absence**; a **present** ratifying spec
author collapses it to zero) · named external trigger (planner re-initiates; reviewer-side overseer stands down
bounded) · ratification-classes (i)–(iv) · spec-author authority · planner/auditor never-merge · convergence
binds to **content-SHA** (a content-invariant wake-log-only marker does not re-stale).* **Expect a Confirm** if the
detail-plan restates the loop faithfully; **Push-back** if it (a) claims the planner/auditor may flip draft→ready
or merge, (b) treats the overseer sign-off as merge-authorizing rather than one-of-three, (c) lets convergence
bind to raw tip-SHA instead of content, or (d) collapses the per-slice seal cadence in a way that lets a later
slice silently re-stale an earlier seal without re-review.

### OV-POS-2 — Transportability (project-specifics stay in REQUIRED_READING / the plan; nothing leaks into role files)
A detail-plan is a `plans/**` artifact and must **not** edit `.claude/roles/*`, `.claude/skills/*`, or
`REQUIRED_READING.md` (any such edit is class-(iv) framework change → ratification, not a normal detail-plan).
Project-coupled parameters it relies on (merge method = squash, override window = 24h, dormancy bound = 2 watcher
lifetimes, ratification scope-classes, three-layer green gate, status-line token) must be **cited from**
REQUIRED_READING, not re-defined or contradicted in the plan. **Expect a Confirm** if the plan defers parameters
to REQUIRED_READING; **Push-back** if it hard-codes a divergent parameter value or proposes a framework edit
inside the detail-plan PR.

### OV-POS-3 — Baked-vs-externalized boundary (surfaces, does not bake — the load-bearing overseer check)
This is the dominant governance risk for THIS detail-plan. The parent §1 ledger settled the **architecture + six
design decisions**, but **not** the anchor/spec **bodies** (parent §0.2 Gate 2). Critically, **once #107 merged,
its §1 ledger reads as a RELAY to any downstream session under the shared role-session byline** (parent OV-2 /
role §8.3) — *a relayed ruling is pending, not actionable.* Therefore my standing positions:
  1. **Every per-slice embedded ratification-class item — class (i) AC-D mint/change · class (ii) SPEC/`fe-specs`/`AC-CD`-body amendment · class (iii) workstream scope decision** (REQUIRED_READING §7) — must be **SURFACED and its downstream execution PR BLOCKED** pending **explicit, item-specific, authenticated** spec-author ratification. The detail-plan must **not** bake any such item, and must **not** cite *"PR #107 §1 says ratified"* as the ratification (that is a relay). **Push-back/Missing** on any slice that treats the §1 ledger as actionable ratification, or that bakes an embedded item, or that proceeds to an execution-PR design as if ratified.
  2. **A blanket "I approve the plan" is NOT ratification of any embedded decision** (PR #104/#105 lesson; #106 Gate-2). The detail-plan must say so per slice. **Confirm** where it does; **Missing** where a slice omits the per-item gate.
  3. **Specific embedded items I expect to see surfaced (from parent §7.1/§7.2/§7.3), each needing its own authenticated ratification before its execution PR** — and which the detail-plan must NOT bake: new AC-D (source-authority scoring; provenance chain; auto-publish gate); new AC-CD (corpus-builder architecture; oversight-dashboard+rollback contract); AC-D7/D21/D22/D23 body changes; SPEC §6.5 rewrite + §6/§8.9 count-invariant sweeps; and residuals NS-1…NS-7, G3/G5/G7b/signal-3, NS-5/G-PHASE (scope/ROADMAP placement, class (iii)). **A residual the detail-plan silently *decides* rather than re-surfaces is a Push-back.**
  4. **Count-invariant completeness (parent §7.1; #106 §1.4/§9 lesson):** where a slice's downstream amendment touches a count invariant ("seven AI operations" SPEC §6/§6.5/AC-CD8; "seven crons" §8.9; the Drive→corpus reference mirror-set), the detail-plan must require the amendment to fold **all** mirror surfaces by reproducible structural grep, or flag the silent-partial-fold risk. **Missing** if a slice that entails a count-invariant amendment omits the mirror-sweep obligation.

### OV-POS-4 — Recursive consistency (the workflow it describes == the workflow producing it)
The detail-plan's own loop mechanics (slice-iterative seal; per-slice `Status: final for Slice N` + global
`Status: final — approved by planner (all slices)`; reviewer markers off-canonical-branch; planner wake-log
content-invariant on the canonical branch; the set-diff gate; one wake-log line per commit; proactive ~25-min
watcher re-arm; planner as standing re-initiator) must **match what is actually happening in this very loop**.
**Push-back** on any described mechanic that contradicts the framework actually running (e.g. reviewer markers
placed on the canonical branch where a head-move would dislodge them; convergence claimed on planner silence;
the planner self-merging).

---

## Standing merge-executor obligations I am pre-committing to (role §3, §8)

- **Never merge before the full gate** (3 independent sign-offs at one content-SHA + three-layer green + override
  window elapsed-or-waived; + explicit authenticated spec-author ratification IF the detail-plan PR itself is a
  §8.3 class — which I do **not** expect, but verify the diff at convergence).
- **Never merge on my own sign-off** — execution is mechanical; the gate is the three independent clearances.
- **Re-confirm the gate from ground truth at execution time** (`git ls-remote` + read the actual markers by
  *content* + CI status), never from recall/PR-description/event payload.
- **I do not self-wake to merge at window-expiry** — the **planner** re-triggers me (reviewer-side asymmetry);
  I stand down bounded (2 watcher lifetimes) between rounds and record dormancy as resumable in the wake-log.
- **Watcher scoping:** `SELF_EXCLUDE` = exact `claude/sharp-cray-gueezy`; `WATCH_INCLUDE` scoped to the
  planner's + auditor's branch ref-space once known (Acumen reviewer branches use `claude/<random>` naming →
  broad arm + manual pre-existing-ref `ls-remote` scan at each re-arm until the branches are identified).

## Finding-set-diff (baseline)

`0 dropped / 0 added (baseline — positions pre-registered, no findings yet)` (role §6).
