# Plan-overseer — pre-registered governance positions: PR-D (Signal spine + oversight) — FINAL LINK

**Status: pre-registered (no draft yet)** — link 4 of 4 (final). Pre-registered BEFORE any PR-D draft (§4.2).
Requirements: extraction §C (PR-D) + §B (AM-15/16/17/18 + signal-3 + DS13-a). Base: `main` @ `4122c55`
(PR-A+B+C merged; AC-D1..AC-D31, AC-CD1..AC-CD25, v1.9, 31 decisions).

**Per-PR authentication:** PR-D needs its **own** §D authenticated in-channel BEFORE authoring; traces to PR-D triage.
**STANDING RULES carried:** version **stays v1.9** (OV-B1; a v1.10+ bump is a finding); count bumps propagate
to ALL count-bearing lines (OV-1/OV-2 lesson); relay/inferred ratifications pending (PR#104/105).

## Positions
- **POS-D0 — ratification-class gate (i+ii).** Oversight AC-CD mint + SPEC §5/§4.11 amendments. No
  auto-merge; own authenticated §D first; traces to PR-D triage. Withhold sign-off until canonical record clean.
- **POS-D1 — mints + count integrity.** Oversight AC-CD (AM-17 → **AC-CD26**); CODE_SPEC §18 index +
  footer AC-CD25→26. Any new AC-D (GapSignal is a §5 entity, not necessarily an AC-D; DS13-a ruled in
  PR-A) — verify exact IDs/bodies match ratification; count headers consistent.
- **POS-D2 — GapSignal §5 entity (AM-15) complete [D1–D2+D3].** One polymorphic table
  (`signal_type`/`dedup_key`/`occurrence_count` + the `consumed_at`/status field D3 needs, authored
  forward-ready in one migration). **Must cohere with §6.5's signal-consuming pipeline (PR-C, on main)
  + §8.9 gap-detection/health crons (PR-A)** — the same three signals; verify §5 entity ↔ §6.5-Inputs prose.
- **POS-D3 — oversight AC-CD (AM-17) complete [E1 read + E2 rollback].** One anchor spans both. The E2
  **rollback matrix** (per pill/question/batch/source — ruling 5) **consumes PublishRecord (PR-C, by
  `batch_id`) + GenerationProvenance (PR-B, by `source_host`) + the AC-D28 E2 source-override layer
  (PR-A, DS13-a)**. Verify the rollback contract coheres with all three upstream surfaces.
- **POS-D4 — §4.11 admin-oversight (AM-18) complete [E1+E2].** Retroactive oversight = read (E1) +
  rollback (E2); no pre-publish gate. Verify complete; coheres with AC-D7/AC-D31 (retroactive shift).
- **POS-D5 — DS13-a E2 override-layer SCHEMA lands here.** AC-D28's E2 half (DB source-override keyed by
  `source_host`, designed in PR-A) — verify the schema implements the PR-A design, no contradiction.
- **POS-D6 — NS-4 NOT re-amended (carried from POS-C13).** NS-4 (catalogue-health: thin-band +
  uncovered-subject) was **defined in PR-C §6.5** (C-7=A). PR-D **implements against it without
  re-amending** — verify PR-D references NS-4, does not re-amend the definition (cross-PR amend-once).
- **POS-D7 — amend-once ACROSS PRs.** PR-D must NOT re-amend PR-A/B/C anchors (AC-D7/21/22/23/28/29/30/31,
  AC-CD25, AC-D1/12/CD8, §6.5/§6.8/§6.9/§290) except backlinks / **forward-ref resolution**. PR-D
  **resolves** the forward-refs PR-C/PR-B left: §290 "rolled-back"→E2; AC-D7 "Stage-E dashboard+rollback,
  PR-D"; §6.5 GapSignal forward-ref; GenerationProvenance/PublishRecord → E2. Grep the diff.
- **POS-D8 — amend-once WITHIN PR-D.** GapSignal complete [D1–D2+D3]; oversight AC-CD complete [E1+E2];
  §4.11 complete [E1+E2]. Each whole, once — no silent partial-fold.
- **POS-D9 — version stays v1.9 + count propagation (OV-1/OV-2 lesson).** Verify NO v1.10/v1.11; CODE_SPEC
  count AC-CD25→26 (+ any AC-D) propagates to ALL count/pairing lines (DECISIONS+SPEC+CODE_SPEC headers/
  footers/pairing); changelog folded under v1.9 ("Changes in v1.9 (fourth link — PR-D)").
- **POS-D10 — dashboard FE scope (fe-specs/FE-N).** Couples NS-5 (FE-phase home). Verify the FE deliverable
  + phase placement; a new fe-spec is a §7(ii) spec amendment (ratification-class) — verify ratified.
- **POS-D11 — signal-3.** Assignment scope-clarification signal in scope? Lean: define `scope_clarification`
  signal type now, defer the admin feature. Verify the ratified scope.
- **POS-D12 — status-taxonomy honesty.** Built-state notes for unbuilt D/E/F execution slices (no false "built").
- **POS-D13 — merge gate.** 3 sign-offs @ one content SHA + three-layer green + override window + §8.3
  ratification. I execute mechanically, re-confirm from ground truth.
- **POS-D14 — CYCLE-CLOSURE INTEGRITY (PR-D is the FINAL link).** After PR-D merges, **every forward-ref
  across the 4-PR cycle must resolve on `main`** — GapSignal (was forward-ref'd from §6.5), the E2 rollback
  mechanism (forward-ref'd from §290/AC-D7/AC-D31/AC-D29/AC-D28-E2), NS-4-implemented, §4.11. **Verify NO
  dangling forward-ref ("PR-D"/"lands in PR-D"/"forward-ref") remains on main after PR-D** — the whole
  cycle must be self-consistent + complete. This is the load-bearing final-link check.

## Cross-PR carry-forwards (RESOLVE at PR-D — the cycle closes)
- GapSignal §5 ↔ §6.5 signals (PR-C) ↔ §8.9 crons (PR-A) — same three signals, must cohere.
- E2 rollback ↔ PublishRecord (PR-C, batch_id) + GenerationProvenance (PR-B, source_host) + AC-D28 E2 override (PR-A, DS13-a).
- NS-4 implemented, not re-amended (POS-D6).
- §290 "rolled-back" + AC-D7 "Stage-E, PR-D" + §6.5 GapSignal forward-refs all resolve (POS-D7/D14).
