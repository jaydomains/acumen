# Plan-auditor — pre-registered positions

**Workstream:** fully-autonomous AI content generation + retroactive oversight — **detail plan**
(the slice-iterative detail plan that refines PR #107's §6 conditional stage/slice breakdown).
**Governing parent:** PR #107, merged at `2110a56` — `plans/2026-06-08-autonomous-content-generation-workstream.md`.
**Audit branch:** `claude/jolly-ptolemy-oui39p` (reviewer-side; markers + wake-log live here, off the canonical PR branch — role files §8 / REQUIRED_READING §6).
**Target detail-plan PR:** *not yet published* at pre-registration time. Anticipated number `#108` (next after #107) — **provisional**; reconcile the wake-log/positions filenames to the real `pr<N>` once the planner publishes.
**Pre-registered (timestamped):** 2026-06-09T19:45Z — authored **before** any detail-plan draft exists (ideal anti-anchoring; role files §3.2 / §4.2).

> **Method.** Positions derived from the **requirements** — the merged #107 parent plan + `SPEC.md`
> §6/§6.5/§8.9 + `DECISIONS.md` AC-D5/D7/D13/D21/D22/D23/D25 + the verified live tree — never from a
> draft I have not yet seen. Per-slice positions (§ "Per-slice") are anti-anchoring expectations; I
> finalize each slice's positions **before reading that slice's draft** (task directive; role §4.2)
> and verify against the actual slice text on arrival.

> **SURFACE vs FINDING (task directive).** When the planner *surfaces* a ratification-class item
> (new AC-D/AC-CD mint, SPEC/AC-CD amendment, AC-D7/D21/D22/D23 body change, scope/ROADMAP decision)
> for downstream authenticated ratification, that is the **correct mechanism** — I tag it **SURFACE
> (good)**, not an audit finding. Findings are reserved for **content correctness** against the
> parent plan + spec: a baked ruling that should have been surfaced, a misverified current-state
> claim, a dependency/sequencing error, a count-invariant mishandled, a safety-floor regression, a
> silently-dropped carried-open item.

---

## Verified ground truth (live tree @ `2110a56`; carry to the detail-plan's own build HEAD)

These are the facts a correct detail-plan must stay faithful to (re-verified by me before pre-registration):

- **GT-1 — `Operation` enum has 8 members, not 7.** `app/ai/provider.py:121-143`:
  `generation, grading, weakness, learning_material, pill_proposal, grade_review,
  anchor_self_review, embed`. The "**seven** AI operations" count invariant (SPEC §6:296, AC-CD8)
  **excludes `embed`** (the docstring at `provider.py:122` says "seven … plus `embed` for Drive
  RAG"). ⇒ A correct ops-count sweep is **"seven → seven+K" in the spec-prose sense**, mapped onto
  an enum that is **already 8**. A detail-plan that says "enum has seven, add → eight" is **wrong**.
- **GT-2 — Provider routing.** `_ANTHROPIC_DEFAULT_OPS` = {generation, grading, weakness,
  learning_material, pill_proposal} (`provider.py:149-157`); `_REVIEW_DEFAULT_OPS` =
  {grade_review, anchor_self_review} → `system_settings.review_provider` (OpenAI default)
  (`provider.py:162-164, 342-348`). The cross-model self-review (ruling 4) extends **this**.
- **GT-3 — Approve gate.** `approve_pill_proposal(db, task, *, actor_id)` at
  `catalogue.py:567-613` reads `task.payload["proposal"]` → `create_pill(...)` → marks task `done`
  → audits `pill_proposal.approve`. This is the human gate the auto-publish gate (Stage C) removes.
- **GT-4 — pgvector already exists.** `DriveChunk` at `models.py:776-799` with
  `embedding: Mapped[list[float]] = mapped_column(Vector(1536))`, IVFFlat index, content_hash;
  `SystemSettings.embedding_model` default `text-embedding-3-small` (`models.py:895-898`). The
  corpus builder **reuses** this; it must **not** "introduce pgvector".
- **GT-5 — Drive/RAG/web-search abstractions exist.** `DriveSource` Protocol +
  `get_drive_source()` (`drive_source.py:68-77, 340-352`); `retrieve_for_generation` /
  `cosine_top_k` (`drive_rag.py:196`) / `render_rag_context` (`drive_rag.py:587`) /
  `_DEFAULT_TOP_K=5` (`drive_rag.py:580`); `WebSearchSource`/`TavilyWebSearch`/
  `get_web_search_source()` (`web_search.py:81-220`).
- **GT-6 — 7 crons.** `app/beat_schedule.py:38-79`: grade_review.reconcile, realism.aggregate,
  drive_rag.ingest, calibration.run, safety_links.check, cost.budget_sweep, engagement.sweep =
  **7**, matching SPEC §8.9. Bootstrap is **not** a cron (admin-triggered, idempotent).
- **GT-7 — No signal store exists.** No discovery_miss / question_tag / scope_clarification /
  gap_detection / signal_* / corpus / provenance table in `models.py`. Stage-D signal spine (G5)
  and corpus/provenance persistence are **greenfield**.
- **GT-8 — Bootstrap currently unwired to publish/approve.** `run_bootstrap` (`bootstrap.py:64-152`)
  orchestrates anchor-pool top-up + self-review + safety-link curation + Drive ingest; it is
  **not** invoked from `approve_pill_proposal`. `generate_anchor_pool_for_pill`
  (`calibration.py:483`) + `curate_links_for_pill` (`safety_links.py:170`) are the reusable
  per-pill primitives Stage F rides. Confirms #107 §4.6 "currently unwired / assumed on approve".

---

## Global / cross-cutting positions (apply to the whole detail-plan)

- **PG-1 — Parent fidelity + autonomy pivot.** Cites #107 @ `2110a56` as governing parent; treats
  #106 only as **merged-superseded record** (never as authoritative human-gate design). Reflects the
  pivot: **no human approve gate, no Drive-folder dependency, generate-button → optional manual
  override**. A slice that re-imports a #106 human-gate assumption as live is a **finding**.
- **PG-2 — Plans-only / NORMAL merge class.** PR diff is `plans/**`-only (detail-plan doc + planner
  wake-log); **bakes no** spec/anchor/`.claude` change. Records rulings, plans work. (#107 Gate 1.)
- **PG-3 — Gate 2 / surface-not-bake.** Every ratification-class item is **SURFACED** for downstream
  authenticated ratification, **not baked**: new AC-D/AC-CD mints, SPEC §6/§6.5/§8.9 amendments, the
  AC-D7/D21/D22/D23 body changes, the §6.5 rewrite. The detail-plan must **not author amendment
  bodies**, and must record that the #107 §1 ledger reads as a **relay** downstream (pending, not
  actionable) — a downstream session re-confirms through its own authenticated channel (OV-2). A
  baked amendment body = finding; a correct surface = SURFACE (good).
- **PG-4 — Carried-open items re-surfaced, leans allowed, decisions not.** G3 (per-band difficulty
  decomp — lean min/max), G5 (signal data model), G7b (prompt-registry version trajectory), NS-5
  (ROADMAP placement — **no planner lean**), signal-3 (assignment scope-clarification feature-scope);
  plus NS-1..NS-7 where a slice touches them. The detail-plan re-surfaces these; a **lean** is fine
  (marked as lean), a **baked decision on an unruled item** is a finding. NS-5 carrying a lean would
  itself be a finding (parent says no lean).
- **PG-5 — Count-invariant mirror-sweeps named with three-class structural-grep rigor.** Each
  amendment surface must fold ALL mirror surfaces of any count invariant it touches: the **"seven AI
  operations" sweep** (word / numeral / `dict[Operation]`/subscript / op-set-floor) and the **"seven
  crons" sweep** (three-class), **plus the Drive→corpus reference mirror-sweep** (A-8: SPEC §6.1:302,
  §6.4:334, §7.3:403-405, AC-D23 step-4 `DECISIONS.md:574`, §8.x). Re-run greps at execution HEAD.
  **Must respect GT-1** (enum already 8 via `embed`). Naive "seven→eight" = finding.
- **PG-6 — Verified, not asserted.** Current-state claims carry file:line and are verified against
  the live tree at the detail-plan's HEAD (role §4.2); structural greps re-run at execution HEAD.
- **PG-7 — Dependency/sequencing fidelity.** A→B→C serialise; D follows B; E and F follow C;
  provenance spans A→B→C→E. One sequenced workstream (ruling 0b), each stage its own squash PR
  ("one PR per phase"). Stage A independently shippable. A slice ordering that breaks this = finding.
- **PG-8 — Slice-iterative structure + markers.** Per-slice `Status: final for Slice N` markers, a
  **global** marker at the end (task directive; #106 precedent). Slices independently testable where
  the parent says so. Each slice names its primary ratification surface (per §6 table).
- **PG-9 — Safety floor preserved.** "Acumen never generates safety teaching content" (AC-D21, SPEC
  §6.4:332) holds. The **safety self-review pass re-adjudicates `safety_relevant`** (ruling 4 / A-13)
  — the autonomous replacement for the removed pre-publish admin catch on a false-negative mis-tag.
  Below-threshold = **publish-with-warning, nothing held pre-publish including safety** (ruling 2),
  **subject to NS-7** (single-provider cross-model degradation — surfaced, not baked). A slice that
  silently no-ops the cross-model floor, or holds safety in a pre-publish queue (re-introducing the
  removed gate), is a finding.
- **PG-10 — No governance overreach.** The detail-plan does not presuppose the planner flipping
  draft→ready or merging (overseer's lane); out-of-scope section mirrors #107 §8.

---

## Per-slice positions (anti-anchoring; finalize each before reading that slice's draft)

Indexed to #107 §6's provisional slice table. IDs `PS-<slice>-<n>`.

### Stage A — reference corpus builder
- **A1 (authority allowlist + scoring registry):** encodes ruling 3 — **T1** {sabs.co.za, *.gov.za,
  nrcs.org.za, iso.org}, **T2** industry/professional bodies + recognised standards, **T3** reputable
  industry/educational; web search **restricted to allowlist**; **authority score by tier**. New AC-D
  (authority scoring) **SURFACED**. Registry shape (config vs table) is a build choice — fine to
  decide; the **anchor** is surfaced.
- **A2 (acquisition pipeline):** allowlist web-search (`web_search.py`, new AC-D21 use — SURFACE) →
  fetch → extract → cross-reference → embed into **existing** pgvector (`DriveChunk` shape / Vector(1536)
  / text-embedding-3-small — GT-4). Stamp authority-score-by-tier per chunk. New AC-CD (corpus
  builder) SURFACE. Must **reuse** pgvector + RAG helpers (GT-5), not reinvent. Embedding via existing
  `embed` op (GT-1) — note if a new op is proposed (→ NS-2-adjacent / ops sweep).
- **A3 (hybrid refresh cron + retrieval helper):** ruling 6 — per-topic on-demand (gap trigger) +
  admin on-demand + **weekly periodic backstop cron**. **Cron-count sweep** named (GT-6: 7 → 7+K).
  Reuses `cosine_top_k`/`render_rag_context` (GT-5).

### Stage B — autonomous generation + provenance
- **B1 (`Operation.pill_generation` mint + provider/stub wiring):** carried **G1**. A **NEW** op,
  **distinct** from existing `generation` (per-question test gen, GT-1) and `pill_proposal` (gap
  analysis). Defaults Anthropic (generate side). **Ops-count sweep** (GT-1 / PG-5). G7b prompt-registry
  versioning re-surfaced. The op mint is SURFACE.
- **B2 (corpus-grounded generation + per-draft provenance chain):** grounds against the **AI-built
  corpus**, not Drive (AC-D22 reframe — SURFACE). **Every generated claim traces to corpus doc(s)**
  with authority tier; provenance persisted per draft (greenfield — GT-7). New AC-D (provenance)
  SURFACE. **NS-3** (per-claim vs per-draft granularity) re-surfaced — lean per-assertion; surface if
  anchor-class.
- **B3 (N-draft fan-out + persistence + cost-share):** carried **G3** (per-band difficulty decomp —
  **lean min/max only**, not baked). Cost-share per AC-D18.

### Stage C — auto-publish gate
- **C1 (multi-pass + cross-model self-review protocol):** ruling 4 — generation pass + **grounding/
  factual + safety + provenance** review passes + **cross-model** (Anthropic-gen / OpenAI-review)
  extending `_REVIEW_DEFAULT_OPS`/AC-D23 (GT-2). **NS-2** (mint dedicated self-review op(s) vs reuse
  `anchor_self_review`) re-surfaced — lean new. **NS-7** (single-provider degradation) **SURFACED, not
  baked** — must not let the safety floor silently no-op. Safety pass re-adjudicates `safety_relevant`
  (A-13). New AC-D (self-review) SURFACE.
- **C2 (confidence scoring + auto-publish gate):** rulings 1+2 — **single global threshold**; ≥ →
  publish live; < → **publish-with-warning** (live + dashboard flag), **nothing held pre-publish incl.
  safety** (PG-9 / NS-7). **Removes** `approve_pill_proposal` (GT-3). **NS-6** (threshold numeric
  default + per-type telemetry) re-surfaced — lean conservative default + per-type telemetry from day
  one. New AC-D (gate) SURFACE.

### Stage D — gap detection + catalogue-health
- **D1–D2 (three §6.5 signal stores + dedup):** carried **G5** — **greenfield** (GT-7). The three
  signals are SPEC §6.5:344's inputs: **discovery-miss** (searches returning no good match),
  **question-tag** (recent generated questions + pill tags), **scope-clarification** (assignments where
  admin clarified scope). Deduped signal store; 3-arm dedup/idempotency. **signal-3 feature-scope**
  (assignment scope-clarification admin feature — parallel-to-G8 scope call) re-surfaced, not baked.
- **D3 (gap-detection sweep + catalogue-health → generation trigger):** carried **G2-analog** (§6.5
  rewrite — SURFACE). **NS-4** (catalogue-health definition) re-surfaced — lean thin-band +
  uncovered-subject. Drives B via **domain-fn call, no HTTP gate** (#107 §4.4).
- **D4 (gap-detection + health-check crons):** carried **G9-analog** — **cron-count sweep** (GT-6).

### Stage E — oversight dashboard (retroactive)
- **E1 (dashboard read surface):** retroactive — recent publishes, **per-item provenance**, confidence,
  source-authority breakdown, spot-check sampling. New AC-CD (dashboard) SURFACE. **G6 transform** —
  governing contract is the dashboard/rollback surface. **G8** (generate button) + **G7a**
  (`pill_proposal` refiner) survive as **optional manual paths only**, distinct from the autonomous
  spine (G7a carried-lean, not cited as ratified — OV-2).
- **E2 (rollback matrix + spot-check):** ruling 5 — **full matrix: per pill / per question / per batch
  / per source** (per-source = retract everything grounded on a discredited corpus source). New AC-CD
  (rollback) SURFACE. **Relocated AC-D21 safety-tag override** lives here (A-13) — retroactive admin
  retoggle of `safety_relevant`, the autonomous-era replacement for the removed pre-publish override.

### Stage F — bootstrap-on-publish
- **F1 (bootstrap-on-publish wiring):** reframes AC-D7/AC-D23 — trigger moves **approve → publish**
  (GT-3 / GT-8). Reuses `generate_anchor_pool_for_pill` + `curate_links_for_pill` (GT-8).
  **Enqueued/async** so publish stays fast (#106 D1 carried). AC-D7/AC-D23 body change SURFACE.

---

## Requirements-baseline updates (post-pre-registration; recorded as discovered)

- **NS-7 RULED 2026-06-09 (degrade-not-gate)** — recorded on **unmerged** `claude/vibrant-euler-a0ewax`
  @ `92886fe` (post-merge addendum to #107). Spec author ruled (authenticated in-session channel per
  the commit) **against** the planner's prerequisite-gate lean: single-provider deployment **degrades
  to single-provider multi-pass** (distinct prompts, same model) + **"single-provider verified"**
  provenance flag + **publish-with-warning** (ruling 2), dashboard-surfaced for higher-rate
  spot-check — **never gated**. Qualifies ruling 4 to "cross-model *where a second provider is
  configured*; single-provider degrades, not gates."
  **⇒ Updates PG-9 / PS-C1 / PS-C2:** Stage C must reflect **degrade-not-gate**. A future Slice 7/8
  baking the *old prerequisite-gate lean* is now a **finding** (contradicts the ruling).
  **⇒ Caveat (overseer's lane):** the ruling lives on an unmerged branch under the shared `Claude`
  byline ⇒ reads as a **relay** (§8.3) until canonical/re-confirmed. I do **not** treat it as
  merged-canonical; I track the divergence (raised as A-6).

## Forward watch-items (not yet in scope; verify when the owning slice lands)

- **Slice 4 (B1) / Slice 7 (C1) ops-count sweep — GT-1.** Live enum is **8** (`embed` is the existing
  +1); the "seven AI operations" spec count **excludes `embed`**. The sweep must be "spec-prose seven
  → seven+K over an enum already at 8", **not** "enum seven → eight." A naive seven→eight = finding.
- **Slice 7 (C1) / Slice 8 (C2) — NS-7 ruled outcome** (see baseline update above).
- **Slice 4 (B1) — `Operation.pill_generation` must be NEW**, distinct from existing `generation`
  (per-question test gen) and `pill_proposal` (gap analysis) — GT-1/GT-2.

## Slice-1 (A1) verification record — 2026-06-09T20:05Z @ planner head `eaee5c4`

PS-A1 **satisfied**. Findings posted (PR #108 review): **A-1..A-4 Confirm** (ruling-3 fidelity;
ratification-class items correctly surfaced incl. DS1-a/b/c, DS1-d an in-scope build choice;
count-invariant + scope fence correct; grounding cites verified against live tree); **A-5 Refine**
(low — `DECISIONS.md:529`→`:531` citation drift at doc:183, substance correct); **A-6 Refine**
(forward/non-blocking — NS-7 ruled, reflect at Slice 7/8). **A1 strong, no blocking finding; not
sealed yet** (awaiting overseer Slice-1 governance review + planner disposition of A-5/A-6).

## STATUS (Slice 1): final — approved by plan-auditor @ `22f3d67` (content-bound to Slice 1 §1 section)

Slice-1 (A1) **content SEAL** — 2026-06-09T20:30Z @ planner folded head `22f3d67`. Re-verified the
round-1 fold against the actual text: **A-5 RESOLVED** (cite `:529`→`:531`, keyword-list `:529` kept)
+ **A-6 RESOLVED** (NS-7 recorded pending-authentication, reflect at Slice 7/8, not baked — §8.3-faithful).
Confirms A-1..A-4 hold (sections byte-identical; A-3 forward GT-1 note folded accurately). **0 open
content findings for Slice 1.** Set-diff this round: **2 dropped [A-5, A-6] / 0 added.**
**This is an interim per-slice seal, NOT my global final-marker** (Gate-1 convergence = all three
parties' global markers at the final whole-doc SHA + green gate + override window — §0.1/OV-4). The
seal is content-bound to Slice 1's §1 section: a later-slice append does not re-stale it; editing
Slice 1's section does. Awaiting overseer Slice-1 seal, then Slice 2 (A2) detail.

## PS-A2 — pre-registered positions for Slice 2 (A2), **before the Slice-2 draft exists** — 2026-06-09T20:48Z

Anti-anchoring (§4.2 / task directive): authored after Slice 1 sealed 3/3 but **before** the planner
posts the A2 section. A2 = corpus acquisition pipeline (allowlist web-search → fetch → extract →
cross-reference → embed → pgvector). Primary ratification surfaces (#107 §6): new AC-CD (corpus
builder) · AC-D21 web-search use · AC-D22 body change. What a correct A2 detail must contain/decide/avoid:

- **PS-A2.1 — Reuse, not reinvent (GT-4/GT-5).** Embeds into the **existing** pgvector store — reuse the
  `DriveChunk` shape (`Vector(1536)`, `text-embedding-3-small`, IVFFlat; `models.py:776-799`,
  `SystemSettings.embedding_model :895`) and the existing **`embed` Operation** (GT-1) for embeddings;
  reuse `cosine_top_k`/`render_rag_context` for retrieval. Must **not** introduce pgvector, a new
  embedding model, or a parallel vector stack. Reinvention = finding.
- **PS-A2.2 — Wire A1's filter (the *application* A1 deferred).** A2 applies `filter_to_allowlist`
  around `get_web_search_source().search(...)` (ruling 3 "web search restricted to the allowlist") and
  **stamps authority-score-by-tier** (A1's `authority_score`/`Tier`) on each acquired chunk/doc. Carries
  **DS1-c** (does the allowlist also tighten the existing AC-D21 curation search?) — re-surfaced, not baked.
- **PS-A2.3 — Corpus-doc store + `authority_score` column.** A1 explicitly deferred these to A2. A2
  *plans* the entity/migration (new table vs extend `DriveChunk`) as a build choice, and **surfaces the
  new AC-CD (corpus-builder architecture)** — must not bake the AC-CD body.
- **PS-A2.4 — Pipeline stages concrete:** fetch → extract → **cross-reference** → embed. Cross-reference
  is the novel autonomous step (corroboration/conflict across sources, authority-weighted) — if it
  defines autonomous behaviour beyond a build detail, expect it **surfaced** (may touch §6.5-scope).
- **PS-A2.5 — AC-D21 web-search-use extension SURFACED** (class ii; web search from safety-link
  curation → corpus acquisition, `web_search.py`/SPEC §7.4). Correct mechanism = surface, not bake.
- **PS-A2.6 — AC-D22 body change SURFACED** (ruling 0a: Drive-folder dependency retired for the AI-built
  corpus; pgvector "queried at every generation call" extends to §6.5 generation). **NS-1** (retire the
  Drive ingest *code* entirely vs keep dormant — lean remove) re-surfaced, not decided.
- **PS-A2.7 — Drive→corpus mirror-sweep (A-8) NAMED.** Retiring Drive carries the same mirror-surface
  obligation as a count invariant: SPEC §6.1:302 / §6.4:334 / §7.3:403-405, AC-D23 step-4
  (`DECISIONS.md:574`), §8.x. A2 (or its AC-D22 amendment) must **enumerate + fold** these by structural
  grep at execution HEAD, or it is a silent partial-fold (§7). A2 should at least name the sweep.
- **PS-A2.8 — Ops/cron scope fence.** A2 should reuse `embed` (no new op ⇒ **no ops-count sweep**) and
  add **no cron** (the hybrid refresh cron is A3). If A2 mints a new acquisition/extract op, the
  ops-count sweep is triggered (GT-1: enum already 8) — flag both ways. Tests zero-network (AC-CD15):
  web-search + fetch faked.

## Loop mechanics (role files §4–§8; REQUIRED_READING parameters)

- **Watcher:** counterpart-change-detector skill. Pre-PR: **broad arm** + manual `git ls-remote`
  pre-existing-ref scan at each arm (planner branch name unknown). `SELF_EXCLUDE` = exact
  `claude/jolly-ptolemy-oui39p`. Once the planner's + overseer's branches are known, add
  `WATCH_INCLUDE`. Proactive re-arm ~25 min; planner is the standing re-initiator.
- **Dormancy bound:** 2 watcher lifetimes (~1h) zero counterpart activity → bounded dormant-wait
  stand-down (REQUIRED_READING §7). Reviewer-side: I do **not** re-initiate.
- **Convergence:** three sign-offs at one whole-doc content-SHA + three-layer green gate + 24h
  override window (collapsed to zero by present spec author) → **overseer** flips draft→ready +
  squash-merges. I never flip draft→ready, never merge.
- **Findings:** stable IDs `A-<n>`; taxonomy Confirm/Push-back/Refine/Missing; file:line + spec
  clause; finding-set-diff per review commit; per-thread `X/5`; 5/5 → surface.

## STATUS (Slice 2): final — approved by plan-auditor @ `5d26906` (content-bound to Slice 2 §2 section)

Slice-2 (A2) **content SEAL** — 2026-06-09T21:15Z @ planner folded head `5d26906`. Re-verified round-1
fold vs actual text: **A-7 RESOLVED** (DS2-b "cross-reference" elevated to a SURFACE — (i)/(ii)/(iii),
lean ≥(ii) for safety topics, not baked) + **A-8 RESOLVED** (cite :169→:274). Confirms A-9..A-12 hold.
**0 open content findings for Slice 2.** Set-diff: **2 dropped [A-7, A-8] / 0 added.** Interim per-slice
seal, NOT global final-marker. PS-A2 satisfied. Awaiting overseer Slice-2 seal (OV-11 fold), then Slice 3 (A3).

## PS-A3 — pre-registered positions for Slice 3 (A3), **before the Slice-3 draft exists** — 2026-06-09T21:30Z

Anti-anchoring (§4.2). A3 = hybrid refresh cron (per-topic / on-demand / weekly) + corpus retrieval
helper. Primary ratification (#107 §6): cron-count sweep; weekly backstop. Ruling 6 (hybrid refresh) is
**settled** — cadence shape is not an open decision. What a correct A3 detail must contain/decide/avoid:

- **PS-A3.1 — Hybrid refresh fully encoded (ruling 6), no mode dropped.** All three: **per-topic
  on-demand** (the entry point Stage-D gap-detection will call — A3 provides the callable, D wires it),
  **admin on-demand** (manual override), **weekly periodic backstop CRON**. A non-hybrid reduction
  (e.g. weekly-only) = finding.
- **PS-A3.2 — Cron-count sweep (GT-6) — FIRST cron the workstream adds.** A3 adds the corpus-refresh
  cron ⇒ touches the **"seven crons"** count invariant (`beat_schedule.py:38-79` = 7 today; SPEC §8.9).
  A3 must **name the three-class cron mirror-sweep** (word / numeral / beat-registry) at execution HEAD,
  "seven → seven+K". This is the cron analog of GT-1 — watch for a naive/omitted sweep. SPEC §8.9
  amendment SURFACED (class ii).
- **PS-A3.3 — Corpus retrieval helper (the A2-deferred retrieval).** `retrieve_corpus_for_topic`
  reusing `cosine_top_k` over `CorpusChunk` + a `render_corpus_context` sibling of `render_rag_context`
  — what B2 generation consumes ("queried at every generation call", AC-D22 reframe). Reuse, not
  reinvent (GT-5).
- **PS-A3.4 — Reuse the refresh machinery.** Per-topic refresh = re-run `corpus_builder.acquire_for_topic`
  (idempotent via content-hash dedup); weekly backstop = sweep all topics/sources; reuse the
  `diff_files`/`DiffSets` diff-ingest scaffold + the `beat_schedule` cron-registration pattern.
- **PS-A3.5 — Scope fence.** A3 = refresh cron + retrieval helper only. NOT gap-detection/trigger logic
  (Stage D), NOT generation grounding (B2). Provides the per-topic-refresh callable; D wires the trigger.
  Tests zero-network (AC-CD15: faked search/fetch/embed; cron logic unit-tested).
- **PS-A3.6 — SURFACE not bake.** SPEC §8.9 cron-list amendment + (the corpus-builder AC-CD from A2 may
  cover refresh cadence, or a §8.9 amendment) surfaced; weekly cadence value a minor build choice.

## STATUS (Slice 3): final — approved by plan-auditor @ `5a6f84e` (content-bound to Slice 3 §3 section)

Slice-3 (A3) content SEAL — 2026-06-09T22:00Z. A-13 RESOLVED (cosine_top_k sig fixed); A-14..A-18 hold.
0 open content findings. Set-diff: 1 dropped [A-13] / 0 added. Interim per-slice seal. Forward watch
carried: **A-18 DS3-a/NS-4 coherence** → re-check at Slice 10 (D3). Awaiting overseer Slice-3 seal, then Slice 4.

## PS-B1 — pre-registered positions for Slice 4 (B1), **before the Slice-4 draft exists** — 2026-06-09T22:00Z

**THE ops-count / GT-1 slice.** B1 = `Operation.pill_generation` mint + provider/stub wiring; carried
G1 + G7b. Anti-anchoring (§4.2). What a correct B1 detail must contain/decide/avoid:

- **PS-B1.1 — NEW op, distinct from existing (GT-1/GT-2).** `Operation.pill_generation` is a **new**
  enum member, distinct from existing `generation` (per-question *test* gen) and `pill_proposal` (gap
  *analysis*). Defaults **Anthropic** (generate side — added to `_ANTHROPIC_DEFAULT_OPS`,
  `provider.py:149-157`). Conflating it with `generation`/`pill_proposal` = finding.
- **PS-B1.2 — Ops-count sweep per GT-1 (the load-bearing check).** Enum is **already 8** (7 spec ops +
  `embed`); adding `pill_generation` → **enum 8→9**, while the **spec-prose "seven AI operations" →
  "eight"** (embed excluded). The three-class structural mirror-sweep (word / numeral /
  `dict[Operation]`-or-`[operation]`-subscript / op-set-floor) must span SPEC §6:296, **AC-CD8** (the
  ops anchor), `provider.py` (enum + `_ANTHROPIC_DEFAULT_OPS` + the "seven…plus embed" docstring),
  prompts registry, `cost.py` aggregation, CODE_SPEC/ROADMAP/CHECKLIST mirrors. **A naive "enum
  seven→eight" is WRONG** (enum is 8→9; spec-seven→eight). This is the exact GT-1 test — watch it
  closely; it's the ops analog of A3's cron coupling. SPEC §6 + AC-CD8 amendment SURFACED (class ii).
- **PS-B1.3 — Provider/stub wiring + prompt.** New op → `_ANTHROPIC_DEFAULT_OPS`; a prompt-registry
  entry (`app/ai/prompts/`) with a version id; stub-provider wiring for zero-network tests.
- **PS-B1.4 — G7b prompt-version trajectory re-surfaced** (carried; lean per #106 — versioned like the
  existing prompts). Not baked.
- **PS-B1.5 — Scope fence.** B1 = op-enum mint + provider/stub + prompt scaffold ONLY. NOT the
  corpus-grounded generation logic or provenance chain (B2), NOT N-draft fan-out (B3). Tests zero-network.
- **PS-B1.6 — Reuse.** Mirror the existing op-add pattern (how the 7 ops + embed are wired); no new
  provider-routing machinery.

## PS-B1 augmentation (G1 superseded-ratification nuance; overseer PS-OV-B1 corroboration) — 2026-06-09T22:15Z

- **PS-B1.7 — G1 op-mint must be SURFACED FRESH, not baked as #106-ratified.** #107 §9 records G1 as
  "Carried (ratified for #106 Slice 1 via the prior chat)" — but #106 is **superseded**, so per OV-2 /
  §8.3 logic a superseded-plan ruling is **not actionable** (the exact parallel to G7a's carried-lean
  treatment). B1 must surface the `pill_generation` op mint as a **fresh** SPEC §6 + AC-CD8 ratification
  item (authenticated downstream), **not** cite the #106 G1 "keep" as the ratification. Treating G1 as
  already-ratified-via-#106 = finding. (Overseer independently pre-registered this in PS-OV-B1.)

## STATUS (Slice 4): final — approved by plan-auditor @ `351d562` (content-bound to Slice 4 §4 section)

Slice-4 (B1) content SEAL — 2026-06-09T22:25Z. **0 findings; 5 Confirms (A-19..A-23).** The ops-count/
GT-1 sweep handled exemplarily (enum 8→9 / spec-seven→eight + construction-oracle); G1 surfaced fresh;
all citations verified. Set-diff: 5 added [A-19..A-23] / 0 dropped. Interim per-slice seal. Re-stales
only if a later overseer-fold edits §4.

## PS-B2 — pre-registered positions for Slice 5 (B2), **before the Slice-5 draft exists** — 2026-06-09T22:25Z

B2 = corpus-grounded generation + per-draft provenance chain. Primary ratification (#107 §6): new AC-D
(provenance) · AC-D22 reframe · NS-3 granularity. Anti-anchoring (§4.2). What a correct B2 must contain:

- **PS-B2.1 — Grounds against the AI-built corpus (A2/A3), NOT Drive.** Wire `retrieve_corpus_for_topic`
  (A3) + a corpus context render into the `pill_generation` call (B1 op). AC-D22 reframe (Drive→corpus
  "queried at every generation call" extends to §6.5) SURFACED. Grounding against Drive RAG = finding.
- **PS-B2.2 — Per-draft provenance chain (greenfield, GT-7).** Every generated claim traces to corpus
  chunk(s)/source(s) **with authority tier**; persisted per draft (new table/column). New AC-D
  (provenance chain) SURFACED, not baked.
- **PS-B2.3 — NS-3 granularity re-surfaced** (per-claim/assertion vs per-draft source-set). Lean
  per-assertion; surface if anchor-class. Not baked.
- **PS-B2.4 — G7b prompt bump v1.0.0→v1.1.0** (B1 surfaced this): output contract gains `grounding_refs`.
- **PS-B2.5 — DS2-b/A-7 cross-reference coupling.** If the spec author rules DS2-b ≥(ii) (cross-source
  corroboration), B2's provenance/authority weighting reflects it. B2 should note the dependency on the
  pending DS2-b ruling (forward coherence), not bake an assumption.
- **PS-B2.6 — Authority-weighted grounding (ruling 3).** Use the A3 retrieval's `authority_tier`/`score`
  to weight grounding; feeds Stage-C confidence (NS-6 coupling).
- **PS-B2.7 — Scope/invocation.** Generation is a domain fn invoked by D3 (gap-detection), not a
  mandatory admin action (parent §4.2). Scope fence: B2 = grounding + provenance chain; **watch the
  B2/B3 persistence split** (B3 owns N-draft fan-out + bulk persistence + cost-share) — flag if B2/B3
  overlap or leave a gap. Tests zero-network.

## STATUS (Slice 5): final — approved by plan-auditor @ `39273dd` (content-bound to Slice 5 §5 section)

Slice-5 (B2) content SEAL — 2026-06-09T23:00Z. **0 findings; 5 Confirms (A-24..A-28).** Standouts: the
queryable-relational provenance constraint (E2-driven) + the DS2-b additive coupling (bakes nothing) +
the explicit B2/B3 split. All grounding verified (generation.py precedent; greenfield provenance).
Set-diff: 5 added [A-24..A-28] / 0 dropped. Interim per-slice seal.

## PS-B3 — pre-registered positions for Slice 6 (B3), **before the Slice-6 draft exists** — 2026-06-09T23:00Z

B3 = N-draft fan-out + persistence + cost-share. Primary ratification (#107 §6/§7.3): **carried G3**
(per-band difficulty decomposition — lean min/max). Anti-anchoring (§4.2). What a correct B3 must contain:

- **PS-B3.1 — N-draft fan-out.** The topic→N generation primitive (B1's `pill_generation` returns a
  `drafts` list of `target_count`); B3 fans out / persists each candidate. Reuse B2's
  `generate_grounded_drafts` + provenance writer per draft (the B2/B3 seam B2 §5.3 stated).
- **PS-B3.2 — Persistence = N `ProcessingTask` rows** (`models.py:1174`, the #106 draft-carrier
  precedent), each with its provenance rows (B2 writer). No new draft entity unless justified.
- **PS-B3.3 — Cost-share via `record_provenance_share`** (`cost.py:97`) — split one generation call's
  cost across the N draft entities (the AC-CD8 spend invariant holds; B3 owns cost-share per B2 §5.3).
- **PS-B3.4 — G3 carried, re-surfaced, NOT baked.** Per-band difficulty decomposition — **lean min/max
  range only** (parent §7.3). The draft schema already carries `available_difficulty_min/max` (B1).
  Baking a per-band decomposition decision = finding; a lean is fine.
- **PS-B3.5 — Scope fence.** B3 = fan-out + persistence + cost-share only. NOT self-review/gate (C1–C2),
  NOT grounding (B2 done), NOT gap-detection/trigger (D), NOT dashboard (E). Tests zero-network (AC-CD15).
- **PS-B3.6 — Carried holds.** B3 execution needs A2+A3+B1+B2 merged (it persists grounded drafts) + NS-5.

## PS-B3 augmentation (per-batch rollback identity; overseer PS-OV-B3 corroboration) — 2026-06-09T23:12Z

- **PS-B3.7 — N-draft fan-out IS the "generation batch" rollback unit (ruling 5).** Ruling 5's rollback
  matrix includes **per generation batch** (parent §4.5 / E2). B3's N-draft fan-out is exactly one
  generation batch, so B3 should establish a **batch identifier** linking the N drafts (one generation
  call → one batch), which E2's per-batch rollback keys on (parallel to how B2's provenance keys E2's
  per-source rollback). B3 *records* the batch identity; E2 *consumes* it for rollback. If B3 omits a
  batch id, E2's per-batch rollback has nothing to key on — flag as a gap. (Overseer independently
  pre-registered this in PS-OV-B3.)

## STATUS (Slice 6): final — approved by plan-auditor @ `07080d1` (content-bound to Slice 6 §6 section)

Slice-6 (B3) content SEAL — 2026-06-09T23:25Z. **0 findings; 5 Confirms (A-29..A-33).** Fan-out +
ProcessingTask persistence + cost-share (AC-CD8 mirror verified) + batch_id for E2 + G3 lean-not-baked +
idempotency well-scoped. All grounding verified. Set-diff: 5 added [A-29..A-33] / 0 dropped. Interim seal.

## PS-C1 — pre-registered positions for Slice 7 (C1), **before the Slice-7 draft exists** — 2026-06-09T23:25Z

**THE NS-7 / safety-floor slice.** C1 = multi-pass + cross-model self-review protocol. Primary
ratification (#107 §6): new AC-D (self-review, ruling 4) · NS-2 · NS-7. Anti-anchoring (§4.2).

- **PS-C1.1 — Multi-pass + cross-model self-review (ruling 4).** Generation pass + independent review
  passes — **grounding/factual · safety · provenance** — + cross-model verify (Anthropic-gen /
  OpenAI-review) extending `_REVIEW_DEFAULT_OPS`/AC-D23 (GT-2). New AC-D (self-review) SURFACED.
- **PS-C1.2 — Safety pass RE-ADJUDICATES `safety_relevant` (parent §4.3 / A-13).** Not merely reviewing
  already-tagged content — it owns re-adjudicating the classification (the autonomous replacement for
  AC-D21's removed pre-publish admin catch on a false-negative mis-tag). **Preserves the "Acumen never
  generates safety teaching content" floor (PG-9, AC-D21/SPEC §6.4).** A C1 that drops this = finding.
- **PS-C1.3 — NS-2 re-surfaced** (mint dedicated self-review op(s) vs reuse `anchor_self_review`). Lean
  new (distinct input/output contract). Drives the ops-count sweep magnitude. SURFACED, not baked.
- **PS-C1.4 — NS-7 (RULED degrade-not-gate, but PENDING AUTHENTICATION) — THE critical check.** Per my
  A-6 + overseer OV-6: the NS-7 ruling (single-provider → same-model multi-pass + "single-provider
  verified" flag + publish-with-warning, **never gate**) lives on unmerged `vibrant-euler@92886fe` — a
  **relay**, pending authentication (§8.3). C1 **must** reflect NS-7 as **recorded-but-pending-
  authentication** (degrade-not-gate, to bind once authenticated), **NOT baked as ratified**. Baking
  NS-7 without authentication = finding. **This is where I ESCALATE the NS-7 authentication question to
  the spec author (the user)** — C1 cannot bind the degrade-not-gate behaviour until NS-7 is
  authenticated through this PR's channel (or vibrant-euler lands via a proper ratification gate).
- **PS-C1.5 — Cross-model floor extends existing pattern (GT-2):** `_REVIEW_DEFAULT_OPS` → OpenAI;
  AC-D23 cross-provider. Anthropic-gen / OpenAI-review.
- **PS-C1.6 — Ops-count sweep (GT-1) iff NS-2 mints op(s).** Enum is **9** after B1 (8 + pill_generation);
  C1 self-review op(s) → enum 9→9+K, spec-prose count → +K. C1 co-owns the ops sweep (slice map). Must
  respect GT-1 (not naive). 
- **PS-C1.7 — Scope fence.** C1 = the self-review passes + verdicts ONLY. NOT the confidence threshold /
  publish gate decision (C2 consumes C1's verdicts). Tests zero-network (stub cross-provider).

## STATUS (Slice 7): final — approved by plan-auditor @ `51cf183` (content-bound to Slice 7 §7 section)

Slice-7 (C1) content SEAL — 2026-06-09T23:50Z. **0 findings; 6 Confirms (A-34..A-39).** Multi-pass+
cross-model self-review (ruling 4); safety pass re-adjudicates safety_relevant (A-13, floor preserved);
NS-2 lean (one op+3 variants); ops-count 2nd expansion GT-1-correct (enum 8→10, named 7→9); **NS-7
recorded pending-authentication, NEITHER baked, single switchable policy point** (correct SURFACE).
All grounding verified (enum=8 on main confirmed; _VARIANT_REGISTRY:58; auto_tag_safety:207).
Set-diff: 6 added [A-34..A-39] / 0 dropped. Interim seal. **NS-7 AUTHENTICATION ESCALATED to spec-author
(user) via in-session authenticated channel (§8.3) — C1/C2 EXECUTION (not detail) waits on the ruling.**

## NS-7 AUTHENTICATED (spec-author, in-session, origin: this conversation) — 2026-06-10T00:30Z

The spec author authenticated NS-7 = **degrade-not-gate** through the direct in-session human channel
(§8.3 reference channel; NOT a relay — reached the auditor directly, explicit/current/complete).
**Yardstick update (supersedes the earlier "pending" + the vibrant-euler relay):**
- Single-provider → same-model multi-pass + "single-provider verified" flag + **publish-with-warning**
  (dashboard higher-spot-check); **no pre-publish gating incl. safety-relevant**; ruling 4 qualified to
  "cross-model where a 2nd provider is configured." Autonomy principle reaffirmed (rein in retroactively).
- **PS-C1.4 RESOLVED-authenticated.** C1/C2 reflecting degrade-not-gate = correct; baking the GATE
  posture, or still hedging "pending", = finding. AC-D binding stays Gate-2 (spec-author-authored).
- Origin for this loop = THIS conversation; planner/overseer cite it (my PR comment is a relay to them).

## PS-C2 — pre-registered positions for Slice 8 (C2), before reading §8 substance — 2026-06-10T00:30Z

C2 = confidence scoring + auto-publish gate. (Seen planner's item-list surface, NOT §8.1-§8.7 build
substance.) Ratification (#107 §6): new AC-D (gate, rulings 1+2) · removes approve gate · NS-6 · AC-D7
body change · §6.5 rewrite · **NS-7 (now AUTHENTICATED degrade-not-gate)**.

- **PS-C2.1 — Single global threshold (ruling 1) + publish-with-warning (ruling 2).** ≥thr→publish live;
  <thr→publish-with-warning (live + dashboard flag); nothing held pre-publish incl. safety. New AC-D gate SURFACED.
- **PS-C2.2 — Removes `approve_pill_proposal` (GT-3, catalogue.py:567).** Publish = create Pill. AC-D7
  body change (remove "queue for admin review/approve") SURFACED, multi-slice w/ F1 (amend-once).
- **PS-C2.3 — Consumes C1 verdicts.** Reads grounding/safety/provenance verdicts + authority-weighted
  scoring → confidence → gate decision.
- **PS-C2.4 — NS-6 re-surfaced** (threshold value + `compute_confidence` formula + per-type telemetry).
  Lean conservative default + telemetry day-one (ruling 1 re-evaluate-iff-data); couples DS1-a tier scores. Not baked.
- **PS-C2.5 — NS-7 AUTHENTICATED degrade-not-gate — C2 is where the switch ACTS.** C2 must reflect:
  single-provider safety-relevant → publish-with-warning + "single-provider verified" flag, **NEVER
  gate/hold**. A C2 that holds safety-relevant single-provider pre-publish = **finding** (contradicts the
  authenticated ruling). A C2 reflecting degrade-not-gate = correct.
- **PS-C2.6 — Safety floor preserved (PG-9):** below-threshold safety content publishes-with-warning
  (not held), per ruling 2 + NS-7. The "never generate safety teaching content" floor (AC-D21/§6.4) is
  upstream (safety pills → curated links) and unaffected by the gate.
- **PS-C2.7 — Scope fence.** C2 = confidence + gate + publish. NOT C1 passes, NOT dashboard/rollback (E),
  NOT bootstrap-on-publish (F1, though publish triggers it). DS8-a PublishRecord = E1/E2 read-surface. Tests zero-net.

## STATUS (Slice 8): final — approved by plan-auditor @ `18a2e74` (content-bound to Slice 8 §8 section)

Slice-8 (C2) content SEAL — 2026-06-10T01:05Z. A-40 RESOLVED (NS-7 RULED degrade-not-gate folded into
§8.3 + §1 status-of-record; NS-7 now authenticated to all 3 sessions own-channel). A-41..A-44 hold.
**A-45 (low, OPEN, global-marker-scoped):** §7.3 left literally "pending" (superseded-by-§1-reference, a
seal-preserving interim) — reconcile before the global marker. Set-diff: 1 dropped [A-40] / 1 added [A-45].
C1 seal holds (§7 byte-identical). Interim per-slice seal.

## PS-D1 — pre-registered positions for Slice 9 (D1–D2), before the draft — 2026-06-10T01:05Z

D1–D2 = three §6.5 signal stores + dedup. Ratification (#107 §6): **carried G5** (signal data model) +
**carried signal-3** feature-scope. Anti-anchoring (§4.2). What a correct D1–D2 must contain:

- **PS-D1.1 — The three §6.5 signals (SPEC §6.5:344), greenfield (GT-7).** discovery-miss (Testee
  searches returning no good match) · question-tag (recent generated questions + their pill tags) ·
  scope-clarification (assignments where admin clarified scope). No signal table exists today → new
  store(s) + migration.
- **PS-D1.2 — G5 carried (signal-capture data model), re-surfaced.** The schema is greenfield; a new
  AC-CD (signal store) or schema SURFACED, not baked. Lean a deduped per-signal-type model.
- **PS-D1.3 — Dedup (3-arm).** Deduped signal store; the **signal-layer** dedup (D owns it; B3 did the
  persistence-layer half — PS-B3 idempotency). 3-arm dedup/idempotency (parent §4.4).
- **PS-D1.4 — signal-3 feature-scope carried** (the assignment scope-clarification admin feature,
  parallel-to-G8 scope call). Re-surfaced, **not baked** (lean noted if any).
- **PS-D1.5 — Scope fence.** D1–D2 = signal stores + dedup ONLY. NOT the gap-detection sweep / trigger
  (D3 / Slice 10), NOT the gap/health crons (D4 / Slice 11), NOT generation (B). Tests zero-network.
- **PS-D1.6 — Reuse + carried holds.** Reuse existing model/migration patterns; D1–D2 is greenfield
  signal capture (no upstream A/B/C dependency for the *store* itself; the trigger wiring is D3).

## STATUS (Slice 9): final — approved by plan-auditor @ `bb2dd89` (content-bound to Slice 9 §9 section)

Slice-9 (D1–D2) content SEAL — 2026-06-10T01:50Z. **0 findings; 5 Confirms (A-46..A-50).** Three §6.5
signals greenfield; G5 one-polymorphic-GapSignal-table surfaced; 3-arm dedup signal-layer; signal-3
define-type-defer-feature; scope fence + §6.5 amend-once. All grounding verified. Set-diff: 5 added
[A-46..A-50] / 0 dropped. Interim seal. (A-45 still open, global-marker-tracked.)

## PS-D3 — pre-registered positions for Slice 10 (D3), before the draft — 2026-06-10T01:50Z

D3 = gap-detection sweep + catalogue-health checks → generation trigger. Ratification (#107 §6): §6.5
rewrite (G2-analog) · NS-4. Anti-anchoring (§4.2). What a correct D3 must contain:

- **PS-D3.1 — Gap-detection sweep clusters `GapSignal` (D1–D2) → topics**, with the **3rd arm** of the
  3-arm dedup/idempotency (D1–D2 = signal-layer 1st; B3 = persistence 2nd; D3 = gap-detection 3rd).
  Triggers generation by **domain-fn call to B** (`enqueue_generated_drafts`/`generate_grounded_drafts`),
  **no HTTP gate** (parent §4.4).
- **PS-D3.2 — Catalogue-health check + NS-4 re-surfaced.** A scheduled health check assessing catalogue
  coverage to trigger generation proactively. NS-4 (what it assesses) — lean **thin-band + uncovered-
  subject**; surfaced, not baked.
- **PS-D3.3 — §6.5 rewrite (G2-analog) SURFACED.** Autonomous §6.5 (capture/detection/generation/
  auto-publish phases). **Amend §6.5 ONCE** (coordinate with C2 §8.3 + D1–D2 §6.5-Inputs — the §1
  amend-once spec-section discipline; OV-38/A-44).
- **PS-D3.4 — Drives B directly (domain-fn, no HTTP gate)** (parent §4.4).
- **PS-D3.5 — Scope fence.** D3 = sweep + health-check + trigger. NOT the crons (D4 schedules them), NOT
  signal capture (D1–D2 done), NOT generation impl (B). Carried holds: D3 needs B merged (it triggers
  generation) + D1–D2 + NS-5 — the slice where the D→B dependency binds. Tests zero-network.
- **PS-D3.6 — A-18 COHERENCE CHECK (carried forward from Slice 3).** Verify the A3 weekly-backstop
  refresh-target-set (catalogue subjects/pills) and the D3 catalogue-health-check definition stay
  **coherent + distinct**: backstop = *refresh corpus* for existing topics on a clock; health-check =
  *trigger generation* for gaps/thin-coverage. If D3 conflates them or leaves a gap/overlap, flag. This
  is the forward watch I committed to at A-18 — now due.

## STATUS (Slice 10): final — approved by plan-auditor @ `ac84af3` (content-bound to Slice 10 §10 section)

Slice-10 (D3) content SEAL — 2026-06-10T02:20Z. **0 findings; 5 Confirms (A-51..A-55).** Gap-detection
sweep + 3rd dedup arm + domain-fn trigger; catalogue-health NS-4 surfaced; §6.5 rewrite amend-once;
**A-18 coherence watch RESOLVED** (A3-corpus-refresh vs D3-pill-generation distinct, test 4 non-overlap);
scope fence. All grounding verified. Set-diff: 5 added [A-51..A-55] / 0 dropped. Interim seal.

## PS-D4 — pre-registered positions for Slice 11 (D4), before the draft — 2026-06-10T02:20Z

D4 = gap-detection + catalogue-health crons. Ratification (#107 §6): cron-count sweep (G9-analog).
Anti-anchoring (§4.2). What a correct D4 must contain:

- **PS-D4.1 — Cron-count sweep (GT-6 / G9-analog), co-owned with A3.** D4 registers the gap-detection +
  catalogue-health crons (scheduling D3's `gap_detection_sweep` + `catalogue_health_check`) → touches the
  **"seven crons"** invariant AGAIN. **Coordinate A3 + D4** (like ops sweep coordinated B1+C1): cron count
  = 7 (main) ± NS-1 (A3 corpus.refresh replaces drive_rag.ingest → net 7, or +1 → 8) **+ 2** (gap-detection
  + catalogue-health) → final value once A3+D4 ratified, **NS-1-coupled**. Three-class cron mirror-sweep
  (word/numeral/beat-registry) at execution HEAD. Must respect GT-6 + the A3 NS-1 coupling. Not naive.
- **PS-D4.2 — Schedules D3's domain fns** (gap_detection_sweep + catalogue_health_check) as beat_schedule
  entries (cadence a default like the existing crons). Reuse beat_schedule pattern.
- **PS-D4.3 — SPEC §8.9 + AC-CD7 cron amendment SURFACED**, coordinate A3+D4 (cron count lands final once
  both ratified; NS-1-coupled). Not baked.
- **PS-D4.4 — Scope fence.** D4 = cron registration ONLY. NOT the sweep/health logic (D3 done), NOT
  generation (B). Tests zero-network. Carried holds: D4 needs D3 merged (it schedules D3's fns) + NS-5.

## STATUS (Slice 11): final — approved by plan-auditor @ `5b1946a` (content-bound to Slice 11 §11 section)

Slice-11 (D4) content SEAL — 2026-06-10T02:55Z. **0 findings; 4 Confirms (A-56..A-59).** Cron-count
sweep co-owned A3+D4, NS-1-coupled (final 9/10), amend-once, cron test-floor oracle; schedules D3 fns;
scope fence. **STAGE D COMPLETE.** Set-diff: 4 added [A-56..A-59] / 0 dropped. Interim seal.

## PS-E1 — pre-registered positions for Slice 12 (E1), before the draft — 2026-06-10T02:55Z

E1 = oversight dashboard read surface (retroactive). Ratification (#107 §6): new AC-CD (dashboard).
Anti-anchoring (§4.2). What a correct E1 must contain:

- **PS-E1.1 — Retroactive read surface.** Recent publishes · per-item **provenance** display · confidence
  scores · source-authority breakdown · spot-check sampling. Consumes the **PublishRecord** (C2/DS8-a) +
  **GenerationProvenance** (B2) + A1 authority tiers. New AC-CD (dashboard API) SURFACED.
- **PS-E1.2 — G6 transform.** The governing API contract is the **dashboard/rollback** surface (#107 §4.5
  / G6-transform), not the admin generate endpoint. **G8** (generate button) + **G7a** (pill_proposal
  refiner) survive as **optional manual paths only**, distinct from the autonomous spine (not baked as
  ratified; G7a carried-lean per OV-2).
- **PS-E1.3 — Retroactive only (no pre-publish step).** E1 is oversight AFTER publish (parent §3: "(E) is
  retroactive oversight only"). Must not introduce a pre-publish gate (would contradict the autonomy pivot).
- **PS-E1.4 — Scope fence.** E1 = dashboard READ surface (API + maybe FE read). NOT rollback (E2), NOT the
  publish gate (C2 done), NOT bootstrap (F1). Carried holds: E1 needs C2 merged (consumes PublishRecord) +
  NS-5. Tests zero-network.
- **PS-E1.5 — Relocated AC-D21 safety-tag override (A-13) — E2 or E1?** Parent §4.5 puts the retroactive
  admin safety-tag retoggle in Stage E; verify whether E1 (read) or E2 (rollback/write) owns it — flag if
  ambiguous. (The override is a write, likely E2.)

## STATUS (Slice 12): final — approved by plan-auditor @ `963d7b2` (content-bound to Slice 12 §12 section)

Slice-12 (E1) content SEAL — 2026-06-10T03:20Z. **0 findings; 4 Confirms (A-60..A-63).** Retroactive
read surface (publishes/provenance/confidence/authority/spot-check); dashboard AC-CD + FE-scope surfaced;
read-only (rollback + AC-D21 override → E2); layering/authz grounded (AC-CD2/AC-CD5/permissions.py).
Set-diff: 4 added [A-60..A-63] / 0 dropped. Interim seal.

## PS-E2 — pre-registered positions for Slice 13 (E2), before the draft — 2026-06-10T03:20Z

E2 = rollback matrix + spot-check. Ratification (#107 §6): new AC-CD (rollback, ruling 5).
Anti-anchoring (§4.2). What a correct E2 must contain:

- **PS-E2.1 — Full rollback matrix (ruling 5):** **per pill · per question · per batch · per source.**
  per-source = retract everything grounded on a discredited corpus source (queries
  `GenerationProvenance.source_host`, B2); per-batch = retract a generation batch (queries `batch_id`,
  B3); per-pill/question via entity ids. New AC-CD (rollback contract) SURFACED.
- **PS-E2.2 — Relocated AC-D21 safety-tag override (A-13).** The retroactive admin retoggle of
  `safety_relevant` lives here (E2 write) — the autonomous-era replacement for the removed pre-publish
  admin catch (parent §4.5). Verify E2 includes it (E1 deferred it as read-only).
- **PS-E2.3 — Rollback keys built upstream.** per-source via `GenerationProvenance.source_host` (B2,
  the queryable-relational provenance I confirmed at A-25 was built for this); per-batch via `batch_id`
  (B3, confirmed at A-31). Verify E2 actually uses these keys (the upstream design pays off here).
- **PS-E2.4 — Scope fence.** E2 = rollback (write/retract) + safety override. NOT the read surface (E1
  done). Carried holds: E2 needs C2 + B2 + B3 merged (the rollback targets/keys) + NS-5. Tests zero-net.
- **PS-E2.5 — Rollback semantics:** what "retract" means (unpublish/hide the pill? mark rolled-back?) —
  build choice; verify it's coherent with the publish model (C2 create_pill) + the audit-log
  (published/rolled-back events, SPEC:290). Spot-check sampling (if not fully in E1) here.

## STATUS (Slice 13): final — approved by plan-auditor @ `ab0851b` (content-bound to Slice 13 §13 section)

Slice-13 (E2) content SEAL — 2026-06-10T03:55Z. **0 findings; 4 Confirms (A-64..A-67).** Full rollback
matrix (pill/question/batch/source) ruling 5; B2/B3/NS-3 keys pay off (per-source claim-level precision);
AC-D21 override relocated (A-13); DS13-a surfaces A1 runtime-editing deferral coherently. **STAGE E
COMPLETE.** Set-diff: 4 added [A-64..A-67] / 0 dropped. Interim seal.

## PS-F1 — pre-registered positions for Slice 14 (F1, FINAL slice), before the draft — 2026-06-10T03:55Z

F1 = bootstrap-on-publish wiring (reframed AC-D7/AC-D23). Ratification (#107 §6): AC-D7/AC-D23 body
change. Anti-anchoring (§4.2). What a correct F1 must contain:

- **PS-F1.1 — Bootstrap trigger moves approve→publish (GT-3/GT-8).** The incremental bootstrap (anchor
  pool + AC-D23 self-review + safety-link curation) fires on **auto-publish** (C2) instead of approve.
  Reuses `generate_anchor_pool_for_pill` (calibration.py:483) + `curate_links_for_pill`
  (safety_links.py:170) (GT-8). Bootstrap currently unwired to approve/publish (GT-8) → F1 wires it to
  the C2 publish event.
- **PS-F1.2 — AC-D7/AC-D23 body change SURFACED.** AC-D7 (remove approve-gate [C2] + bootstrap trigger
  approve→publish [F1] — **multi-slice C2+F1**, amend-once); AC-D23 (bootstrap-on-approve→on-publish).
- **PS-F1.3 — Enqueued/async so publish stays fast (#106 D1 carried).** Bootstrap enqueued (async) off
  the publish event so C2 publish stays fast.
- **PS-F1.4 — Scope fence.** F1 = wire bootstrap to the publish event ONLY. NOT the gate (C2 done), NOT
  the bootstrap primitives (exist, GT-8), NOT generation (B). Rides the C2 publish event. Carried holds:
  F1 needs C2 merged (the publish event) + NS-5. Tests zero-network.
- **PS-F1.5 — GLOBAL MARKER WATCH (after F1 seals):** verify A-45 (§7.3 NS-7 reconciliation) IS resolved
  at the global pass; verify the whole-doc is internally consistent; my global final-marker binds the
  final whole-doc content-SHA (all 3 parties). Convergence: 3 global markers @ one SHA + 3-layer green +
  override window → overseer merges (I never merge).

## STATUS (Slice 14 / F1): final — approved by plan-auditor @ `a379e7f` (content-bound to Slice 14 §14 section)

Slice-14 (F1) content SEAL — 2026-06-10T04:35Z. **0 findings; 4 Confirms (A-68..A-71).** Bootstrap-on-
publish (approve→publish, GT-8 reuse); AC-D7/AC-D23 body change (multi-slice amend-once); async (#106 D1);
floor preserved (safety_links self-guard:200); global-marker-pass section correctly addresses A-45 +
convergence. **ALL 14 SLICES CONTENT-SEALED.** Set-diff: 4 added [A-68..A-71] / 0 dropped.

## GLOBAL-MARKER PASS — readiness (my obligations when the planner runs it)

When the planner runs the global pass (reconcile C1 §7.3 NS-7-line = A-45/OV-39 + post global marker):
1. **Re-verify C1 §7.3** is reconciled to RULED degrade-not-gate (pointing at §1 status-of-record) →
   **A-45 RESOLVES** (drop it). Verify no §7.3↔§1 contradiction remains.
2. **Re-verify the WHOLE DOC** is internally consistent at the final SHA (all per-slice seals' content +
   the §1 cross-cutting + the global-pass reconciliation).
3. **Post MY GLOBAL final-marker** (Status: final — approved by plan-auditor) on my audit branch,
   content-bound to the final whole-doc content-SHA + an approval comment. (Distinct from the 14 interim
   per-slice seals.)
4. **Convergence:** 3 global markers @ one whole-doc SHA + 3-layer green gate + 24h override window
   (collapsed by present spec author) → **overseer** flips draft→ready + squash-merges. I NEVER merge.
5. **Terminal stand-down on merge** (verify via git ls-remote; stop watcher; end role).
