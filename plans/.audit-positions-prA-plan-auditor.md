# Plan-auditor — pre-registered positions for PR-A (Corpus & authority foundation)

**Status: pre-registered — draft not yet read (anti-anchoring, role §4.2)**
**Pre-registered:** 2026-06-10 (UTC) — before any PR-A draft exists on the remote.
**Audit branch:** `claude/plan-auditor-amendment-prs-dipipy`
**Yardstick:** `plans/2026-06-08-amendment-cycle-extraction.md` §C(PR-A)/§B/§D ·
`SPEC.md` v1.8 · `DECISIONS.md` v1.8 · `CODE_SPEC.md` · REQUIRED_READING.md §4/§7.
**Ground-truth at pre-registration HEAD (`git`-verified, not recalled):**
- `DECISIONS.md:7` — "27 decisions (AC-D1 through AC-D27)"; next mint **AC-D28**.
- `CODE_SPEC.md` — AC-CD1…AC-CD24 (closing note :1251 "AC-CD20..24 added"); next mint **AC-CD25**.
- "seven crons" — CODE_SPEC `beat_schedule.py` comment (~:110), §8 prose (~:337), **AC-CD7** (~:632);
  ROADMAP :193/:196 ("all seven crons"); SPEC §8.9; CHECKLIST P11.
- "seven AI operations" — SPEC §6 (~:296) + AC-CD8 enum (~:302) {generation, grading, weakness,
  learning_material, pill_proposal, grade_review, anchor_self_review} (ops-count is **PR-B**, not PR-A).
- AC-D23 bootstrap step 4 = "Embed and index … Drive folder per AC-D22" (DECISIONS:574); AC-D7
  "Implications" = "When a new pill is **approved**, an incremental bootstrap auto-runs per AC-D23"
  (DECISIONS ~:209) — the approve→publish language PR-A's AC-D23 (here) and PR-C's AC-D7 must jointly retell.

These positions are what a *correct* PR-A must satisfy. A position the draft meets → **Confirm**;
a position it fails → a finding (stable `A-<n>` ID). Findings live as PR review comments; this file is
the durable pre-registration record.

---

## Minting & numbering

- **PA-1 (AM-1 / AC-D28 mint).** Source-authority scoring must mint **AC-D28** (next sequential — no
  reuse/renumber). `DECISIONS.md:7` count-header must move 27→28 / "AC-D1 through AC-D27"→"…AC-D28",
  and the v1.x amendment line must record the mint. Body must carry decision/context/rationale/status/
  version + `Related` back-links on AC-D21/AC-D22 (extraction §B AM-1). SPEC §7.4 web-search prose
  (allowlist-restricted) updated. Exact body+ID is **ratification-class (i)** — must be authenticated,
  not planner-assigned (extraction §D).
- **PA-2 (AM-2 / AC-CD25 mint).** Reference-corpus-builder must mint **AC-CD25** in CODE_SPEC §18; the
  ledger index + the closing "No open technical anchors / AC-CD20..24" note (:1251) must extend to
  AC-CD25. Body: acquisition pipeline + `CorpusChunk` table + authority stamping + pgvector/
  `AIProvenanceMixin` reuse. Schema shape (DS2-a) is ratification-class — authenticated, not invented.
- **PA-3 (AM-3 / AC-CD1 dep).** HTML→text extraction dep added to **AC-CD1** body + `requirements*.txt`
  pins. HTML-only vs HTML+PDF corpus scope must be *surfaced* (not silently chosen). Ratification-class.

## Complete cross-stage AC-D body folds — NO silent partial-fold (role §7; extraction §A)

- **PA-4 (AM-4 / AC-D21 COMPLETE [A2+C1+E2]).** All three substances present: (a) web-search extended
  to corpus acquisition (A2); (b) safety-pass re-adjudicates `safety_relevant` (C1); (c) admin
  tag-override relocates to retroactive dashboard/rollback (E2). + SPEC §7.4. Any one missing = finding
  (silent partial-fold recreates the inconsistency the amendment exists to close).
- **PA-5 (AM-5 / AC-D22 COMPLETE [A2+B2]).** (a) Drive-folder ingestion retired → AI-built corpus (A2);
  (b) "queried at every generation call" extended to §6.5 (B2); PLUS the **Drive→corpus mirror-sweep**
  three-class structural pass (SPEC:302/334/403-405, DECISIONS:574, §8.x). SPEC §7.3 + §6.5 touched.
  Couples **NS-1**.
- **PA-6 (AM-6 / AC-D23 COMPLETE [A2+B2/C1+F1], OV-67 triple-touch).** (a) Drive embed step 4 retired
  (DECISIONS:574); (b) cross-provider self-review precedent the gate extends (B2/C1); (c)
  bootstrap-on-approve → **bootstrap-on-publish** (F1). **Double-touch warning:** AC-D23 is hit by both
  the Drive-retirement angle and the bootstrap angle — authored once here, not partial-folded.

## Cron-count (the cross-PR-critical number)

- **PA-7 (AM-7 / §8.9 + AC-CD7 cron-count COMPLETE [A3+D4]).** Every "seven crons" surface must move
  consistently to the ratified number: CODE_SPEC :110/:337/:632 (AC-CD7) + SPEC §8.9 + ROADMAP :193/:196
  + CHECKLIST P11. Add `corpus.refresh` (A3) + `gap_detection.sweep` + `catalogue_health.check` (D4).
  Final = **nine** (NS-1=retire: corpus.refresh replaces drive_rag.ingest net 0, +2) **or ten** (NS-1=
  keep: +1). No surface may be left saying "seven". Number is **held on NS-1**.
- **PA-8 (NS-1 ruling).** Retire-Drive-code-vs-keep-dormant must be **authenticated + current**; it sets
  BOTH the cron number (PA-7) AND the AC-D22 shape (PA-5). Lean = relocate shared
  `chunk_document`/`content_hash`/`cosine_top_k` to a shared module, not delete. Verify chosen number
  flows to every surface coherently.

## Ratification authenticity & sequencing rulings (role §8.3; extraction §D)

- **PA-9 (relay discipline / OV-2).** The merged detail-plan reads as a **relay** downstream — NOT
  actionable on its own. Every ratification-class change here (4 mints' exact bodies+IDs, count-header
  sweep, AM-2/AM-3 schema, NS-1, NS-5) must rest on an **authenticated, current** spec-author ruling.
  PR-A body cites "9 ratifications + 3 coordination rulings" — verify each is authentic/current, and that
  the body does **not** treat a relayed/inferred/superseded ruling as actionable. A merge forming on a
  relay = surface, never clear.
- **PA-10 (DS13-a binds AM-1's shape — must be ruled BEFORE PR-A is authored).** DS13-a decides whether
  source-authority AC-D is multi-slice [A1+E2] — i.e. whether PR-A must already carry the **E2
  DB-source-override / source-demotion layer**. If ruled (i): PR-A MUST carry that layer; its absence is
  a finding (racing ahead of an owed binding). If ruled (ii) content-only-retract: allowlist stays
  code-only. Verify PR-A's AM-1 shape matches the *current* DS13-a ruling.
- **PA-11 (NS-5 ROADMAP phase-home).** Gates every slice's execution-close. PR-A touches ROADMAP +
  CHECKLIST, so any phase identity it writes must match the ruled NS-5 home; if NS-5 unruled, PR-A must
  not fabricate a placement (surface as pending, don't invent).

## Cross-PR coordination — the chain the task put me on watch for

- **PA-12 (AC-D23 [PR-A] ↔ AC-D7 [PR-C] — one approve→publish story).** PR-A's AC-D23 says bootstrap
  fires on **publish**; PR-C's AC-D7 removes the approve gate. PR-A's AC-D23 must read coherently with
  that — no dangling "on approve" semantics, no statement AC-D7 will contradict. **Both sides must read
  coherently** — re-verify AC-D23 here when PR-C's AC-D7 lands. Also note AC-D7 Implications
  (DECISIONS:~209) currently says bootstrap runs "when a new pill is **approved**" — that line is AC-D7's
  to change in PR-C, but PR-A's AC-D23 must not leave the joint story inconsistent.
- **PA-13 (cron-count [PR-A] vs ops-count [PR-B] — do not conflate).** PR-A ratifies the **cron**-count
  (§8.9, nine/ten). PR-B's §6 **ops**-count (seven→nine ops) is a different number on a different axis.
  PR-A must not conflate crons with ops; the cron number PR-A ratifies is the one PR-B must **respect**,
  not redefine. Track for drift across the link.
- **PA-14 (§8.9 crons [PR-A] presuppose GapSignal [PR-D] / §6.5 [PR-C]).** AM-7 introduces
  `gap_detection.sweep` + `catalogue_health.check`; these presuppose the GapSignal model (PR-D, AM-15)
  and the §6.5 gap-detection phase (PR-C, AM-13). PR-A's cron prose must be coherent with the GapSignal
  shape coming in PR-D — not assume a shape PR-D contradicts. (Primary §6.5↔GapSignal coupling is
  PR-C↔PR-D; PR-A touches the edge via the sweep cron.)

## Structural / scope / completeness

- **PA-15 (amend-once front-loading intact).** Because A2 is first toucher of AC-D21/22/23, PR-A MUST
  carry the COMPLETE cross-stage bodies of all three — no shared anchor split out into a later PR. A
  shared-anchor split = the dominant-constraint violation (extraction §A).
- **PA-16 (file-set correctness — FE_CHECKLIST scrutiny).** Extraction §C(PR-A) scopes PR-A to
  `DECISIONS, CODE_SPEC, SPEC(§7.3/7.4/8.9), ROADMAP, CHECKLIST, requirements*`. The cron-count lives in
  **backend CHECKLIST.md P11**, not FE_CHECKLIST.md. The task's ID criteria name **FE_CHECKLIST.md** — if
  PR-A touches FE_CHECKLIST, verify the justification; if it edits FE_CHECKLIST in place of / in addition
  to CHECKLIST P11 cron rows, that's a finding candidate (wrong/extra surface, or a missed CHECKLIST row).
- **PA-17 (triage-artifact fold-completeness, role §7).** PR-A body cites a ratification triage + 9
  ratifications + 3 coordination rulings. Verify the triage enumerates the full §D ratification surface
  for PR-A (no scoped item silently dropped — a silent partial-fold of the triage is itself a finding).
- **PA-18 (draft status).** PR-A is/should be a **draft** PR. I never flip draft→ready and never merge
  (role §3.7). Recorded here so the boundary survives compaction.

---

*Finding IDs `A-1…` will be assigned as positions are tested against the actual draft text. This file is
the §6 baseline: first wake-log line records `0 dropped / 0 added (baseline — positions pre-registered)`.*
