# Plan-auditor — pre-registered positions for PR-C (Auto-publish gate + self-review + governance prose)

**Status: pre-registered — PR-C draft not yet authored (anti-anchoring, role §4.2)**
**Pre-registered:** 2026-06-11, after PR-B (#111) merged to `main` `921ac23` — before any PR-C draft/ratification.
**Link 3 of 4** in the sequenced cycle (PR-A ✅#110 · PR-B ✅#111 · **PR-C** · PR-D).
**Yardstick:** `plans/2026-06-08-amendment-cycle-extraction.md` §C(PR-C)/§B/§D · live `SPEC.md`/`DECISIONS.md` v1.9 on `main`
· REQUIRED_READING.md §7 · the **v1.9 one-version-per-cycle standing rule** (OV-B1).

**Ground truth at pre-registration (`git show origin/main`, verified):**
- Count-header: "**29 decisions** (AC-D1 through AC-D29; … AC-D29 added in v1.9)". Next AC-D mint = **AC-D30**;
  PR-C mints **two** (self-review + auto-publish-gate) → **AC-D30 + AC-D31**, count-header **29→31**.
- **AC-D7 Implications (DECISIONS:205):** "New pill proposals surface in admin's **review queue** … When a new pill is
  **approved**, an incremental bootstrap auto-runs per AC-D23." ← AM-12's removal target. AC-D7 **Related** already lists AC-D23.
- **AC-D23 on main (PR-A, merged):** already states "AC-D7's removal of the 'queue for admin review / approve' governance
  language (PR-C) and this anchor's bootstrap-on-publish reframe together tell **one** approve→auto-publish story; **AC-D7
  carries the reciprocal back-reference**." So AC-D23's forward side is locked on main; PR-C must complete AC-D7's reciprocal.
- **§6.5 (SPEC:344) "Pill proposal"** = the human-gated section AM-13 rewrites. **§290 audit-log (SPEC:294)** = "pill
  proposals approved or rejected" (AM-14 target). **§6/§7.1/§7.2/§9.7** carry PB-5's `content_self_review` forward-refs
  ("protocol AC-D + wiring land in … PR-C") that AM-10 must satisfy.

**Per-link discipline:** PR-C's ratifications need their **own** authenticated in-session ratification (the PR-A/PR-B rulings
do NOT cover PR-C). NS-7 is "already RULED degrade-not-gate" but **OV-2 requires each execution/amendment PR re-confirm it
through its own authenticated channel** — the merged ruling does not auto-actionate downstream.

---

## Mints (AC-D30 self-review · AC-D31 auto-publish-gate)

- **PC-1 (AM-10 self-review AC-D mint → AC-D30) — satisfies PB-5 carry-forward.** Mint the self-review protocol AC-D (cites
  AC-D19/AC-D23 cross-provider precedent). **It DEFINES `content_self_review`** — the op PR-B's ops-count nine forward-referenced.
  Verify it is **coherent with PR-B's forward-refs** on main (§6 "content self-review … protocol AC-D … PR-C", §7.2
  "degrades-not-gates per NS-7", §9.7). Carries **NS-2** (one `content_self_review` op + 3 variants vs reuse
  `anchor_self_review`) and **NS-7** (degrade-not-gate). Ratification-class (i).
- **PC-2 (AM-11 auto-publish-gate AC-D mint → AC-D31).** Mint the auto-publish-gate AC-D (rulings 1+2: **single global
  threshold + publish-with-warning**; removes the human approve gate **for generated drafts**). Carries **NS-6** (threshold
  numeric default + `compute_confidence` formula + per-type telemetry; couples DS1-a = the PR-A T1/T2/T3 scores 1.0/0.6/0.3).
- **PC-3 (count-header sweep 29→31, v1.9 batch).** DECISIONS:7 "29"→"31 (AC-D1 through AC-D31; … AC-D30/AC-D31 added in v1.9)";
  the v1.9 amendment paragraph extends to a "**third link — PR-C**" entry; footer 31. **Stays v1.9** (standing rule).

## AC-D7 (AM-12) — the PA-12 cross-link, NOW DUE

- **PC-4 (AM-12 AC-D7 complete [C2+F1]).** Remove the "queue for admin review / approve" governance language (C2) +
  incremental bootstrap fires on **auto-publish** not approve (F1). AC-D7 Implications (DECISIONS:205) rewritten: no
  approve-gate; bootstrap on auto-publish. Also SPEC reference (§3/§4/§5; the SPEC:174-area "review queue" if present).
- **PC-5 (PA-12 — AC-D7 ⇄ AC-D23 ONE approve→publish story; the hard mutual cross-ref the dispatch flagged).** PR-C's AC-D7
  MUST: (a) remove the approve-gate language; (b) carry the **reciprocal back-reference to AC-D23** (AC-D23 already points
  here); (c) tell the **same** approve→auto-publish story AC-D23 tells — **no contradiction**. **Verify BOTH sides read
  coherently together on `main`** (AC-D23 already merged + AC-D7 now amended): no dangling "approved" semantics, the auto-publish
  gate (AC-D31) is the referenced mechanism, bootstrap-on-publish is consistent across AC-D7/AC-D23/AC-D31. This is THE
  cross-PR coherence check from the dispatch — scrutinize it hardest.

## §6.5 rewrite + §290 audit-log

- **PC-6 (AM-13 §6.5 rewrite complete [C2+D1–D2+D3], 3-slice OV-45).** Rewrite §6.5 "Pill proposal" (human-gated) →
  autonomous **signal-analysis (D1–D2 Inputs) / gap-detection (D3) / generation (B) / auto-publish (C2)** phases. **NS-4
  health-check** definition lands here (thin-band + uncovered-subject; coherent with A3 corpus-refresh — distinct actions).
  **PA-14 carry-forward:** the §6.5-Inputs prose for the three signals describes the same signals as the **§5 GapSignal entity
  (PR-D, AM-15)** — verify §6.5 forward-references GapSignal coherently, NOT pre-committing a shape PR-D contradicts. Also
  must stay coherent with §8.9's `gap_detection.sweep`/`catalogue_health.check` crons (PR-A, on main).
- **PC-7 (AM-14 §290 audit-log complete [C2+E2], OV-38).** "pill proposals approved or rejected" → **published /
  publish-with-warning / rolled-back** events. The rolled-back EVENT is E2's (PR-D) but the §290 prose lands **complete here** —
  verify no partial-fold (all three event classes present, forward-ref to E2 rollback coherent).

## Rulings / discipline

- **PC-8 (NS-7 degrade-not-gate — RE-CONFIRM through PR-C's own channel, OV-2).** NS-7 is ruled, but the merged ruling is a
  **relay** downstream — PR-C must re-confirm it through its **own authenticated channel**. Verify: single-provider
  safety-relevant content **publishes-with-warning** (always dashboard-flagged) on **same-model multi-pass review + a
  "single-provider verified" flag; NO second-provider prerequisite gate**. Binds AM-10 (C1 exposes the `degrade` switch) +
  AM-11 (C2 reads it). Flag any "second-provider-gate" framing as a contradiction of the ruling.
- **PC-9 (NS-2 — op consistency with PR-B's nine).** `content_self_review` = **one op** (+ variants), NOT a reuse of
  `anchor_self_review`. Must match PR-B's ops-count nine (content_self_review = the 9th, cross-family). Verify the self-review
  AC-D's op definition is the SAME op PR-B counted (no second new op, no count drift back).
- **PC-10 (NS-6 — confidence contract).** Auto-publish threshold numeric default + `compute_confidence` formula + per-type
  telemetry; **reads the AC-D28 authority scores (1.0/0.6/0.3, DS1-a)** coherently (PR-A on main). Verify the confidence
  contract couples DS1-a without re-deciding the tier scores.
- **PC-11 (per-link authentication).** PR-C's ratifications (AC-D30/D31 exact bodies+IDs, NS-2, NS-6, NS-7 re-confirm) rest on
  PR-C's **own** authenticated in-session ratification; the body cites **this conversation**, not a relay/triage-only.
- **PC-12 (version-pairing completeness — v1.9 standing rule; OV-1/OV-2 + PB-19 lesson).** PR-C **stays v1.9**. Sweep EVERY
  header+footer+pairing line (SPEC, DECISIONS; CODE_SPEC if touched) — all v1.9; count-header 29→31; PR-C folds into the v1.9
  batch ("third link — PR-C"); **no `v1.10`/`v1.11` straggler**; no "v1.9 = 29 decisions" left where it should be 31.
- **PC-13 (amend-once / no out-of-scope anchor).** PR-C must NOT touch PR-D anchors (oversight AC-CD, GapSignal §5, §4.11).
  It legitimately touches AC-D7, mints AC-D30/D31, §6.5, §290, §6 error-handling. Verify no PR-D anchor body changed (grep diff).
- **PC-14 (auto-publish-gate scope — no over-reach).** AM-11 removes the human approve gate **for generated drafts only** —
  verify it does NOT remove admin oversight/approve elsewhere (grades AC-D19, other flows). The retroactive-oversight model
  (read E1 + rollback E2, PR-D) is the replacement, not "no oversight".
- **PC-15 (file-set).** PR-C target ≈ `DECISIONS.md` (AC-D30, AC-D31, AC-D7), `SPEC.md` (§6.5, §290, §6 error-handling).
  Likely **no CODE_SPEC** (no AC-CD mint) and **no cron-row** touch. May update CHECKLIST workstream C1/C2 rows. Flag spurious/missing.
- **PC-16 (draft status).** PR-C is/should be a draft; I never flip draft→ready and never merge.

---

*Findings get stable IDs `C-1…`. Carry-forwards arriving at this link: **PA-12→PC-5** (AC-D7↔AC-D23, the dispatch's flagged
cross-link), **PB-5→PC-1** (content_self_review protocol AC-D), **PA-14→PC-6** (§6.5↔GapSignal PR-D). Still forward: PB-9
(provenance source_host ↔ E2) and the §290 rollback EVENT resolve at **PR-D**. v1.9 standing rule (PC-12) holds for PR-D too.*

---

**PARKED (2026-06-11) — stood down per spec-author instruction after PR-B merged.** These PR-C positions are
pre-registered (anti-anchoring preserved — written before any PR-C draft/ratification). A re-dispatch for PR-C resumes
non-blind: reconstruct from this file + the wake-logs, re-arm a watcher, await PR-C's authenticated ratification + draft.

---

**RESUMED (2026-06-11) — stand-down rescinded (wrong session); continuing PR-C.** PR-C planner branch
`claude/pr-c-autopublish-selfreview` @ `fa51e35` = **triage only** (`ratification-triage-…-pr-c…md`, 9 decisions C-1…C-9),
awaiting spec-author batch ruling (PC-11 per-link auth). Triage cross-checked vs PC-1…PC-16: **defaults align comprehensively**
(C-1 AC-D30 self-review/NS-2 one-op=PC-1/PC-9; C-2 AC-D31 auto-publish=PC-2; C-3 NS-6/0.70=PC-10; C-5 AC-D7+reciprocal=PC-4/PC-5
PA-12; C-6 §6.5 rewrite+GapSignal fwd-ref=PC-6/PA-14; C-8 §290 3-event=PC-7; stay v1.9 29→31=PC-3/PC-12). **Adjust PC-15:** PR-C
DOES touch CODE_SPEC (the AC-CD8 content_self_review caveat, C-9) — that is in-scope (completing PB's forward-ref), not spurious.
**FLAG (pre-draft, my built-state-honesty catch — echoes PR-B B-2/PB-17):** C-9 proposes the AC-CD8 caveat
"content_self_review (v1.9, wiring completes in PR-C)" → **"wired"** — but content_self_review's **enum value + code wiring are
C1 EXECUTION (unbuilt at PR-C amendment time)**; PR-C defines only the **protocol** (AC-D30). "wired" would be built-state-FALSE.
Correct: "**protocol defined in PR-C (AC-D30); enum value + wiring at C1 execution**" (keep a built-state caveat, don't claim
wired). → **C-finding if the draft says "wired".** Genuine spec-author choices: **C-7** (NS-4 define-now-in-§6.5 vs defer-PR-D)
+ **C-3** (0.70 threshold). NS-7 re-confirm (PC-8/OV-2): the spec-author's PR-C ruling should explicitly re-confirm NS-7's
AM-10/AM-11 binding, not rely on "carried" status.

---

## RATIFICATION FOLD — authenticated spec-author ruling (2026-06-11, in-session, direct origin)

**PR-C gate CLEARED (PC-11 met).** All 9 decisions ratified = planner defaults (C-1…C-9 = A); cycle **stays v1.9**.
Draft still unread → anti-anchoring intact. Bound values (verify the draft matches EXACTLY):

- **PC-1/PC-9** → **C-1=A:** AC-D30 self-review; **one `content_self_review` op + 3 variant passes** (grounding/safety/
  provenance), OpenAI cross-model (ruling 4); **safety pass re-adjudicates `safety_relevant`**; distinct op from
  `anchor_self_review`. Matches PR-B's nine (content_self_review = the 9th).
- **PC-2** → **C-2=A:** AC-D31 auto-publish gate; **single global threshold + publish-with-warning** (rulings 1+2); replaces
  `approve_pill_proposal` for generated drafts; nothing held pre-publish.
- **PC-10** → **C-3=A:** NS-6 threshold **0.70** + authority-weighted (DS1-a 1.0/0.6/0.3) `compute_confidence` + per-type
  telemetry day one (threshold tunable).
- **PC-17 (NEW) → C-4=A:** **`PublishRecord` table** (DS8-a) as the separate E1/E2 read-surface (publish-state distinct from
  Pill reference data). Verify the AC-D31 body / §6 describe it for E1 read + E2 per-batch rollback join.
- **PC-4/PC-5 (PA-12)** → **C-5=A:** AC-D7 — remove approve-gate → auto-publish; **bootstrap-on-publish (F1)**; **reciprocal
  back-ref to AC-D23** (closes the mutual cross-ref ruled in PR-A). Verify the AC-D7↔AC-D23 one approve→publish story is fully
  coherent on main (scrutinize hardest).
- **PC-6 (PA-14)** → **C-6=A:** §6.5 **full rewrite** to the autonomous pipeline (signal→gap/health→generate→auto-publish);
  GapSignal forward-refs PR-D (no schema bake); amend-once forces the complete rewrite now.
- **PC-6** → **C-7=A:** **NS-4 health-check (thin-band + uncovered-subject) DEFINED in PR-C §6.5** (intentional deviation from
  the extraction's PR-D grouping). **NEW carry-forward to PR-D: PR-D IMPLEMENTS against the NS-4 definition without
  re-amending it** — flag any PR-D re-amendment of NS-4 as out-of-scope/double-touch.
- **PC-7** → **C-8=A:** §290 audit-log all three states (published / publish-with-warning / **rolled-back**); rolled-back
  forward-refs E2.
- **PC-3/PC-12** → **C-9=A:** §6.9 "Content self-review" subsection; AC-D30+AC-D31 mints; count-header **29→31**; **stay v1.9**.

**C-9 built-state flag — CONCEDED (spec-author adjudicated).** I flagged "AC-CD8 caveat → wired" as premature; the spec-author
ruled **C-9=A including "caveat updated to wired"** (informed of my flag). I concede — that is the spec-author's content call.
**Refined verification (PC-18, NEW):** "wired" = the **doc/protocol-level** resolution (AC-D30 now defines the op, so the
"wiring completes in PR-C" forward-ref is satisfied). Verify it does **NOT** create an internal **build-state inconsistency** —
the CHECKLIST/workstream rows must still show `content_self_review`'s **code** (enum value + impl) as **C1-execution-unbuilt**;
"wired" must not be read as "the code op is built." If the draft's "wired" claims the code is built, that is a C-finding.

**NS-7 (PC-8):** the ruling ratifies C-1/C-2 which bake NS-7 (degrade-not-gate); NS-7 was prior triple-authenticated. Treat as
re-confirmed via the C-1/C-2 ratification. **Verify the draft's AM-10/AM-11 honor NS-7** — single-provider safety-relevant
content **publishes-with-warning** (dashboard-flagged) + "single-provider verified" flag; **NO second-provider prerequisite
gate**. Flag any second-provider-gate framing.
