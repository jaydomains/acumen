# Plan-auditor — pre-registered positions for PR-B (Generation + provenance + ops-count)

**Status: pre-registered — PR-B draft not yet authored (anti-anchoring, role §4.2)**
**Pre-registered:** 2026-06-10 (UTC), immediately after PR-A (#110) merged to `main` `c5128d9` — before any
PR-B draft or PR-B ratification exists. **Link 2 of 4** in the sequenced cycle.
**Audit branch:** `claude/plan-auditor-amendment-prs-dipipy`.
**Yardstick:** `plans/2026-06-08-amendment-cycle-extraction.md` §C(PR-B)/§B/§D · live `SPEC.md`/`DECISIONS.md`/
`CODE_SPEC.md` v1.9 on `main` · REQUIRED_READING.md §7.

**Ground truth at pre-registration (`git show origin/main`, verified):**
- AC-D last = **AC-D28** → next mint **AC-D29**; AC-CD last = **AC-CD25** → next **AC-CD26**.
- Ops-count "**seven**" surfaces: `DECISIONS.md:96` (AC-D1 Implications "seven AI-driven operations"),
  `SPEC.md:298` ("seven distinct AI operations"), `SPEC.md:399` ("five of seven operations" — Anthropic),
  `CODE_SPEC.md:304-308` (AC-CD8 enum {generation, grading, weakness, learning_material, pill_proposal, …} +
  "The seven operations route to the four protocol methods"), `DECISIONS.md:346` (AC-D12 "five Anthropic
  operations default to Anthropic"). The AC-CD8 enum already carries **8 entries** (7 named ops + `embed`, GT-1).
- **Cron-count = nine is now RATIFIED on `main`** (PR-A) — PR-B must NOT touch it; the two nines are different axes.

**Per-link discipline:** PR-B's ratifications are NOT covered by the PR-A ruling — each needs its own authenticated
in-session ratification before authoring (spec-author: "PR-B/C/D … each gets its own direct-channel authentication
when their turn comes"). These positions are the §6 baseline; I fold PR-B's authenticated values when they land.

---

## Ops-count (AM-8) — the cross-PR-critical axis (carries PA-13 forward)

- **PB-1 (AM-8 ops-count seven→nine — COMPLETE sweep, §7 no silent partial-fold).** Every ops-count-bearing
  surface must move consistently and name the two new ops (`pill_generation` B1 Anthropic +1; `content_self_review`
  C1 cross-family +1): AC-D1 Implications (DECISIONS:96), SPEC §6 (:298), SPEC §7.1 provider split (:399
  "five of seven" → updated, e.g. six-of-nine), AC-CD8 enum + routing prose (CODE_SPEC:304/308), AC-D12 Implications
  (:346 "five Anthropic operations" → six). No surface left at the old count. (This is PR-B's analogue of PR-A's
  cron-count sweep — completeness is the bar.)
- **PB-2 (ops-count vs enum-count axis, GT-1).** The AC-CD8 enum has 8 entries (7 ops + `embed`); PR-B adds 2 →
  **10 entries (9 ops + embed)**. The "nine **operations**" prose vs the 10-entry enum must stay coherent — `embed`
  is the mechanical embedding op (corpus acquisition per PR-A), NOT one of the nine AI operations. Verify the
  enum-count/ops-count distinction is handled, not conflated.
- **PB-3 (AC-D1 Implications body change — class (i)).** AC-D1's "seven AI-driven operations" list → nine, naming
  the two new ops. AC-D1 is a canonical anchor → ratification-class; verify authenticated, body amended in place.
- **PB-4 (PA-13 CARRY-FORWARD — two-nines, NO conflation).** PR-B's ops-count **nine (operations)** must not be
  conflated with the **nine crons (§8.9, ratified on main)**. PR-B must **not** touch §8.9 / AC-CD7 / the cron
  surfaces; its §6 "nine" must read unambiguously as **operations**. Flag any cross-wiring.
- **PB-5 (AM-8 front-load echo — analogue of PR-A's AC-D23→AC-D7).** The ops-count nine includes
  `content_self_review` (C1), whose **defining self-review AC-D lands in PR-C (AM-10)**. So PR-B's "nine" references
  an op whose anchor is PR-C's. Verify PR-B forward-references that cleanly (not a dangling/undefined op); re-verify
  the coherence when PR-C's self-review AC-D lands.
- **PB-6 (AM-8 authoring choice — combined vs per-op).** Spec-author choice: one combined seven→nine amendment OR
  per-op (seven→eight at B1, eight→nine at C1). Per-op is tolerated because construction-oracle test floors + greps
  make completeness checkable (extraction §7.4). Verify whichever is ratified is followed and completeness holds.

## Provenance (AM-9) — new AC-D mint + relational table

- **PB-7 (AM-9 provenance AC-D mint → AC-D29).** Mint the provenance-chain AC-D (claim → corpus source + authority
  tier) as next-sequential **AC-D29**; DECISIONS count-header 28→29 + v1.x amendment line + footer (full
  version-pairing sweep — PB-13). Exact body+ID ratification-class (i) — authenticated, planner does not assign IDs.
- **PB-8 (NS-3 granularity).** `claim_ref` granularity (per-assertion vs per-draft) — lean per-assertion. Sets the
  claim_ref shape; ratification-class. Verify authenticated + body matches the ruled granularity.
- **PB-9 (AM-9 table shape — RELATIONAL, cross-PR coupling).** `GenerationProvenance` must be a **relational** table
  (NOT JSONB) because **E2 (PR-D) queries by `source_host`** (§5.1). Verify PR-B fixes it relational and surfaces
  the E2/PR-D coupling. (If PR-B mints an AC-CD for the table, it would be AC-CD26 — verify whether the table is a
  new AC-CD or folded into the provenance AC-D body.) Also couples AC-D28's authority-tier (PR-A, on main).

## Secondary rulings

- **PB-10 (G7b prompt-version trajectory).** `pill_generation` v1.0.0 (B1) → v1.1.0 at B2 (grounding_refs added);
  rides the prompt registry, **not** a doc anchor. Verify PR-B does not over-amend a doc anchor for this (build choice).
- **PB-11 (G3 per-band difficulty decomposition).** Lean min/max only; if ruled per-band, amends the generation
  AC-D / §6.5 output contract (no standalone anchor). Verify whichever is ratified; no fabricated anchor.

## Discipline / structural (carries OV-1/OV-2 lesson forward)

- **PB-12 (relay/authentication, per-link).** PR-B's ratifications (AC-D29 exact body/ID, NS-3, ops-count authoring
  choice, G7b/G3) must rest on **PR-B's OWN authenticated in-session ratification** — the PR-A ruling does NOT cover
  PR-B. Verify the body authenticates through the channel, not a relay/inference, and is current.
- **PB-13 (VERSION-PAIRING COMPLETENESS SWEEP — folded from OV-1/OV-2).** PR-B bumps to **v1.10**. Sweep **every**
  version-pairing line — *headers AND footers AND pairing pointers* — across ALL touched paired docs (SPEC header:6
  + footer; DECISIONS header:5 + footer; CODE_SPEC header:16 + footer §18), so none self-contradicts on version /
  decision-count. (This is the gap the overseer caught on PR-A as OV-1/OV-2; I own it now as a standing check.)
- **PB-14 (amend-once across PRs — no out-of-scope anchor).** PR-B must not touch PR-C/PR-D anchors (self-review
  AC-D, auto-publish AC-D, AC-D7, oversight AC-CD, GapSignal §5). It legitimately touches AC-D1, AC-CD8, AC-D12(?),
  §6, + mints AC-D29. Verify no out-of-scope anchor body changed (grep the diff).
- **PB-15 (file-set).** PR-B target ≈ `DECISIONS.md`, `CODE_SPEC.md`, `SPEC.md` (§6). Should NOT re-touch the §8.9
  cron surfaces or PR-A's ROADMAP/CHECKLIST cron rows (done). May add provenance/ops execution rows to the
  autonomous-content workstream section. Flag spurious/missing surfaces.
- **PB-16 (draft status).** PR-B is/should be a **draft**; I never flip draft→ready and never merge (role §3.7).

---

*Findings get stable IDs `B-1…`. First PR-B audit-branch commit records the §6 baseline. Carry-forwards still live:
PA-12 (AC-D7 reciprocal @PR-C), PA-14 (GapSignal coherence @PR-D), and now PB-5/PB-9 (cross-PR echoes into PR-C/PR-D).*
