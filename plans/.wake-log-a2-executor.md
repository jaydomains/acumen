# Wake-log — Slice A2 executor (reference corpus builder)

Mechanical append-only action log for the sequenced code-execution audit loop on the
A2 slice PR (autonomous-content-generation workstream, slices A1→F1). Each line:
`<UTC date> · <action> · <last-seen remote HEAD> · <set-diff> · <round-trips>`.
A1 merged @ main `4f95414` (PR #114) — A2 branches off that updated main, carrying A1's
`source_authority` module (the import dependency AC-CD25/OV-11 require live on main).

- 2026-06-12 · A2 draft authored — built against the **ratified AC-CD25 body**
  (`CODE_SPEC.md:1278`) + amended AC-D22/AC-D21 + AC-D28, not the detail-plan lean. Verified the
  formerly-surfaced items as ratified: extraction = HTML+PDF (bs4+pypdf, AC-CD1, deps already
  pinned in `requirements.txt` by PR-A); DS2-b = **option (ii)** cross-source corroboration for
  safety-relevant topics; new `CorpusChunk` table (DS2-a); NS-1 = relocate (separate step — A2
  imports `chunk_document`/`content_hash` from `drive_rag`, does not delete it). Files:
  `app/models.py` (`CorpusChunk` — DriveChunk-mirror + `source_host`/`authority_tier`/
  `authority_score`/`corroboration_count`, unique `(source_host, content_hash)`),
  `alembic/versions/0009_a2_corpus_chunk.py` (table + IVFFlat `ix_corpus_chunk_embedding` +
  trigger, up/down clean), `app/domain/corpus_builder.py` (NEW: `acquire_for_topic` —
  search→filter_to_allowlist→fetch(httpx seam, fail-soft)→extract(HTML/PDF)→chunk_document→
  content_hash dedup→embed→persist; cross-source corroboration), `app/ai/cost.py` (`CorpusChunk`
  joins `current_month_spend`), `tests/unit/test_corpus_builder.py` (8 zero-network tests),
  `tests/unit/test_p1_schema.py` (corpus_chunk in table set → 35; trigger count → 35), CHECKLIST
  A2 row missing→built. Three-layer local green: ruff lint+format clean, mypy clean, structure-gate
  OK, unpinned-deps OK, 430/430 unit pass; migration `--sql` round-trip validated offline.
  · last-seen remote HEAD `4f95414` (origin/main, post-A1-merge) · set-diff `0 dropped / 0 added
  (baseline — no reviewer findings yet)` · round-trips: none open.
- 2026-06-12 · A2 revision r1 — folded the **blocking SSRF/allowlist-escape** finding (CA-A2-2 ≡
  overseer OV-A2-17 ≡ Gitar F1) + the two coupled refines (CA-A2-3≡Gitar-F2 memory, CA-A2-4≡Gitar-F3
  N+1), each verified against the code first. **SSRF (REAL, AC-D28 contract break):** `_fetch_body`
  followed redirects without re-validating the final host, so an allowlisted host 3xx-redirecting
  off-allowlist (e.g. `169.254.169.254`) was fetched/embedded/persisted stamped with the original
  host's T1 authority. Fix: `_fetch_body` returns the **final** host; `_gather_candidates`
  re-validates it via `authority_tier` (drop if off-allowlist) + re-resolves the tier from the final
  host (stamps where the bytes actually came from). **Memory:** `_fetch_body` now **streams** with a
  hard `_HTTP_FETCH_MAX_BYTES` cap (was `content[:MAX]` after a full buffer). **N+1:** batched dedup
  into one `_existing_keys` IN-query. Added `test_redirect_off_allowlist_is_dropped` +
  `test_redirect_within_allowlist_restamps_final_host`. 432/432 unit pass; ruff/format/mypy clean.
  **CA-A2-1 (DS2-b corroboration efficacy) — SURFACED to the spec author, NOT baked:** exact-text
  (content_hash) corroboration is the documented v1 floor; whether that satisfies AC-CD25's "same
  claim/fact" intent or should be embedding-similarity is a substantive safety-mechanism ambiguity
  both reviewers flagged — held pending an authenticated ruling (§7(a)). Gitar-F4 (nullable embedding)
  = not-a-finding (both reviewers: faithful DriveChunk-mirror per AC-CD25). · last-seen remote HEAD
  `54dbf05` · set-diff `8 added [CA-A2-1(surfaced), CA-A2-2, CA-A2-3, CA-A2-4, OV-A2-17, Gitar-F1,
  Gitar-F2, Gitar-F3] / 0 dropped` · round-trips: CA-A2-2/OV-A2-17 1/5 · CA-A2-3 1/5 · CA-A2-4 1/5 ·
  CA-A2-1 1/5 (surfaced, awaiting spec-author ruling).
- 2026-06-12 · A2 revision r2 — **CA-A2-1 RULED Option-2 (embedding cosine similarity) by the spec
  author through the authenticated in-session channel (this conversation, direct origin,
  2026-06-12).** Implemented: `_corroboration_counts` over the already-computed chunk embeddings,
  `_CORROBORATION_COSINE_THRESHOLD = 0.90`; `corroboration_count` = distinct `source_host`s within
  cosine ≥0.90 of the evaluated chunk (own source counted); restructured `acquire_for_topic` to
  embed-new-chunks → corroborate → persist. **Amended the AC-CD25 body in place** (`CODE_SPEC.md`)
  to record cosine-match + the 0.90 value + the tune-from-telemetry note, citing this conversation
  as authenticated origin (v1.9, no version bump). Tests: `_RecordingEmbed` now text-derived
  (identical text → cosine 1.0); added `test_safety_distinct_text_not_corroborated` +
  `test_corroboration_counts_threshold` (pins 0.90). 434/434 unit pass; ruff/format/mypy clean.
  **MERGE-CLASS CHANGE:** r2 amends a spec body (AC-CD25) → A2 is now a **ratification-class (ii)
  spec amendment**, not NORMAL. Authenticated origin = this conversation (the overseer, as
  merge-executor, confirms ratification through its own authenticated channel per relay discipline).
  · last-seen remote HEAD `442f00f` · set-diff `0 added / 0 dropped` (CA-A2-1 resolved-by-ruling;
  no new reviewer IDs) · round-trips: CA-A2-1 2/5 (ruled, resolved).
- 2026-06-12 · A2 revision r3 — **blind-SSRF fix (CA-A2-2r ≡ OV-A2-18, the residual blocker the
  watcher-lapse made me miss).** Root cause: r1/r2 `_fetch_body` used `follow_redirects=True`, so
  httpx **issued** the GET to a redirect target (internal/metadata) before the post-hoc host check —
  the request firing IS the SSRF. Fix: `follow_redirects=False`; redirects followed **manually**,
  re-validating **each hop's host** against the allowlist **+** an internal-IP guard (`_is_blocked_host`:
  RFC1918/loopback/link-local/reserved) **before** issuing that hop; bounded `_MAX_REDIRECTS=5`.
  Tests: off-allowlist + internal-IP redirect requests **never fire** (`not in fetched`);
  within-allowlist redirect followed + re-stamped; `_is_blocked_host` unit. 436/436 unit pass;
  ruff/format/mypy clean. **OV-A2-19 / CA-A2-5 (AC-CD25 amend-in-execution-PR + relay-ratification)
  STILL SURFACED — pending the spec-author's A/B/C ruling; NOT defaulted** (cosine code + the AC-CD25
  CODE_SPEC edit left exactly as ruled r2, awaiting the mechanics decision). · last-seen remote HEAD
  `57475e8` · set-diff `1 resolved [CA-A2-2r/OV-A2-18] / 0 dropped; CA-A2-5/OV-A2-19 held-surfaced`
  · round-trips: CA-A2-2r/OV-A2-18 2/5 (fixed) · CA-A2-5/OV-A2-19 1/5 (surfaced, awaiting ruling).
