# Plan-overseer — pre-registered governance positions: PR-C (Auto-publish gate + self-review + governance prose)

**Status: pre-registered (no draft yet)** — link 3 of 4. **Pre-registered (timestamped):** see wake-log.
**Pre-registered BEFORE any PR-C draft (§4.2 anti-anchoring).** Requirements: extraction §C (PR-C) + §B
(AM-10..14) + `REQUIRED_READING.md` §7. Base: `main` @ `921ac23` (PR-A v1.9 + PR-B v1.9, 29 decisions).

**Per-PR authentication:** PR-C needs its **own** §D surface authenticated in-channel BEFORE the planner
authors (relayed/blanket/inferred = pending). Ratifications must **trace to the PR-C triage** artifact.

**STANDING RULES carried (class-iv, ratified):**
- **Version stays v1.9** — one-version-per-cycle (OV-B1 ruling). A v1.11 bump at PR-C is a finding. Count
  bumps (29→31 for two mints) propagate to ALL count-bearing lines (OV-1/OV-2 lesson; SPEC+CODE_SPEC
  pairing lines + DECISIONS header+footer). Changelog = a new "Changes in v1.9 (third link — PR-C)" entry.

## Positions
- **POS-C0 — ratification-class gate (i+ii).** Two AC-D mints + AC-D7 body change + SPEC §6.5/§290. No
  auto-merge; own authenticated §D first; traces to PR-C triage. Withhold sign-off until canonical record clean.
- **POS-C1 — mints + count integrity.** self-review AC-D + auto-publish-gate AC-D — next-sequential
  (→ **AC-D30, AC-D31**); `DECISIONS:7` 29→31 + footer + numeral sweep. Verify bodies/IDs match ratification.
- **POS-C2 — AM-12 AC-D7 COMPLETE [C2+F1] — the AC-D23 mutual cross-ref CLOSES here (load-bearing).** AC-D7
  must (a) remove the "queue for admin review / approve" governance language (approve-gate removal), and
  (b) carry the **reciprocal back-reference to AC-D23** — the two tell **one** approve→auto-publish story
  (AC-D23's forward half landed in PR-A; PR-C completes the mutual ref). Verify both halves; a missing
  back-ref is the cross-PR mutual-reference failure I flagged at PR-A (POS-A4).
- **POS-C3 — content_self_review protocol AC-D (AM-10): the PR-B front-load echo CLOSES here.** PR-B
  authored the ops-count naming `content_self_review` "per the self-review AC-D, PR-C". PR-C must mint that
  AC-D; verify the op name + family (cross-family/review) + the §7.2 reference are consistent with what PR-B
  baked. Carries **NS-2** (one `content_self_review` op + 3 variants vs reuse `anchor_self_review`) + **NS-7**.
- **POS-C4 — §6.5 rewrite (AM-13) COMPLETE [C2+D1–D2+D3].** Human-gated "pill proposal" → autonomous phases
  (signal→gap→generate→auto-publish). Must coordinate with **PR-B's §6.8 generator** (no contradiction/dup)
  + the GapSignal §5 entity (PR-D). The ratified **sequencing guard** ("D1–D2 must not execute until PR-C
  merges", on `main` from PR-A) means §6.5 lands complete here. Verify whole, not partial-fold.
- **POS-C5 — §290 audit-log (AM-14) COMPLETE [C2+E2].** "pill proposals approved/rejected" → published /
  publish-with-warning / rolled-back events. Verify complete.
- **POS-C6 — NS-6 (auto-publish threshold + `compute_confidence` + per-type telemetry).** Couples DS1-a
  (PR-A T1/T2/T3 scores 1.0/0.6/0.3) — the confidence formula reads the authority scores. Verify the
  threshold default + formula are baked per ratification and cohere with the AC-D28 scores on `main`.
- **POS-C7 — amend-once ACROSS PRs.** PR-C must NOT amend PR-D anchors (oversight AC-CD, GapSignal §5,
  §4.11) except references; must NOT re-amend PR-A/PR-B anchors (AC-D21/22/23/28/29, AC-CD25,
  AC-D1/AC-CD8/AC-D12) except legitimate `Related:` backlinks. **AC-D7 IS in scope (AM-12).** Grep the diff.
- **POS-C8 — amend-once WITHIN PR-C.** AC-D7 complete [C2+F1]; §6.5 complete [C2+D1–D2+D3]; §290 complete
  [C2+E2]. Each whole, once — no silent partial-fold.
- **POS-C9 — version stays v1.9 + count 29→31 propagation (OV-1/OV-2 lesson).** Verify NO v1.11; all
  version-pairing lines stay v1.9; the decision-count 29→31 moves across DECISIONS header+footer + SPEC
  pairing line + CODE_SPEC pairing line consistently; changelog folded under v1.9.
- **POS-C10 — NS-7 degrade-not-gate re-confirm.** RULED (triple-authenticated in detail-plan); binds C1/C2;
  **re-confirm through PR-C's own authenticated channel** (OV-2 relay discipline — merged ruling not
  auto-actionable downstream). Verify the publish-with-warning / single-provider-verified posture baked.
- **POS-C11 — status-taxonomy honesty.** Built-state notes for unbuilt C1/C2 wiring (no false "built").
- **POS-C12 — merge gate.** 3 sign-offs @ one content SHA + three-layer green + override window (present
  spec-author go collapses to zero) + §8.3 ratification. I execute mechanically, re-confirm from ground truth.

## Cross-PR carry-forwards
- **AC-D7 ↔ AC-D23 mutual cross-ref COMPLETES at PR-C** (POS-C2) — the highest-priority cross-PR check.
- **content_self_review front-load echo (PR-B) CLOSES at PR-C** (POS-C3).
- **§6.5 ↔ GapSignal §5 + §6.8 generator** coordination (PR-D / PR-B) — verify coherence.
- **NS-6 ↔ DS1-a (AC-D28 scores)** confidence-formula coupling.
- **GenerationProvenance by-source_host → E2** (PR-D) — provenance consumed by the auto-publish confidence (C2).
