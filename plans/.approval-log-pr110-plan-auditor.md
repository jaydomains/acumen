# Plan-auditor — approval log / final-marker · PR-A (PR #110)

**Status: final — approved by plan-auditor**

- **Bound SHA (content):** `6f58fdc8324d8c0b380dd96426ed67fb1310bc62` (PR #110, `claude/pr-a-corpus-authority-foundation`).
- **Role:** plan-auditor (content-correctness). **Branch:** `claude/plan-auditor-amendment-prs-dipipy` (marker sits on my
  own review branch, off the canonical PR branch — role §8 marker placement).
- **Link:** 1 of 4 in the sequenced ratification cycle PR-A → PR-B → PR-C → PR-D. **Not** a terminal stand-down — I stay
  subscribed across the whole chain.

## What I verified (independently, against actual text — not the PR description)
- All **24 pre-registered positions** PA-1…PA-24 (recorded in `.audit-positions-pr110-plan-auditor.md` before this draft
  existed) verify as **Confirm** at `6f58fdc8`.
- The **9 ratifications + 3 coordination rulings** the PR consumes match the **authenticated in-session ruling** I hold
  verbatim, 1:1 (the absent `…-delightful-church.md` triage file notwithstanding — the in-channel ruling is the reference,
  PA-9 ✓).
- AC-D28 mint (+multi-slice [A1+E2] E2 override layer) · AC-CD25 mint (+CorpusChunk/DS2-b, no new Operation enum) ·
  allowlist hosts/scores exact · DS1-c guard · AC-D21/22/23 complete cross-stage folds · comprehensive Drive→corpus sweep
  with the NS-1 relocate-not-delete scope boundary surfaced · AC-CD1 HTML+PDF/pypdf · cron-count nine consistent across all
  surfaces · NS-5 named non-phase workstream · no two-nines conflation (ops axis left at seven for PR-B).

## Finding history
- **A-1 (Refine)** — SPEC §8.9 "total nine" enumerated `competence_estimate` recompute (not a beat task) and omitted
  `cost.budget_sweep`, diverging from CODE_SPEC §8 / actual `beat_schedule.py`. Raised at `22d24e55`; converged with Gitar.
  **RESOLVED at `c627d26`** (folded into `6f58fdc8`): §8.9 reconciled to the single canonical nine-member set, identical to
  CODE_SPEC §8; `competence_estimate` correctly reclassified event-driven (post-attempt loop, AC-D9). Count preserved (nine).
  Verified against actual text + Gitar concurs resolved. Thread A-1: 2/5, closed.
- **PA-19 durability** (non-blocking note) — folded at `6f58fdc8`: the "D1–D2 must not execute until PR-C merges" guard now
  lives durably in ROADMAP + CHECKLIST, not only the PR body.

## Boundaries (role §3.7 / §8)
I do **not** flip draft→ready and do **not** execute the merge. This marker is one of **three** independent sign-offs;
the **overseer** executes the merge once all three land at one SHA + the three-layer green gate + the spec-author override
window (collapsed to zero only by a present spec author who ratifies). The three-layer green gate (CI + Gitar + GitHub
mergeable) is the overseer's execution-time check; Gitar is ✅ approved at this SHA.

**Marker staleness:** bound to the **content** at `6f58fdc8`. A content-invariant commit (e.g. the planner's wake-log-only
final-marker) does **not** re-stale it; any content-changing commit does → I re-verify and refresh at the new SHA.
