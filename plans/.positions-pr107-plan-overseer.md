# Pre-registered governance positions — plan-overseer

**Loop:** AI autonomous content-generation workstream — the planner's *autonomy-reframing*
plan PR that **supersedes PR #106's human-gate assumptions** (Stage D
"incremental-bootstrap-on-approve" → autonomous architecture).

**Role:** plan-overseer (workflow-governance / infrastructure correctness lane + merge-executor)
per `.claude/roles/plan-overseer.md`, bound by `plans/REQUIRED_READING.md`.

**PR number:** provisional **#107** (next sequential after merged #106) — *to be reconciled
against ground truth (`git ls-remote` + open-PR poll) when the planner's draft is identified;
files renamed if the actual number differs.*

**Pre-registered — timestamp 2026-06-08 (anti-anchoring, role file §4.2 / §3.2).** Recorded
**before** reading the planner's draft plan text. The draft did not yet exist at pre-registration
time (no open PR; canonical `main` @ `9bde51f`), so these positions are derived purely from the
requirements, the framework's own discipline, the merged precedents (PR #104 framework install,
#105 workstream plan, #106 detail-plan), and the role/REQUIRED_READING infrastructure — not from
the draft.

Each position below is an **expectation a governance-correct plan must satisfy**. On reading the
draft, a satisfied position becomes a **Confirm**; a failed one becomes an `OV-` finding (role
file §6). These are not findings yet.

---

## Standing governance baseline — the §8 topology invariant (must not drift)

The convergence/merge topology is **baked** into the six role files and is **not project-tunable**.
I verify the plan against the invariant, not by byte-diff:

> three independent sign-offs · one SHA (content-bound) · green CI (= Acumen's three-layer green
> gate) · overseer-executes-mechanically · execution-vs-authority · override-window (full 24h for
> spec-author **absence**; a **present** spec author who ratifies collapses it to zero) · named
> external trigger (the planner re-triggers execution) · ratification-classes (i)–(iv) ·
> spec-author authority · planner/auditor never-merge · convergence binds to the **content-SHA**
> (a content-invariant marker does not re-stale).

Any plan content that re-tunes this topology is itself a **§8.3 class (iv) framework change** —
surfaced + ratified, never baked.

---

## P-A · Topology faithfulness — autonomy architecture preserved as-ratified

The 8 architectural ratifications (authenticated in the prior chat that originated this plan) must
be reproduced **faithfully**. Expectation:
- The plan records all 8 ratified architectural decisions accurately, with no silent alteration,
  omission, or partial-fold under cover of "reframing."
- No **un-ratified** architectural deviation is introduced as if it were part of the ratified set.
- The autonomy architecture's shape (auto-publish gate, provenance chain, authority scoring, etc.)
  matches what was ratified, not a planner re-interpretation.

## P-B · Transportability — role files unchanged; project params via REQUIRED_READING.md

The reframing is a **plan/workstream** change. Expectation:
- Scope is `plans/**` only (the new plan doc + this/the planner's wake-log). The project-agnostic
  role prompts under `.claude/roles/*` are **not** edited and carry no project specifics.
- Any genuinely new *project parameter* the autonomy architecture needs belongs in
  `REQUIRED_READING.md` (and editing that file is itself **class (iv)** — surfaced/ratified, not a
  free edit). It is not baked into a role file.
- The role files travel unchanged between repositories after this PR.

## P-C · Baked-vs-externalized boundary correctness

Two distinct gate systems must not be conflated:
- **The plan-review governance topology** (§8) — baked, not tunable.
- **The product's own autonomy gates** (auto-publish gate, provenance, authority scoring) — these
  are *product architecture*, governed by AC-D / AC-CD anchors + SPEC, distinct from the
  governance topology.
Expectation: the plan keeps these separate; it does not re-tune the baked governance topology, and
it does not present a product gate as a governance-topology change (or vice-versa). Only the
externalized **postures** (e.g. counterpart-silence merge-eligibility) are project policy.

## P-D · Recursive consistency — does the new plan match the framework's own discipline?

The plan must obey the discipline it is produced under:
- **Surfaces, does not bake** every ratification-class item (role file §7).
- `plans/**`-only scope; no spec/anchor/code edits enacted by this PR.
- Wake-log + **set-diff gate** discipline; status taxonomy backed by **evidence** (a test path /
  command / artifact), never prose (REQUIRED_READING §2 / SESSION_START).
- The plan PR is **NORMAL merge class** *iff* it bakes no ratification-class change — every
  embedded item is surfaced for **downstream per-item** ratification, **not** folded into this
  PR's merge gate.

## P-E · PR #106 supersession correctness — cited as merged record, not amended

PR #106 is merged history (head `claude/dreamy-mccarthy-dAr4h` @ `291ef22`, merged into `main`).
Expectation:
- Supersession is achieved by **new plan-doc text that explicitly cites #106 as superseded** —
  **not** by editing/reopening/rewriting the merged #106 detail doc
  (`plans/2026-06-07-ai-pill-generation-gap-detection-detail.md`) in place. A merged plan PR is an
  **immutable historical record**; it is superseded *forward*, never amended *backward*.
- Likewise PR #105's merged workstream plan is cited, not rewritten.
- The supersession itself (replacing #106's human-gate Stage D) is a **scope decision** — see P-F.

## P-F · Ratification-class classification correctness (the merge-executor's load-bearing duty)

This PR's content is expected to carry (per the dispatch's ratification-class read):
- **class (iii) scope decision** — the major architectural pivot / autonomy supersession of #106;
- likely **class (ii) spec changes** — SPEC §6.5 / §5 / `CODE_SPEC` AC-CD amendments;
- likely **class (i) new AC-D anchors** — auto-publish gate, provenance chain, authority scoring.

Expectation — **the plan PR's own merge is NORMAL class ONLY IF** all of these are *surfaced for
downstream per-item authenticated ratification* and **not baked into the plan PR's merge gate**
(merging the plan must execute nothing, amend no spec, mint no anchor, ratify no item — the PR #106
"two gates — do not conflate" precedent). If merging this PR would **enact/bake** any
ratification-class change, it is a **§8.3 class** change: I do **not** auto-merge it; it requires
**explicit, authenticated, item-specific spec-author ratification first**, then I execute.
**I am the party that classifies — misclassifying a ratification-class change as normal (and
auto-merging it) is the load-bearing failure of this role.**

## P-G · The 8 ratifications recorded as authenticated, not relay-pending

The 8 architectural ratifications are stated to be already authenticated (prior chat = origin).
Expectation:
- The planner records them citing **this conversation / the authenticated in-session channel** as
  origin — **not** as a relayed "the spec author ratified X" under the shared role-session byline,
  which is **pending, not actionable** (role file §8.3). A relay carries the same byline a genuine
  ruling would; it cannot by itself authorize a merge.
- The citation is **explicit about structural implications** and **current** (not superseded by a
  later ruling on the same question).
- **The 8 do not blanket-authorize NEW items.** Any new ratification-class item the plan surfaces
  beyond the 8 (a new AC-D anchor, a specific spec amendment) requires its **own** per-item
  authenticated ratification before its downstream PR proceeds.

## P-H · No silent partial-fold; supersession surfaced as the spec author's call

Expectation:
- The plan does not silently ship part of a ratified scope as if it were the whole (a silent
  partial-fold recreates the inconsistency the surfacing discipline exists to prevent).
- The supersession of #106's human approval gate is surfaced as a **class (iii) scope decision**
  for the spec author — recorded with its authenticated origin, current, and explicit about what
  replaces the human gate (the auto-publish gate + provenance + authority architecture).

---

## Execution-time parameters I will enforce (recorded for the gate re-confirm)

- **Merge method:** squash (`gh pr merge --squash --delete-branch`) — overseer-only.
- **Override window:** 24h after the third converging sign-off at the convergence SHA; a **present**
  spec author who ratifies/voices-no-objection collapses it to zero. Not self-firing — the planner
  re-triggers me at window-expiry.
- **Green CI:** Acumen's **three-layer green gate** — CI (`ci.yml` checks + migration-chain;
  `frontend.yml` checks + docker-build + e2e) **+ Gitar review + GitHub mergeable**, simultaneously
  green at the convergence SHA.
- **Convergence:** three independent sign-offs (planner + plan-auditor + plan-overseer) at one
  content-SHA. Reviewer markers on their own branches; the planner's content-invariant marker on
  the canonical branch.
- **Ratification gate:** for any §8.3 class item baked by this PR (should be none if P-F holds),
  no auto-merge without explicit authenticated current spec-author ratification.
