# Wake-log — Slice B2 executor (corpus-grounded generation + per-draft provenance chain)

Mechanical append-only action log for the sequenced code-execution audit loop on the
B2 slice PR (autonomous-content-generation workstream, slices A1→F1). Each line:
`<UTC date> · <action> · <last-seen remote HEAD> · <set-diff> · <round-trips>`.
B1 merged @ main `f346cc2` (PR #118). B2 is **where the A→B dependency binds** (OV-20): it
needs A2+A3 merged (CorpusChunk + retrieve_corpus_for_topic) + B1 merged (pill_generation op)
— all merged. Branched off updated main.

- 2026-06-12 · B2 started — read the detail §5 + the ratified AC-D29 (provenance) + SPEC §6.8
  (generator). Like A3/B1, the spec is already ratified (AC-D29 by PR-B; the GenerationProvenance
  migration + generate_grounded_drafts are "B2 execution deliverables") → B2 is code-only (NORMAL).
  **SURFACED to the spec author — a genuine load-bearing spec ambiguity (per §7 / the A3 verify-
  against-ratified-spec lesson; NOT baked):** AC-D29 + SPEC:358 specify a **per-assertion** provenance
  chain ("one row per (assertion, grounding-chunk)"; `claim_ref` = the per-assertion identifier), but
  SPEC §6.8's v1.1.0 output contract is **`grounding_refs` = "the corpus source_doc_refs THE DRAFT
  cited"** — a per-**draft** flat list with no per-assertion decomposition. The two ratified surfaces
  are in tension: the prompt output (per-draft grounding_refs) carries no per-assertion structure, yet
  the provenance table (AC-D29) is per-assertion. This determines (i) the `pill_generation` v1.1.0
  output schema (flat `grounding_refs: [str]` per draft vs structured `[{claim, refs}]` per assertion),
  (ii) the `GenerationProvenance.claim_ref` shape, and (iii) the row cardinality — all load-bearing.
  **Held pending an authenticated ruling; B2's provenance model + prompt-bump + writer are gated on it
  (the unambiguous parts — the retrieval wiring — are not, but the core schema is).** · last-seen remote
  HEAD `f346cc2` · set-diff `n/a (pre-implementation surface)` · round-trips: none.
- 2026-06-12 · B2 RULED + implemented — **provenance-granularity ruled Option-1 (per-assertion
  structured output) by the spec author (authenticated direct origin, this conversation).** I made the
  directed **§6.8 spec-amendment call: §6.8 is PERMISSIVE → Option-1 lands directly in B2, no
  amendment** — because (a) §6.8 opens "Per AC-D29" (subordinate to AC-D29's per-assertion mandate) and
  (b) its "grounding_refs (the source_doc_refs the draft cited)" is descriptive of WHAT grounding_refs
  carries, not a normative flat-string-only JSON shape; a structured `[{claim, source_doc_refs}]` still
  carries "the source_doc_refs the draft cited" per-claim. So **NORMAL class — SPEC/CODE_SPEC/DECISIONS
  unchanged** (verified empty diff). Implemented: `pill_generation.py` v1.0.0→**v1.1.0** (G7b —
  `{corpus_context}` slot + ground-and-cite + structured per-assertion `grounding_refs`); `app/models.py`
  **`GenerationProvenance`** (one row per (assertion, grounding-chunk); columns per AC-D29: draft_ref,
  claim_ref, corpus_chunk_id FK, source_host, authority_tier/score, batch_id nullable; indexed
  source_host/corpus_chunk_id/draft_ref for E2) + migration `0010_b2_generation_provenance` (up/down
  clean); `app/domain/generation.py` (NEW) **`generate_grounded_drafts`** (retrieve_corpus_for_topic →
  render authority-tagged context → pill_generation → write per-(assertion,chunk) provenance; empty-corpus
  general-knowledge fallback → no rows); `retrieve_corpus_for_topic` now returns `corpus_chunk_id`; stub
  emits structured grounding echoing injected corpus refs; tests `test_grounded_generation.py` + updated
  `test_ai_pill_generation` (v1.1.0 grounding) + `test_p1_schema` (36 tables, trigger count 36). B2/B3
  split held (B2 = grounded-gen fn + provenance model/writer; B3 = N-row persistence + cost-share).
  459/459 unit pass; ruff/mypy/structure/unpinned clean; migration `--sql` round-trip valid. Carries the
  B1 handover. · last-seen remote HEAD `f346cc2` · set-diff `0 dropped / 0 added (baseline — no reviewer
  findings yet)` · round-trips: none open.
