# Wake-log — PR #102 execution auditor

Role: end-of-workstream adversarial execution audit of the post-audit
pre-deploy fix workstream (plan `621a549` / #94). Slices 1–3 merged to
`main`; Slice 4 (V2 anchor-ID strip) open as PR #102, branch
`claude/great-fermi-aopl2-s4`, HEAD `5080e91`.

## Reading list completed
SESSION_START.md (spec-drift floor :80–85), the merged plan
`plans/2026-06-06-post-audit-pre-deploy-fix-workstream.md`, the prod-readiness
synthesis + auditor1/2, FE-3/4/5/9 specs, amendments #96/#97/#101, handovers
PR-098/099/100/102, and the executed code for all four slices.

## Verification verdict per slice (against the binding plan)
- **Slice 1 (#98, C1)** — faithful. `app_frontend_url` distinct from
  `app_public_url`; two append-to-`errors` boot clauses; path-segment links;
  integration + startup tests. Matches plan §Slice 1 / D2a / G1. No finding.
- **Slice 2 (#99, V4+V5)** — faithful. V4 predicate is exactly
  `q.state.data === undefined`, exported as `resultErrorIsInitial`
  (`result/page.tsx`); V5 escapes off `isError` to a distinct error
  affordance (`GradingOverlay.tsx`), Gitar-refined to tolerate transient
  poll blips + clean retry. Matches D4/F3/F4. "Back to dashboard"→`/` is
  correct (testee-scoped surface). No finding.
- **Slice 3 (#100, V1)** — faithful. `dashboardPathFor` exported
  (`lib/auth/guards.tsx`); `DashboardLink` client component for the server
  `not-found.tsx` + `403`; `error.tsx` uses `useAuth()` directly;
  `result/error.tsx` correctly left at `/`. Matches G5/Slice 3. No finding.
- **Slice 4 (#102, V2)** — 13 rendered `AC-Dxx` sites stripped; all 13 carry
  genuine, non-vacuous regression coverage (anchor-leak-guard.test.tsx 6 leaf
  surfaces incl. IntegrityBadge whose `<details>` children stay in the jsdom
  DOM, + element-scoped guards in GradingOverlay/adaptive-loop-card/
  admin-grade-review-queue/pill-detail tests). See findings F1–F3.

## Findings (to post as PR #102 review comments — one per comment)

### F1 — #101 spec-amendment provenance (spec drift; non-blocking, pending confirm)
git author of `40b81cd` (#101) is `Claude <noreply@anthropic.com>` (session
`01K13tng…`); #96 (`83e7b43`) and #97 (`9988aa0`) are authored by `J`. The
PR-102 handover line 28 calls #101 "the user-authored standalone doc-only
amendment," and #101's banner says "per the spec-author ruling." SESSION_START
.md:80–85 requires the *user* to author spec-clarification PRs. Ask: confirm
the spec author explicitly authorized a separate Claude session to type #101,
and reconcile the handover's "user-authored" wording with git ground truth.
Triages to the merged-#101 venue; posted on #102 per PR-scope rule.

### F2 — 7→13 scope-expansion authorization + Option A/B label flip (out-of-plan scope / worth-knowing; non-blocking)
Plan §4 Slice 4 enumerates 7 sites; executed set is 13. The extra 6
(GradingOverlay×2 `GradingOverlay.tsx:53/57`, IntegrityBadge×2 `:28/52`,
`admin/safety-toggle.tsx`, `(testee)/pills/[pillId]/page.tsx` subtitle) are
genuine testee-facing leaks — closing them is strictly better. But commit
`19c1183` calls it "Overseer authorized … (Option B)" while commit `5080e91`
+ handover call the same 13-site outcome "Option A," and the authorization
itself is not in any repo artifact. Ask: point to the overseer-authorization
comment and reconcile the A/B labels.

### F3 — FE-4 (#101) re-quotes "OpenAI gpt-4o-mini" but code renders "OpenAI" (out-of-plan scope; non-blocking)
`fe-specs/FE-4-runner.md:136` (post-#101) quotes phase (iii) verbatim as
"Cross-family review pass · OpenAI **gpt-4o-mini** · 60s ceiling";
`GradingOverlay.tsx:53` renders "Cross-family review pass · OpenAI · 60s
ceiling" (model name absent). Pre-existing (Slice 4 only stripped
" per AC-D19"), so out of V2 scope — but #101 re-affirmed the verbatim line
without reconciling. Flag for the post-deploy UI-hygiene carry-forward so
FE-4:136 isn't read as a satisfied verbatim contract.

## State
- Verification: COMPLETE (read-only, grounded against `5080e91` + `main`).
- Review comments: PENDING GitHub MCP OAuth (server requires authentication;
  flow started, awaiting user). Will post F1–F3 one-per-comment + submit +
  subscribe + start 90s background ls-remote poll once authed.
- last-action SHA: (this commit)
