# Wake-log — Slice C2 executor (autonomous auto-publish gate — confidence + publish)

Mechanical append-only action log for the sequenced code-execution audit loop on the
C2 slice PR (autonomous-content-generation workstream, slices A1→F1). Each line:
`<UTC date> · <action> · <last-seen remote HEAD> · <set-diff> · <round-trips>`.
C1 merged @ main `a59a40c` (PR #121, slice 7/14, safety floor live). C2 is the **autonomous
auto-publish gate** (AC-D31 / §6.5) — confidence score → publish (live or with-warning, nothing
held). Branched off `a59a40c`.

- 2026-06-12 · C2 started — **verify-before-write against the MERGED v1.9 spec on main (the A3
  lesson)**. Read: detail §8 (Slice 8/C2), ratified **AC-D31** (`DECISIONS:792` — single global threshold
  default 0.70, publish-with-warning, nothing held; `compute_confidence` hard-fail floor + authority-
  weighted DS1-a + DS2-b corroboration; `PublishRecord` DS8-a; bootstrap-on-publish is F1), **AC-D7**
  (approve-gate removal, authored complete on main), **§6.5/§290** (ratified). **CONFIRMED NORMAL class**
  (no spec edit) **+ a migration** (PublishRecord + SystemSettings.threshold). Both reviewers pre-registered
  + concur: auditor P-C2-1..10; overseer OV-C2-1..9.
  **🔶 SURFACED a genuine ratified-vs-detail conflict (per §7 / the A3 lesson — NOT silently reconciled):**
  ratified AC-D31/AC-D7 say `approve_pill_proposal` is **removed** + the **refiner routes through the gate**
  ("one publication path"), but the detail §8.6 + **OV-C2-8** + auditor scope C2 as **generated-only**, keep
  `approve_pill_proposal`; the detail plan keeps "Approve path unchanged for the refiner" across **all 14
  slices** (line 2503) → the refiner-reroute is assigned to **no slice**. Surfaced to the spec author (plain
  text, 3 options); built the **3-party-aligned generated-only core** (option 1) pending the ruling — the
  core is unambiguous + needed under any ruling. Implemented: `app/models.py` `PublishRecord` (DS8-a:
  pill_id/batch_id/confidence/3 verdicts/low_confidence/per-type telemetry, indexed) +
  `SystemSettings.pill_publish_confidence_threshold` (float, server_default 0.70); migration
  `0011_c2_publish_record` (CREATE TABLE + 4 indices + trigger + ADD COLUMN; up/down clean); NEW
  `app/domain/publish.py` `compute_confidence` (hard-fail floor on grounding/provenance; authority-weighted
  mean + capped corroboration) + `auto_publish_draft` (self_review → score → create_pill honouring
  re-adjudicated safety; ≥threshold live / <threshold|NS-7-degrade flagged; nothing held; audit
  publish/publish_flagged; task done; PublishRecord). Tests `test_autopublish_gate.py` (7 — above/below
  threshold, re-adjudicated safety honoured, single global threshold + tenant override, per-type telemetry,
  NS-7 degrade, compute_confidence floors) + `test_p1_schema` (37 tables/triggers, migration 0011 round-trip,
  0.70 default). 486/486 unit pass; ruff/format/mypy(71)/structure/unpinned clean; migration `--sql`
  round-trip valid; **spec-anchor diff EMPTY (NORMAL class)**. Carries the **C1 handover**. · last-seen
  remote HEAD `a59a40c` · set-diff `0 dropped / 0 added (baseline — no reviewer findings yet)` · round-trips: none.
- 2026-06-12 · C2 round-1 — PR #122 opened, subscribed, watcher `bdb6jdkdy`. **Gitar = Changes-requested
  (1 ⚠️ Bug)**: `auto_publish_draft` never consulted `review.safety.verdict` — in a **multi-provider** config
  (single_provider False) a draft that FAILED the cross-model safety pass could publish **live + unflagged**
  (high grounding/provenance + authority → high confidence; ns7_degrade needs single_provider). Real gap for
  "the non-negotiable safety floor". **Folded (consistent with the safety-floor + retroactive-oversight
  design; ruling 2 "nothing held" preserved — flags, doesn't hold; executor discretion):** `safety_failed =
  review.safety.verdict == "fail"` → `low_confidence = confidence < threshold or ns7_degrade or
  safety_failed`. A safety-fail now publishes-with-warning (reaches Stage-E flagged), still LIVE. +1
  regression test (`test_safety_fail_flags_even_multi_provider_high_confidence` — the untested path Gitar
  flagged). 487/487 unit pass; ruff/format/mypy clean; spec-anchor diff still EMPTY (NORMAL class). · last-seen
  remote HEAD `a59a40c` (main) · set-diff `1 Gitar finding folded / 0 reviewer-blocking open` · round-trips: Gitar round 1.
