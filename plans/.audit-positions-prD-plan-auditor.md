# Plan-auditor — pre-registered positions for PR-D (Signal spine + oversight) — the FINAL link

**Status: pre-registered — PR-D draft not yet authored (anti-anchoring, role §4.2)**
**Pre-registered:** 2026-06-11, after PR-C (#112) merged to `main` `4122c55` — before any PR-D draft/ratification.
**Link 4 of 4** (PR-A ✅#110 · PR-B ✅#111 · PR-C ✅#112 · **PR-D**). The cycle completes on PR-D's merge.
**Yardstick:** `plans/2026-06-08-amendment-cycle-extraction.md` §C(PR-D)/§B/§D · live `SPEC.md`/`DECISIONS.md`/`CODE_SPEC.md`
v1.9 on `main` · REQUIRED_READING.md §7 · the **v1.9 one-version-per-cycle standing rule**.

**Ground truth at pre-registration (`git show origin/main`, verified):**
- Next AC-CD mint = **AC-CD26** (oversight, AM-17); **no new AC-D** (count stays **31**, AC-D1–AC-D31).
- §5 data model (SPEC:258+) carries Pill / Reference Corpus (`CorpusChunk`) / GenerationProvenance / Audit Log — **GapSignal**
  (AM-15) lands here as a new §5 entity. §4.11 (SPEC:240) "Admin grade override and loop oversight" = AM-18's target.
- **AC-D28 (PR-A, on main, DECISIONS:742):** the per-source override/demotion DB layer keyed by `source_host` — "the override
  layer's **schema lands with the oversight rollback contract in PR-D** (DS13-a)." PR-D's AC-CD26 must land that schema,
  consistent with this recorded design (code-tier + DB-override = join).
- **PR-C §6.5 (on main):** signals persist in "the **GapSignal** entity (§5 — landed in the signal-spine link, PR-D)"; the three
  signal types = generated-question pill-tags, no-match discovery searches, **admin scope-clarification** (signal-3). §8.9's
  `gap_detection.sweep` + `catalogue_health.check` (PR-A) scan GapSignal.

**Per-link discipline:** PR-D's ratifications need their **own** authenticated in-session ratification (PR-A/B/C rulings do not
cover PR-D). The C-7 ruling (NS-4 defined in PR-C §6.5; **PR-D implements without re-amending**) carries forward as PD-7.

---

## Mints + the E2 rollback (the convergence of the whole cycle's data spine)

- **PD-1 (AM-17 oversight AC-CD mint → AC-CD26, complete [E1 read + E2 rollback]).** Mint the oversight dashboard API: **one
  AC-CD spans both** the E1 read contract (recent publishes + confidence + provenance + authority breakdown) and the E2 rollback
  contract (ruling 5 matrix: **pill / question / batch / source**). CODE_SPEC §18 + the index/footer (AC-CD25→26). Verify
  next-sequential, body covers read+rollback **complete** (no partial-fold of E1 vs E2).
- **PD-2 (E2 rollback consumes the PR-B/PR-C stores — PB-9 + PC-17 carry-forwards CONVERGE HERE).** The E2 per-batch/per-source
  rollback must query **`PublishRecord` by `batch_id`** (PR-C, on main) + **`GenerationProvenance` by `source_host`** (PR-B, on
  main) — the two stores were shaped *specifically* for this (relational, indexed). Verify AC-CD26's rollback query shapes
  actually MATCH those indices (per-batch via PublishRecord.batch_id; per-source via GenerationProvenance.source_host). A
  mismatch is the cross-PR-drift finding I've tracked since PR-B/PR-C.
- **PD-3 (DS13-a E2 source-override schema closes — AC-D28 binding).** AC-CD26 must land the **per-source override/demotion DB
  layer keyed by `source_host`** that AC-D28 (PR-A) recorded the *design* for: code-registry tier (seed) + DB override (demotion)
  = the join at retrieval/ranking. Verify the schema matches AC-D28's recorded design; this **closes the DS13-a multi-slice
  [A1+E2]** opened in PR-A.

## GapSignal §5 (AM-15) — PA-14 carry-forward

- **PD-4 (AM-15 GapSignal §5 entity, complete [D1–D2 + D3]).** New §5 entity: **one polymorphic table** (DS9-a lean) —
  `signal_type` / `dedup_key` / `occurrence_count` + the `consumed_at`/status field D3 needs (authored forward-ready at D1–D2 in
  one migration). Must cover the **three signal types** PR-C's §6.5 forward-referenced (no drift); coherent with §8.9's
  `gap_detection.sweep`/`catalogue_health.check` (PR-A) that scan it. **PA-14: verify GapSignal §5 ↔ §6.5 (PR-C) ↔ §8.9 (PR-A)
  describe ONE coherent signal model** — this is the last cross-PR coherence check.
- **PD-5 (signal-3 = `scope_clarification` signal type).** Define the `scope_clarification` signal type now (the admin
  scope-clarification signal); the admin **feature** is deferred (parallel-to-G8). Verify the type is defined in GapSignal §5,
  the feature correctly deferred (not built/over-scoped).

## §4.11 + NS-4

- **PD-6 (AM-18 §4.11 admin-oversight prose, complete [E1+E2], OV-59).** Amend §4.11 to add **retroactive** admin oversight:
  read (E1 dashboard) + rollback (E2). Oversight is retroactive (no pre-publish gate — consistent with AC-D31/AC-D7). Complete
  [E1+E2], no partial-fold.
- **PD-7 (AM-16 / NS-4 — IMPLEMENT, do NOT re-amend; C-7 carry-forward).** NS-4 (catalogue-health: thin-band + uncovered-subject)
  is **DEFINED in PR-C §6.5** (merged). Per the spec-author's C-7 ruling, **PR-D implements against it WITHOUT re-amending.**
  Flag any PR-D re-touch/re-definition of the §6.5 NS-4 prose as out-of-scope **double-touch**.

## Discipline / structural

- **PD-8 (per-link authentication).** PR-D's ratifications (AC-CD26 exact body/ID, GapSignal DS9-a shape, signal-3, §4.11,
  the E2 rollback matrix, FE scope) rest on PR-D's **own** authenticated in-session ratification; body cites **this conversation**.
- **PD-9 (version-pairing — v1.9 standing rule; OV-1/OV-2 + PB-19 lesson).** PR-D **stays v1.9**. Sweep EVERY header+footer+
  pairing line (SPEC, CODE_SPEC; DECISIONS if touched). AC-D count stays **31** (no AC-D mint); **AC-CD index/count 25→26**
  (CODE_SPEC); PR-D folds into the v1.9 batch ("fourth link — PR-D"). **No `v1.10`/`v1.11` straggler**; no stale AC-CD25-as-last.
- **PD-10 (amend-once / no re-touch of merged anchors).** PR-D must NOT re-amend AC-D7/28/29/30/31, AC-CD25, §6.5 (incl. NS-4),
  §290, the ops/cron counts (all merged PR-A/B/C). Legitimately touches: SPEC §5 (GapSignal) + §4.11, CODE_SPEC (AC-CD26),
  fe-specs. Verify no out-of-scope re-touch (grep the diff).
- **PD-11 (fe-specs FE-N dashboard scope).** The admin oversight-dashboard FE deliverable (`fe-specs/FE-<n>`), parallel-to-G8,
  couples NS-5 (FE-phase home, established PR-A). Verify it's added coherently or scoped/deferred (not fabricated/over-built);
  the FE surfaces the E1 read + E2 rollback contract.
- **PD-12 (draft status).** PR-D is/should be a draft; I never flip draft→ready and never merge.
- **PD-13 (CYCLE COMPLETION — the terminal check).** PR-D is the **final link**. On its merge the whole sequenced ratification
  cycle (PR-A→D) completes → my **only terminal stand-down**. Before marking PR-D, confirm the cycle's cross-links all resolved
  on main: AC-D23↔AC-D7 one-story (PR-A/C ✓), two-nines (cron PR-A + ops PR-B ✓), GapSignal↔§6.5↔§8.9 (PR-C/D, PD-4),
  provenance/PublishRecord→E2 (PR-B/C/D, PD-2), DS13-a [A1+E2] (PR-A/D, PD-3).

---

*Findings get stable IDs `D-1…`. This is the last pre-registration. Carry-forwards CONVERGING here: **PA-14→PD-4**
(GapSignal↔§6.5↔§8.9), **PB-9+PC-17→PD-2** (provenance+PublishRecord→E2 rollback), **DS13-a→PD-3** (AC-D28 override schema),
**C-7→PD-7** (NS-4 implement-not-amend). On PR-D merge: verify via ls-remote, terminal stand-down, end the role cleanly.*
