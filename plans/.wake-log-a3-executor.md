# Wake-log — Slice A3 executor (corpus refresh cron + retrieval helper)

Mechanical append-only action log for the sequenced code-execution audit loop on the
A3 slice PR (autonomous-content-generation workstream, slices A1→F1). Each line:
`<UTC date> · <action> · <last-seen remote HEAD> · <set-diff> · <round-trips>`.
A2 merged @ main `2588f39` (PR #115) — A3 branches off that updated main, reading the A2
`CorpusChunk` store + calling A2's `acquire_for_topic` (AC-CD25/OV-11: A1+A2 merged required).

- 2026-06-12 · A3 draft authored — built against the **merged AC-CD7/§8.9 nine-cron spec** + AC-CD25
  retrieval contract (ruling 6 hybrid refresh). Verified the cron-count ratified state on main:
  AC-CD7 = nine canonical (`corpus.refresh` replaces `drive_rag.ingest` net-0; D4 adds two); NS-1
  (removing `drive_rag.ingest`) is a **separate** execution slice, so A3 only **adds** `corpus.refresh`
  (built 7→8, §3.6 leaves `drive_rag.ingest` untouched). Files:
  `app/domain/corpus_builder.py` (`retrieve_corpus_for_topic` — cosine_top_k over CorpusChunk +
  authority tier/score + `min_tier` + fail-soft + query-embed `corpus.retrieve` cost-audit;
  `refresh_corpus_for_topic` wrapper; `refresh_corpus_all` over active non-retired pills, DS3-a),
  `app/worker.py` (`corpus_refresh_task`), `app/beat_schedule.py` (`corpus.refresh` weekly Mon 08:00 +
  docstring/table sweep), `app/ai/cost.py` (`corpus.retrieve` folded into `_rag_retrieve_spend`,
  AC-CD8), `tests/unit/test_corpus_retrieval.py` (7 zero-network tests), `tests/unit/test_p11_celery_
  wrappers.py` (+corpus wrapper), `tests/integration/test_p11_beat_schedule.py` (7→8 + weekly),
  CHECKLIST A3 row missing→built + P11 cron row 7→8. **Cron-count mirror sweep (§3.4):** code/test
  count-words made count-agnostic ("the §8.9 crons"); spec docs (AC-CD7/§8.9/ROADMAP) already at nine
  from PR-A — amend-once, untouched. Reuse-not-reinvent: `cosine_top_k` (A3 = second consumer, OV-15 —
  strengthens the NS-1 relocate-not-delete requirement), `acquire_for_topic` (idempotent refresh), no
  new `Operation`. Also carries the **A2 handover**. Three-layer local green: ruff/format/mypy clean,
  structure-gate + unpinned-deps OK, 453/453 (unit + beat integration) pass.
  · last-seen remote HEAD `2588f39` (origin/main, post-A2-merge) · set-diff `0 dropped / 0 added
  (baseline — no reviewer findings yet)` · round-trips: none open.
