# Wake-log — Slice D1-D2 executor (the three §6.5 coverage-gap signal stores + dedup)

Mechanical append-only action log for the sequenced code-execution audit loop on the
D1-D2 slice PR (autonomous-content-generation workstream, slices A1→F1). Each line:
`<UTC date> · <action> · <last-seen remote HEAD> · <set-diff> · <round-trips>`.
C2 merged @ main `7aa5119` (PR #122, slice 8/14, Stage C complete). D1-D2 is the **signal
spine** the D3 gap-detection sweep consumes — the SPEC §5 `GapSignal` polymorphic store +
discovery-miss/question-tag capture + the signal-layer dedup arm. Branched off `7aa5119`.

- 2026-06-12 · D1-D2 started — **verify-before-write against the MERGED v1.9 spec on main (the spec
  author's standing note — anchors govern, NOT the detail-plan body)**. Read: detail §9 (Slice 9/D1-D2),
  ratified **SPEC §5:302 `GapSignal`** (single polymorphic store — `signal_type` discriminator
  discovery_miss/question_tag/scope_clarification, `dedup_key`, `detail`, `source_ref`, `occurrence_count`,
  **`consumed_at`** [D3 sets it], `occurred_at`; deduped `(signal_type, dedup_key)`) + **SPEC §6.5:356**
  (signal capture) — PR-D (the final link). **CONFIRMED ratified — NO drift this time** (the ratified
  SPEC §5 schema matches the detail's one-polymorphic-table lean, incl. `consumed_at` authored complete →
  no second migration). `scope_clarification` type defined, capture **ratified-deferred** (signal-3 admin
  feature). **NORMAL class + a migration** (no separate AC-CD — GapSignal rides SPEC §5 + §6.5). Both
  reviewers pre-registered + concur: auditor P-D-1..10; overseer OV-D1-1..7. Implemented: `app/models.py`
  `GapSignalType` enum + `GapSignal` model (Index on `(signal_type, dedup_key)`); migration
  `0012_d1_d2_gap_signal` (CREATE TYPE + TABLE + 2 indices + trigger; up/down clean); NEW
  `app/domain/signals.py` `capture_discovery_miss`/`capture_question_tag` + `_upsert_signal` (signal-layer
  dedup — increment `occurrence_count` on `(signal_type, dedup_key)` for unconsumed signals); wired
  discovery-miss into `catalogue.list_discoverable_pills` (no-match search → signal). Tests
  `test_gap_signals.py` (6 — capture, dedup increment, consumed-not-absorbed, scope_clarification-deferred,
  discovery wiring) + `test_p1_schema` (38 tables/triggers, 18 types, migration 0012 round-trip).
  **1010/1010 (unit+integration) pass**; ruff/format/mypy(72)/structure/unpinned clean; migration `--sql`
  round-trip valid; **spec-anchor diff EMPTY (NORMAL class)**. Carries the **C2 handover**
  (`handovers/C2-autopublish-gate.md`). · last-seen remote HEAD `7aa5119` · set-diff `0 dropped / 0 added
  (baseline — no reviewer findings yet)` · round-trips: none.
- 2026-06-12 · D1-D2 round-1 — PR #123 opened @ `fe30b18`, subscribed, watcher `bgzbtc2i6`. **Overseer
  governance CLEAN @ `fe30b18` (OV-D1-1..7 Confirm; 1/3).** **Auditor CA-D-1 (medium, Push-back):**
  `capture_question_tag` was a helper with NO call-site → question_tag signals never emitted in practice,
  contradicting **§5:302 "the discovery-miss AND question-tag signals... are captured now"** (discovery-miss
  WAS wired, question_tag wasn't — the asymmetry). **Folded (wired, not deferred — the sweep CONSUMES
  signals so it can't emit them; question_tag must be captured at generation; §5 "captured now" governs):**
  wired `capture_question_tag` into `start_attempt`'s **per_testee branch** (the AI-generated-question path;
  frozen/hand-authored reuse existing questions) keyed on the test's pill name (`test.pill_id` → `get_pill`)
  with `source_ref=pill.id` — one low-risk hook per attempt-start (the attempt's generated questions all
  carry the test's pill tag; occurrence_count accumulates topic-assessment demand for the D3 sweep). Only
  fires for per_testee tests WITH a pill (pill-less tests capture none → the existing per_testee test stays
  green). +1 targeted test (`test_per_testee_attempt_captures_question_tag_signal`). 1011/1011 unit+integration
  pass; ruff/format/mypy clean; spec-anchor diff still EMPTY (NORMAL class). Pushed → re-stales the overseer's
  `fe30b18` governance seal (content change; governance positions [schema/scope/amend-once] unaffected → re-seal
  expected) + addresses the auditor's open marker. · last-seen remote HEAD `7aa5119` (main) · auditor
  `2a350cf` / overseer `c14b473` · set-diff `1 CA-D-1 folded / 0 open` · round-trips: CA-D-1 round 1.
- 2026-06-12 · D1-D2 round-2 — **CA-D-2 + Gitar x6 + overseer GATE-HELD** (consolidated; the overseer
  surfaced the systemic **test-fidelity** root cause — my fake-only tests masked a no-persist bug + degraded
  prod to fit the fake). Folded (all execution against ratified §5/§6.5, NORMAL class): **#1/CA-D-2 (Bug —
  discovery_miss never persisted)** — the GET discovery path never commits; added `await db.commit()` after
  capture in `list_discoverable_pills`, fixed the false "commits with the request" comment; **#2 (Perf —
  O(n) tenant scan)** — pushed `(tenant_id, signal_type, dedup_key)` into the SQL WHERE (the ratified
  `ix_gap_signal_type_dedup_key` index now bounds the read), kept a defensive Python re-check for the
  WHERE-ignoring unit fake (B3 pattern); **#4 (whitespace empty-key)** — `search.strip()` guard; **#5
  (question_tag used test.pill_id, not the generation pill)** — relocated the capture into the per_testee
  branch keyed on `rag_pill` (the actual generation pill; self-initiated → None → no capture); **#6 (name
  dedup collision)** — keep name-keying (intended topic clustering, §6.5) + record contributing `pill_id` in
  `detail`. **Test-fidelity:** replaced the masking `object()`-db unit stub with a real-path integration
  test (`test_discovery_miss_persists_gap_signal` — GET /catalogue/pills → persist + dedup + whitespace-skip)
  + an assignment-driven `test_per_testee_assignment_attempt_captures_question_tag_signal`. **#3 (race / partial
  unique index) — DECLINED in code (recommended-not-blocking, executor discretion):** a bare partial unique
  index + read-then-write would convert a rare benign occurrence_count under-count into a user-facing 500 on
  the GET/attempt commit; the race-safe form (partial unique index + ON CONFLICT/savepoint) is a clean
  follow-up; single-tenant low-concurrency + D3's own dedup arm backstop it (a rare dup shares the same
  `(type,key)` so clustering is preserved). 1011/1011 unit+integration pass; ruff/format/mypy(72)/structure/
  unpinned clean; spec-anchor diff still EMPTY (NORMAL class). · last-seen remote HEAD `7aa5119` (main) ·
  auditor `df8c5d9`/overseer `7bce222` · set-diff `6 Gitar + CA-D-2 folded; 1 declined-with-rationale (race)`
  · round-trips: round 2.
