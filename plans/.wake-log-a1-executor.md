# Wake-log — Slice A1 executor (source-authority allowlist + scoring registry)

**Status: final — approved by executor (Slice A1)** — content-bound to the A1 code substance at
`ef30795` (`app/domain/source_authority.py` + `tests/unit/test_source_authority.py` +
`app/config.py` + `.env.example` + CHECKLIST A1 row). This is the **producer's third sign-off**,
joining the code-auditor's content final-marker (`5e1be81` @ `ef30795`) + the code-overseer's
governance final-marker (@ `ef30795`) — three independent sign-offs at one content-SHA. This
marker is **content-invariant** (wake-log-only; the A1 code is byte-identical to `ef30795`), so
it does **not** re-stale the reviewers' code-bound markers (§8). The executor does **not** flip
draft→ready and does **not** merge — the **code-overseer** executes the squash-merge mechanically
once it re-confirms the three-layer green gate (CI + Gitar + GitHub mergeable; e2e still finishing
at marker time, no failures) + the override window (collapsed to zero by a present spec author).
Any A1 **code** change after this re-stales this marker and I re-verify at the new SHA.

Mechanical append-only action log for the sequenced code-execution audit loop on the
A1 slice PR (autonomous-content-generation workstream, slices A1→F1). Each line:
`<UTC date> · <action> · <last-seen remote HEAD> · <set-diff> · <round-trips>`.
Slice-keyed name (the executor chains across 14 slice PRs; the PR number is allocated
at push and recorded in the PR body).

- 2026-06-12 · A1 draft authored — `app/domain/source_authority.py` (NEW: `Tier` IntEnum,
  `authority_tier`/`authority_score`/`is_allowlisted`/`filter_to_allowlist`,
  `_load_allowlist` env-merge), `app/config.py` (3 `source_authority_t*_extra` fields,
  AC-CD18), `.env.example` siblings, `tests/unit/test_source_authority.py` (16 zero-network
  tests), CHECKLIST A1 row missing→built. Built against the **ratified AC-D28 body**
  (`DECISIONS.md:728`), not the detail-plan lean — verified DS1-a (T1=1.0/T2=0.6/T3=0.3),
  DS1-b (T2 seed nace/osha/iec/astm; T3 seeded-minimally/none), DS1-c (corpus-acquisition
  only; AC-D21 untouched) all match the recommended direction the cycle ratified. Three-layer
  local green: ruff lint+format clean, mypy clean, structure-gate OK, 420/420 unit pass.
  · last-seen remote HEAD `03ece05` (origin/main, PR #113) · set-diff `0 dropped / 0 added
  (baseline — no reviewer findings yet)` · round-trips: none open.
- 2026-06-12 · A1 revision r1 — folded the overseer's **OV-A1-4r** push-back + **Gitar findings
  1/2/3** (verified each against the code first). **F1/OV-A1-4r (REAL contract break):**
  `authority_tier` short-circuited on the exact-host branch before the wildcard scan, so an env
  *exact* at a weaker tier silently downgraded a host a seed *wildcard* ranked higher
  (`*.gov.za`=T1 + T3 `data.gov.za` → returned T3). Fix: take the **strongest** of the exact tier
  and any covering wildcard (AC-D28 "resolves to the stronger tier"; exact-wins-on-tie preserved).
  **F2 (perf):** extracted `@lru_cache`d `_build_allowlist(t1,t2,t3)` so `filter_to_allowlist`
  no longer rebuilds the map per row. **F3 (coverage):** added
  `test_env_exact_does_not_downgrade_seed_wildcard` + `test_strongest_wildcard_wins_among_multiple`.
  18/18 A1 tests + 422/422 unit pass; ruff/format/mypy clean. Autonomous loop fold — no
  spec-author surface (ratified AC-D28 invariant is clear; code-fidelity fix). · last-seen remote
  HEAD `a3f105f` · set-diff `4 added [OV-A1-4r, Gitar-F1, Gitar-F2, Gitar-F3] / 0 dropped`
  (OV-A1-4r supersedes the withdrawn OV-A1-4; OV-A1-1..3/5..14 remain positive-coverage Confirms)
  · round-trips: OV-A1-4r 1/5 · Gitar-F1 1/5 · Gitar-F2 1/5 · Gitar-F3 1/5.
- 2026-06-12 · A1 producer final-marker @ `ef30795`. On-wake pairing on the auditor branch move
  (`afe539e..5e1be81`): read both reviewers' threads + fresh `git ls-remote` + diff. **Convergence
  reached** — code-auditor content final-marker (CA-A1-1/2/3 all RESOLVED-by-r1, 18/18 verified,
  branch `5e1be81`) + code-overseer governance final-marker (OV-A1-4r resolved-by-fold, 0 open,
  both @ `ef30795`). Three-layer green at `ef30795`: Gitar ✓ success (changes-requested cleared),
  all `checks`/`migration-chain`/`docker-build` ✓; e2e in-progress (frontend-only, no failures).
  Posting the producer marker (content-invariant) + approval comment. · last-seen remote HEAD
  `ef30795` · set-diff `3 added [CA-A1-1, CA-A1-2, CA-A1-3] / 0 dropped` (auditor's findings ≡
  Gitar-F1/F3/F2 + OV-A1-4r, all resolved by r1; no new work) · round-trips: CA-A1-1 1/5 ·
  CA-A1-2 1/5 · CA-A1-3 1/5 — **all reviewer threads resolved; awaiting overseer merge execution.**
