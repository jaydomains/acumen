# Wake-log — Slice B3 executor (N-draft fan-out + processing_tasks persistence + cost-share)

Mechanical append-only action log for the sequenced code-execution audit loop on the
B3 slice PR (autonomous-content-generation workstream, slices A1→F1). Each line:
`<UTC date> · <action> · <last-seen remote HEAD> · <set-diff> · <round-trips>`.
B2 merged @ main `30cde56` (PR #119, slice 5/14). B3 is the **fan-out/persistence
mechanism over B2's grounded-generation primitive** — one call → N persisted draft rows,
each with its 1/N cost share + the shared generation `batch_id` (E2 per-batch rollback key).
Branched off `30cde56`.

- 2026-06-12 · B3 started — **verify-before-write against the MERGED v1.9 spec on main (the A3
  lesson)**, NOT the detail plan. Read: detail §6 (Slice 6/B3), ratified **AC-D29** (`DECISIONS:758` —
  *"a `batch_id` stamped by the B3 fan-out for per-batch rollback"*; *"reused by the B3 N-draft
  persistence (each persisted draft's rows carry the shared `batch_id`)"*; *"cost-split across N drafts
  at B3"*), **SPEC §6.8:390** (G3 = *"per-band decomposition is **min/max-range only — no richer
  breakdown**"* — RATIFIED, not "surfaced"; the detail-plan "surfaced, not baked" was pre-ratification
  framing), **AC-CD7** (`ProcessingTask` carrier), **AC-CD8** (spend invariant). **CONFIRMED NORMAL class,
  no migration** — `batch_id` column was minted nullable at B2; drafts reuse `processing_tasks`;
  `_pill_generation_spend` is an absorbable AC-CD8 mirror. **Both reviewers pre-registered + independently
  concur:** auditor P-B3-1..10 (incl. P-B3-2 "B3 extends B2's writer to take batch_id"); overseer
  OV-B3-1..7 ("G3 already ratified min/max-only; B3 pure execution, no migration"). Implemented:
  `app/domain/generation.py` — `generate_grounded_drafts` extended additively with `batch_id` (stamps
  every `GenerationProvenance` row); NEW `enqueue_generated_drafts` (fan-out → N `pending`
  `pill_generation` `ProcessingTask` rows; payload = topic/batch_id/gap_signal/draft/provenance-share) +
  `_pending_batch_for` (`(topic, gap_signal)` pending-row idempotency, §6.2d); `app/ai/cost.py` —
  `_pill_generation_spend` (payload `cost_share` fold) registered in `current_month_spend`. Tests:
  `tests/unit/test_generation_fanout.py` (5 — fan-out N rows, shared-batch_id provenance seam, exact 1/N
  cost-share → `current_month_spend` total [AC-CD8], `(topic,gap_signal)` idempotency, G3 min/max-only).
  466/466 unit pass; ruff/format/mypy(66)/structure/unpinned clean; **spec-anchor diff EMPTY** (NORMAL
  class verified). Carries the **B2 handover** (`handovers/B2-grounded-generation.md`). · last-seen remote
  HEAD `30cde56` · set-diff `0 dropped / 0 added (baseline — no reviewer findings yet)` · round-trips: none.
- 2026-06-12 · B3 round-1 — PR #120 opened @ `babf0a0`, subscribed, watcher `bfbez71ry`. **Auditor content
  review @ `babf0a0` = APPROVED (1/3), all P-B3-1..10 Confirm**, incl. independent verification of the G3
  ratified-min/max claim (§6.8:390); sole note **CA-B3-1** (`_pending_batch_for` full-scan, low, **explicitly
  non-blocking/optional**). **Gitar = Changes-requested (3 findings)** — green leg RED, so all addressed
  (none touch a ratified contract; executor discretion): (1) ⚠️ **perf** ≡ CA-B3-1 → pushed indexed
  `task_name`+`status` into the SQL WHERE (bounds the D3 hot-path scan), kept the Python re-check so the
  WHERE-ignoring AC-CD15 fake still tests exclusion; (2) 💡 **idempotency `gap_signal=None` collapse** → made
  dedup **gap-keyed** (only dedup when `gap_signal is not None`; the gap_signal IS the §6.2d key — D3 always
  supplies one so the real pipeline is unchanged; a direct null-gap call no longer self-suppresses); (3) 💡
  **empty-draft cost leak** → WARN the (rare, pathological) AC-CD8 leak + return `[]` (mirrors the B2
  grounding-mismatch WARN). +3 regression tests (no-dedup-without-gap, done/other-task exclusion,
  empty-draft WARN). 469/469 unit pass; ruff/format/mypy/structure/unpinned clean; spec-anchor diff still
  EMPTY (NORMAL class). Pushed → re-stales the auditor's `babf0a0` marker (content change; expected — auditor
  re-reviews at new SHA). Overseer governance verdict still pending. · last-seen remote HEAD `30cde56` (main)
  · auditor `a9f426a` · set-diff `3 Gitar findings folded / 0 reviewer-blocking open (CA-B3-1 non-blocking,
  resolved by the perf fold)` · round-trips: Gitar round 1.
