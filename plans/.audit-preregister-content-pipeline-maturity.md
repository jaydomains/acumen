# Plan-auditor — pre-registered positions

**Workstream:** content pipeline maturity — dual-path generation + audit-driven fixes
**Role:** plan-auditor (content-correctness lane). Counterpart: planner. Co-reviewer: plan-overseer.
**Audit branch:** `claude/content-pipeline-plan-audit-hkn81t`
**Pre-registered:** 2026-06-13 (~13:45 UTC), **before the planner's parent-plan PR was open**
(ground truth at registration: `git ls-remote` shows no planner/overseer branch; `list_pull_requests state=open` returns `[]`). This is the anti-anchoring baseline (role files §4.2): positions are derived from the *requirements*, never from the draft.

## Yardstick (what I measure the plan against)
1. **The merged overpass audit** — `plans/AUDIT_OVERPASS_2026-06-13.md`, specifically the "Fix-workstream scope preview" (items 1–11) + both severity roll-ups. This is the structural input the planner builds from.
2. **The ratified strategic direction** (recorded in the merged audit; I do **not** relay it — I treat the merged-audit record as the requirement surface, and any *new* ruling must reach the loop through the authenticated channel): **dual-path generation; LLM-direct as primary for the KBC pilot; corpus-grounded matured to spec for future tenants; per-tenant mode configuration; KBC defaults to `llm_direct`.**
3. **The v1.9 anchor bodies** — AC-D28 (`DECISIONS.md:728`), AC-D29 (`:754`), AC-D30 + NS-7 (`:770`/`:776`), AC-D31 (`:788`); AC-CD25 (`CODE_SPEC.md:1278`), AC-CD26 (`:1341`), AC-CD7 nine crons (`:647`), AC-CD18 env-default model IDs, AC-CD8 AIProvider interface, AC-CD3 one-schema/RLS-deferred, AC-CD5 auth one-file-swap, AC-D18 cost visibility/alerts.
4. **The #107 precedent** — parent-plan PR shape: Gate-1 (records direction + plans work, normal-merge-class) vs Gate-2 (each downstream anchor mint / spec body change is spec-author-authored), surfaced-not-baked residuals (NS-*), a carry/supersede ledger so nothing rots, `plans/**`-only scope, planner final-marker + status-line token.

## Finding-ID convention
Per the task directive: findings carry the **`CA-*`** prefix with a **severity classification (CRITICAL / HIGH / MEDIUM / LOW)** mirroring the audit doc's legend, alongside the role-file taxonomy tag (Confirm / Push-back / Refine / Missing). Positions below pre-seed the `CA-*` IDs: a position the draft **satisfies → Confirm**; a position it **fails → open finding**, same ID; a genuinely new residual → new ID (`CA-Nr`), never recycled.

---

## Positions

### Faithfulness to the ratified dual-path direction
- **CA-1 — direction recorded faithfully, not baked.** The plan records dual-path (LLM-direct primary for KBC pilot; corpus-grounded matured-to-spec for future tenants; per-tenant mode config; KBC defaults `llm_direct`) as *direction*, on the #107 Gate-1/Gate-2 split. The anchor mints / spec amendments the direction implies (a per-tenant **generation-mode** product anchor + its AC-CD) are **surfaced as downstream spec-author-authored ratification-class PRs**, not baked into this parent plan.
- **CA-2 — pilot path is LLM-direct.** The close-the-loop phase wires the drain for the **LLM-direct** primary path (the pilot path) and explicitly **decides the trigger model** ("decide the trigger model", scope item 1). It does not silently make the corpus-grounded drain the pilot path.

### Path-independent blockers (must be in the plan, correctly sequenced)
- **CA-3 — CRITICAL: drain wired + tested e2e.** Generation→self-review→publish drain wired and tested end-to-end for the chosen primary path, **with timeout/retry** (P2-#8) and a **cost-idempotency key** (P1-#7). (Audit Finding 1; the workstream's core thesis.)
- **CA-4 — HIGH: cost ceiling + fail-closed, before the drain.** Hard cost ceiling (per-sweep batch cap and/or budget kill-switch) **and** missing-AI-key fail-closed outside dev, landing **before** autonomous publish is enabled. (P2-#2, #10.) Sequencing is load-bearing — the audit says "before enabling the drain".
- **CA-5 — HIGH: concurrency-safety.** `FOR UPDATE SKIP LOCKED` / unique constraints on the task-claim path **and** the `/approve` path. (P2-#1, #5.)
- **CA-6 — HIGH: operable oversight surface OR autonomy-disabled pilot.** Either ship a thin operable oversight + rollback surface, **or** explicitly run the pilot with autonomy disabled (`/approve` publish path) until FE-10 lands. (P1-#3.) The plan picks one and states it as a launch criterion — not left implicit.
- **CA-7 — MEDIUM/HIGH: publish-health metrics + ≥1 proactive alert.** (P2-#7.)

### Corpus-path maturation (deferred for the LLM-direct pilot; matured-to-spec for future tenants)
- **CA-8 — present, correctly scoped, not dropped.** Retrieval-side demotion (P1-#2), content-validity gate + paywalled-source policy (P2-#4), contradiction posture (P2-#9), provenance-purge `ON DELETE CASCADE` (P2-#8) are all **present** in the plan, **matured to spec**, scoped as future-tenant maturation — neither silently dropped nor pulled into the pilot's blocking path.
- **CA-9 — paywalled-source policy + contradiction posture are SURFACED, not baked.** The audit explicitly says "decide a paywalled-source policy" / "decide a contradiction posture" — these are product/safety posture decisions for the spec author, surfaced (NS-style), not baked.
- **CA-10 — provenance-purge coupling reconciled.** The plan must not propose a corpus chunk-purge remediation that cascade-deletes published-pill provenance (audit Pass-2 Axis-5 MEDIUM); the `ON DELETE CASCADE` coupling is surfaced/reconciled with retrieval-side demotion.

### Per-tenant mode config (newly ratified — re-raised earlier than expected)
- **CA-11 — tenant-from-actor seam + guard test, reconciled with AC-CD3/AC-CD5.** Tenant-from-actor seam threaded through the workstream's domain functions + a cross-tenant guard test (P2-#6). The plan reconciles this with AC-CD3 (RLS deferred) and AC-CD5 (auth one-file-swap): surface whether per-tenant mode config forces the full tenant seam now, or rides a lighter config-keyed seam pre-second-tenant.

### Hygiene / awareness
- **CA-12 — hygiene bundle.** `SESSION_START.md` refresh to v1.9 (P1-#4); surface the carry-forward ledger (P1-#11); clamp the publish threshold (P1-#9); **re-confirm the C2 "nothing held" gate (AC-D31)** and §290 audit-log completeness **against the LLM-direct provenance shape** (an LLM-direct/refiner draft has no `draft_ref`/provenance chain → oversight read facets degrade to empty — expected, but verify).

### Cross-cutting disciplines
- **CA-13 — decision points surfaced, not baked.** Every "decide"-flagged item is surfaced for spec-author ratification, not baked: trigger model, paywalled-source policy, contradiction posture, cost-ceiling values + kill-switch-vs-alert, pilot-autonomy-enabled-vs-disabled, per-tenant mode-config shape, provenance-cascade remediation, generation-mode anchor mints. No phase implicitly assumes an answer to an unsurfaced decision.
- **CA-14 — anchor coverage.** The plan references the anchors its scope touches (AC-D31, AC-D30/NS-7, AC-D29, AC-D28, AC-CD25, AC-CD26, AC-CD7, AC-CD18, AC-CD8, AC-D18, AC-CD3, AC-CD5). Any anchor a phase would amend is surfaced as a Gate-2 spec-author-authored body change, not baked.
- **CA-15 — sequencing soundness.** Phase dependencies coherent: cost-ceiling + fail-closed + concurrency-safety **before** the drain is enabled; oversight-surface decision gates the pilot; corpus maturation does **not** block pilot phases; tenant seam precedes second-tenant onboarding (not the pilot). No slice references something not planned in an earlier/same phase.
- **CA-16 — no scope creep beyond the 5 phases.** Post-deploy MEDIUM/LOW items (unbounded scans/N+1, `GenerationProvenance`/`CorpusChunk` growth + IVFFlat reindex, DNS-rebinding, decompression-ratio, English-centric embeddings/content-type trust, F3 cosmetic model-name) stay **deferred** — present as noted-deferred per the audit's "fine post-deploy", not pulled into pilot-blocking scope.
- **CA-17 — #107 precedent shape.** Gate-1/Gate-2 split; surfaced-not-baked residuals section; a **carry/supersede ledger** mapping **every** audit finding (P1 #1–14, P2 #1–14) → addressed-in-phase-N / surfaced / deferred-post-deploy, so nothing silently rots; `plans/**`-only scope; planner final-marker + status-line token; clean per-phase done-when criteria.
- **CA-18 — pilot-launch-criteria clarity.** A crisp, enumerated pilot-launch-criteria section listing exactly the blocker conditions for the chosen pilot path, and what is explicitly **out** of the pilot gate (corpus maturation for future tenants; post-deploy MEDIUM/LOW).

---

## Standing disciplines I hold for the whole loop (role files §3)
- Verify before I write: fresh `git ls-remote` + read the **actual plan text** at the moment I act — never the PR body, a comment, or recalled context. Re-verify every open finding on every wake. Read any SHA back from the push before citing it.
- Finding-set-diff gate before every audit-branch commit, keyed on stable `CA-*` IDs; every dropped ID carries a one-line justification.
- One wake-log line per commit (`plans/.wake-log-content-pipeline-maturity-plan-auditor.md`), even on a no-change wake (`0 dropped / 0 added`).
- Watcher: `SELF_EXCLUDE` = my exact branch; `WATCH_INCLUDE` = planner's **and** overseer's ref space (watcher is comment-blind — never scope out the overseer). Re-arm ~25 min, proactively.
- I do **not** flip draft→ready, do **not** merge, do **not** rubber-stamp. Surface (not adjudicate) the closed-list §7 conditions. Findings live as PR review comments; this branch carries positions + wake-log + final-marker token.

Status: positions pre-registered — standing by for the planner's parent-plan PR.
