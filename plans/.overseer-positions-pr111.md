# Plan-overseer — pre-registered governance positions: PR-B (Generation + provenance + ops-count)

**Status: pre-registered (no draft yet)** — link 2 of 4, sequenced ratification cycle.
**Pre-registered (timestamped):** 2026-06-10T13:32:10Z — BEFORE any PR-B draft exists (§4.2 anti-anchoring).
**Role:** plan-overseer (governance lane + merge-executor) · `.claude/roles/plan-overseer.md`

**Requirements source (NOT a draft):** `plans/2026-06-08-amendment-cycle-extraction.md` §C (PR-B) + §B
(AM-8/AM-9/G7b/G3) + `REQUIRED_READING.md` §7. PR-A merged on `main` @ `c5128d9` (AC-D28/AC-CD25,
v1.9) — PR-B builds on that baseline (back-port not owed; PR-A is the amendment, now landed).

**Per-PR authentication (load-bearing):** PR-B's ratifications are **NOT** covered by PR-A's clearance.
PR-B requires its **own** §D surface authenticated through the **in-session channel BEFORE the planner
authors** (relayed/blanket/inferred = pending, not actionable — PR #104/#105 lesson; the spec-author's
own statement: each PR gets its own direct-channel authentication when its turn comes).

---

## PR-B scope (extraction §C): files DECISIONS (AC-D1 Implications, AC-CD8 numeral, new provenance AC-D), CODE_SPEC (AC-CD8), SPEC (§6). Class mix i, ii, iv.

- **POS-B0 — ratification-class gate.** PR-B is class (i) [AC-D mint + AC-D1/AC-CD8 body changes] + (ii)
  [SPEC §6] + (iv) [if any durable-precedent]. Does NOT auto-merge; needs PR-B's own authenticated §D
  ratification first; re-confirm at execution. I withhold sign-off until the canonical record is clean.
- **POS-B1 — mint/body IDs (spec-author assigns).** New **provenance-chain AC-D** (next-sequential →
  **AC-D29**); **AC-D1 *Implications*** body change (class i); **AC-CD8** numeral change. Verify each
  authored body+ID matches the in-channel ratification exactly.
- **POS-B2 — count-header + numeral integrity.** If AC-D29 minted: `DECISIONS.md:7` 28→29 + footer +
  numeral sweep; AC-CD8 numeral updated in `CODE_SPEC` body. Verify header/footer/body all consistent.
- **POS-B3 — THE TWO-NINES (carried from PR-A POS-A15; load-bearing).** PR-B moves **ops-count
  seven→NINE** (SPEC §6: mint `Operation.pill_generation` (B1) + `content_self_review` (C1)). This is the
  **OPS** nine — a **different axis** from the **CRON** nine already on `main` (PR-A, §8.9/AC-CD7).
  Verify: (a) PR-B does **not** touch or cross-wire the cron count; (b) per GT-1 the enum is already 8
  (7 ops + `embed`), so spec-prose goes seven→nine over **enum 8→10** — verify the enum-vs-prose
  distinction is handled, not conflated; (c) every "seven operations" invariant line (e.g. SPEC §9.7,
  §6 header, §7.1, §8.4, CODE_SPEC §6 routing) moves to nine **consistently** — a partial move is the
  §8-drift partial-fold. **And re-check the v1.9→v1.x version header+footer+pairing-line sync** (the
  exact OV-1/OV-2 defect class from PR-A — SPEC footer + CODE_SPEC line-16 pairing must not lag).
- **POS-B4 — amend-once / front-load echo (AM-8 shared B1+C1).** Author the ops-count **complete (nine)**
  covering both ops, OR per-op (spec-author choice, extraction §B AM-8 — per-op tolerated here because
  construction-oracle test floors + greps make completeness checkable; confirm which was ratified). The
  "nine" references `content_self_review`, whose **anchor lands in PR-C** (front-load echo) — verify
  PR-B's reference to the PR-C self-review op is **mutually consistent** (the AC-D23↔AC-D7 discipline,
  applied to the ops-count↔self-review-anchor cross-PR reference).
- **POS-B5 — amend-once ACROSS PRs.** PR-B must **not** amend anchors owned downstream: the self-review
  AC-D + auto-publish AC-D + AC-D7 (PR-C), the oversight AC-CD + GapSignal (PR-D). And must not re-amend
  PR-A's anchors (AC-D21/22/23/28, AC-CD25) except via legitimate `Related:` back-links. Grep the diff.
- **POS-B6 — provenance table RELATIONAL shape (AM-9 + NS-3).** The `GenerationProvenance` table must be
  **relational** (forced by E2's by-`source_host` query, §5.1 — **not** JSONB). **NS-3** granularity
  (per-assertion vs per-draft — lean per-assertion) sets the `claim_ref` shape. Verify the ratified
  shape (relational) + ratified NS-3 granularity is baked. **Surface forward to PR-D/E2** (the query that
  forces relational lives there).
- **POS-B7 — G7b prompt-version trajectory.** `pill_generation` v1.0.0 (B1) → v1.1.0 (B2,
  grounding_refs). Rides the **prompt registry, not a doc anchor** — verify it is NOT minted as a
  spurious AC-D/AC-CD.
- **POS-B8 — G3 per-band difficulty.** Lean **min/max only**; if per-band, amends the generation
  AC-D / §6.5 output contract (no standalone anchor). Verify no spurious anchor minted.
- **POS-B9 — recursive consistency / version bump.** PR-B bumps the paired docs (likely **v1.10**).
  Verify SPEC + DECISIONS + CODE_SPEC **headers AND footers AND pairing lines** all move together — the
  PR-A OV-1/OV-2 lesson: do not let a footer or a pairing pointer lag the header.
- **POS-B10 — status-taxonomy honesty.** The ops enum/prose change is a spec amendment, not a build —
  verify CHECKLIST honestly reflects built-vs-not (no false "nine ops built" claim), as PR-A did for crons.
- **POS-B11 — merge gate.** 3 independent sign-offs @ one content SHA + three-layer green + override
  window (present spec-author who voices a go collapses to zero) + §8.3 ratification. I execute
  mechanically, never on my own marker, re-confirming the gate from ground truth at execution time.

## Cross-PR carry-forwards (tracked across links)
- **Two-nines:** ops-nine (PR-B) vs cron-nine (PR-A, on `main`) — different axes; PR-B must not cross-wire.
- **Provenance relational shape** forced by E2 by-`source_host` (PR-D) — surface; verify coherence at PR-D.
- **AM-8 "nine" ↔ PR-C self-review anchor** — front-load echo; verify reciprocal consistency at PR-C.
- **AC-D7 reciprocal back-ref + one approve→publish story** — verify at PR-C (AC-D23 forward half landed in PR-A).

## Disposition
Pre-registered. No PR-B draft, no PR-B §D ratification yet. Reviewer-side: planner authors PR-B (PR-A
merge unblocks it) after the spec-author authenticates PR-B's §D in-channel. Holding bounded; watcher
scoped for the PR-B branch. Resume reconstructs from this doc + the wake-log.

---

## G. Spec-author ratification RECORDED — PR-B §D, authenticated in-channel (2026-06-10)

**Authentication:** in-session human channel (§8.3 reference), **direct origin, explicit, current**.
Spec-author = `jaydomains`. **Traces to the authenticated triage artifact**
`plans/ratification-triage-for-pr-b-generation-provenance.md` @ `claude/pr-b-generation-provenance:4355780`
(exists — clean trace, unlike PR-A). **POS-B0 → SATISFIED.** "Cite THIS conversation as authenticated origin."

**Ratified (all 9 planner defaults B-1..B-9 = A):**
1. **B-1=A** — ops **seven→NINE now** (combined), naming `pill_generation` (B1) + `content_self_review`
   (C1, protocol AC-D **front-loaded** to PR-C); amend-once = single touch of AC-D1 + AC-CD8; **built-state
   honesty spec note required:** *"9 ops total; content_self_review wiring completes in PR-C."*
2. **B-2=A** — `pill_generation` (distinct from `pill_proposal` refiner, kept per G7a).
3. **B-3=A** — AC-D1 *Implications* → nine, by-name + anchor cross-refs, **"as of v1.9"** → **PR-B stays
   v1.9, NO version-header bump** (resolves the version-label flag; count-in-pairing-lines still 28→29).
4. **B-4=A** — AC-CD8 enum prose: add `pill_generation`→generate + `content_self_review`→review; numeral
   **9-operation→4-method**.
5. **B-5=A** — mint **AC-D29** (Generation provenance chain), count-header **28→29** + footer, body per triage.
6. **B-6=A** — NS-3 **per-assertion** (claim→chunk) — required for the per-source rollback matrix (autonomy
   item 5 a+b+c+d).
7. **B-7=A** — **relational** `GenerationProvenance` (`draft_ref × claim_ref × corpus_chunk_id ×
   source_host × tier × score`, indexed on `source_host`) — forced by E2 by-source queryability.
8. **B-8=A** — `AIProvenanceMixin` **complement** (call-cost) + new table (claim→source); orthogonal.
9. **B-9=A** — concise §6 generator subsection in PR-B; pipeline-phase prose deferred to PR-C §6.5 rewrite.
- **ADDITIONAL (ratified count-sweep requirement):** `DECISIONS.md:346` "**five Anthropic operations**" →
  **six** (Anthropic-family sub-count incl. `pill_generation`) — fold into the count-surface sweep, **no
  partial-fold drift**.

**Position updates (sharpened for PR-B review):**
- **POS-B3 → CONCRETE COUNT-SURFACE INVENTORY** (verify ALL move; any miss = partial-fold finding):
  `DECISIONS:96` (AC-D1 Impl seven→nine) · `DECISIONS:63` (AC-CD8 numeral 7→9 op→4-method) ·
  **`DECISIONS:346` (Anthropic-family five→six)** · `CODE_SPEC:304-308` (enum prose +2 ops) ·
  `SPEC:298/399/445/529` ("seven operations"→nine) · count-header **`DECISIONS:7` 28→29 + DECISIONS footer**
  · **SPEC pairing line ("…28 decisions")→29** · **CODE_SPEC pairing line→29** (the OV-1/OV-2 lesson).
- **POS-B9 → RESOLVED:** PR-B stays **v1.9** (no version-header bump); only the decision-count 28→29
  propagates across pairing lines.
- **POS-B10 → verify** the "9 ops total; content_self_review wiring completes in PR-C" note + CHECKLIST
  honest built-state ("8 of canonical 9" after B1; 9 after PR-C C1).
- **POS-B4 → verify** PR-B's `content_self_review` reference is consistent with the PR-C self-review AC-D
  (front-load echo, the AM-8↔PR-C cross-reference).
- **POS-B6/B7 → verify** relational schema cols + per-assertion granularity + `source_host` index baked.
- **POS-B5 → verify** AC-D29 minted next-sequential, body matches triage, no out-of-scope anchor touched
  (AC-D22 NOT re-touched; AC-D7/self-review/auto-publish/oversight = PR-C/PR-D).

---

## OV-B1 RESOLVED — spec-author ruling (authenticated, class-(iv) durable precedent) — 2026-06-11
- **Ruling:** revert PR-B **v1.10 → v1.9**. The autonomous-content cycle (PR-A..PR-D) is **one v1.9 batch** — spec versions track *features, not PRs*. Authenticated in-channel (cited origin).
- **STANDING RULE (class-iv, applies to PR-C/PR-D + future cycles):** a sequenced ratification cycle ships under **one version**; per-PR bumps only for **semantically independent** feature additions. → **PR-C and PR-D also stay v1.9** (verify at their reviews; a v1.11/v1.12 bump would now be a finding).
- **Status:** OV-B1 open until the planner reverts the draft to v1.9 **completely** (all labels + changelog fold into v1.9; decision-count 28→29 STAYS — AC-D29 is real). Then re-verify v1.9 consistency (no v1.10 straggler) → my 11 Confirms hold → final-marker.
