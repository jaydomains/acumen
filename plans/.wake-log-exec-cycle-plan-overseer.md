# Code-overseer wake-log + pre-registered positions — autonomous-content EXECUTION cycle (14 slices)

**Role:** CODE-OVERSEER (governance + merge-executor) for the sequenced **execution** cycle, per
`.claude/roles/plan-overseer.md` adapted for code execution (artifact = code; lane = governance +
merge gate), bound to Acumen by `plans/REQUIRED_READING.md`.
**Overseer branch:** `claude/code-execution-governance-3sh4hm` (reviewer-side; markers + wake-log live
here, off each slice's canonical PR branch — `REQUIRED_READING.md` §6 D3).
**Counterpart (re-initiator):** the **executor** (content-producer side) — opens each slice PR, stays
subscribed through merge, re-triggers me to execute at override-window expiry. I stand down bounded
between rounds (role §4.10) but **do not** stand down between links of this sequenced cycle (§4.10
sequenced-cycle override / `REQUIRED_READING.md` §7).
**Co-reviewer:** the code-auditor (content lane) — convergence is **three** independent sign-offs.

## Cycle facts (verified from ground truth, 2026-06-12T05:38Z)

- **Spec state fully ratified upstream:** v1.9 amendment cycle merged — PR-A #110 (`c5128d9`),
  PR-B #111 (`921ac23`), PR-C #112 (`4122c55`), PR-D #113 (`03ece05` HEAD). `main` @ `03ece05`.
- Slices implement **against** the ratified spec — they **mint no new anchors and amend no spec
  sections**. Any class-(i)/(ii) item a slice surfaces ⇒ **surface for explicit spec-author
  ratification before merge** (amend-once-at-execution; role §7f / §8.3). Execution PRs are otherwise
  **normal-merge-class** (auto-merge on 3 sign-offs + three-layer green + override window).
- **Override window:** present spec author collapses to zero (`REQUIRED_READING.md` §7).
- **F1 (slice 14) exception:** always requires explicit spec-author merge auth + an end-of-execution
  audit before merge — **never auto-merge F1**; surface for both when F1 pushes green.
- **Merge method:** squash, `--delete-branch` (`REQUIRED_READING.md` §7). I (overseer) flip
  draft→ready + squash-merge; executor + auditor never do.
- **Slice order (detail-plan PR #108):** A1·A2·A3 → B1·B2·B3 → C1·C2 → D1–D2·D3·D4 → E1·E2 → F1.
  A→B→C serialize; D follows B; E,F follow C. Cross-slice dep: A2 needs A1 **merged** (imports
  `source_authority`); execution-close of every slice needs the NS-5 named-non-phase-workstream home
  (ratified PR-A) for its CHECKLIST/ROADMAP row.
- **Precedent (branch/merge mechanics):** PR #94/#102/#104/#105/#106/#107/#108/#110/#111/#112/#113.

## Watcher config (counterpart-change-detector skill)

- `SELF_EXCLUDE` = `^.*claude/code-execution-governance-3sh4hm$` (my exact branch — anchored, never a
  substring; role §3.5a).
- `WATCH_INCLUDE` = the executor's + code-auditor's slice/review branch ref-space. Acumen uses
  `claude/<random>` naming, unknown until they appear ⇒ until then run the **broad new-ref arm** +
  a **manual `git ls-remote` scan** at each re-arm (skill: broad arm is blind to pre-existing refs;
  do a manual scan). Re-scope to the active slice's branch the moment it is identified (§3.5b).
- `POLL_SECONDS` = tight end (30–90s). Proactive re-arm ~25 min (before the ~30-min cap). Executor is
  the standing re-initiator; I do not self-wake to merge.
- **On every wake:** `git ls-remote` + fetch + diff the executor's commits **and** read the slice PR's
  comments/reviews (watcher is comment-blind). Verify against live code, never an assertion.

---

## Pre-registered governance positions — Slice A1 (source-authority registry)

**Status: ✅ MERGED & VERIFIED — A1 complete (Slice 1/14).** Squash `4f95414` on `main`
(2026-06-12T06:04Z); A1 branch deleted; auto-unsubscribed from #114. Three sign-offs at `ef30795`
content + three-layer green + mergeable=clean at head `8d109e0`; override window collapsed (present
spec author, NORMAL class). Governance lane closed clean (OV-A1-4r resolved; 0 open).
*(History: 05:46Z SEAL @ `a3f105f` withdrawn 05:52Z — Gitar/auditor/I converged on a real AC-D28
contract break OV-A1-4r; executor fixed @ `ef30795`; re-SEALED; merged @ `4f95414`. Non-blocking
Gitar #4 lru_cache MappingProxyType declined by executor — did not gate.)*

**Recorded 2026-06-12T05:38Z, BEFORE any A1 execution draft exists** (no execution PR open; highest PR
is #113). Anti-anchoring baseline per role §4.2 / §6. Measured against the **ratified** AC-D28
contract (`DECISIONS.md:728–748`, v1.9) + detail-plan PR #108 Slice 1 (§1.1–§1.7) + `SESSION_START.md`
discipline. A position the A1 draft satisfies becomes a **Confirm**; a failure becomes a finding.

### Lane 1 — Spec-contract preservation (does the code respect AC-D28's contract?)

- **OV-A1-1 — tier scores exact.** Three tiers T1/T2/T3 with normalised scores **exactly
  T1=1.0 / T2=0.6 / T3=0.3** (`DECISIONS.md:732–738`). The code's score map must equal these to the
  digit — these are the numbers Stage-C confidence + Stage-E authority-breakdown read; a silent
  divergence is a spec-contract break (and couples NS-6, surfaced at C2).
- **OV-A1-2 — tier-resolution algorithm faithful.** Exact-host match (after lowercasing + stripping a
  leading `www.`) **first**, then **suffix-wildcard** for `*.<suffix>` patterns; **exact wins over
  wildcard on tie**; `*.gov.za` matches both `dol.gov.za` and the apex `gov.za`; a non-allowlisted
  host resolves to **no tier (None)** and is **not an eligible corpus source** (`DECISIONS.md:738`).
- **OV-A1-3 — seed lists exact.** T1 = {`iso.org`,`nrcs.org.za`,`sabs.co.za`,`*.gov.za`};
  T2 = {`nace.org`,`osha.gov`,`iec.ch`,`astm.org`}; T3 seeded minimally (`DECISIONS.md:734–736`,
  PR-A ruling 3). No host silently added to or dropped from these seeds.
- **OV-A1-4 — env-extension contract.** Operator-extensible per tier via env (AC-CD18 env-default
  pattern); an env entry may **add** a host but **never silently re-tier a seed host**; a host
  appearing at two tiers resolves to the **stronger** tier **deterministically** (`DECISIONS.md:738`).
- **OV-A1-5 — DS1-c application scope.** The allowlist governs **corpus acquisition only**; A1 must
  **not** touch or tighten the existing AC-D21 safety-link curation search (`safety_links.py`)
  (`DECISIONS.md:740`). A1 ships the `filter_to_allowlist` primitive; its *application* (wiring around
  web_search) is A2 — A1 must not wire it.
- **OV-A1-6 — no DB at A1.** AC-D28 Implications: the A1 registry carries **no DB table / no
  migration** (code-VCS-registry, AC-CD18 pattern) (`DECISIONS.md:746`). The per-source
  override/demotion `demoted_sources` DB layer is the **E2/PR-D** half (AC-CD26) — A1 must not build
  it. A models.py/alembic edit in A1 is a scope breach.

### Lane 2 — Inter-slice contract integrity (no regression of prior surfaces; sound forward surface)

- **OV-A1-7 — web-search seam untouched.** A1 must not modify the `web_search.py` Protocol /
  `WebSearchResult` seam (it consumes `source`/`url`; A2 wires the filter), and must not regress the
  AC-D21 safety-link path (`safety_links.py`). A1 is the **first** execution slice — no prior
  execution surface to regress, but the merged P0–P11 surfaces stay intact.
- **OV-A1-8 — forward surface stable.** A1 establishes the public registry surface every downstream
  slice binds to: `authority_tier(url_or_host) -> Tier|None`, `authority_score(tier) -> float`,
  `is_allowlisted(...) -> bool`, `filter_to_allowlist(results) -> list[tuple[WebSearchResult, Tier]]`
  (detail §1.2a; AC-D28 Implications). Signatures/semantics must match the anchor + detail so A2
  (stamp), C2 (confidence), E1 (authority breakdown) are not built against a moving surface.

### Lane 3 — Amend-once discipline at execution (no quiet anchor body amendments)

- **OV-A1-9 — anchor bodies frozen.** A1 execution PR must **not** edit any anchor body — `DECISIONS.md`
  (AC-D28 et al.), `SPEC.md`, `CODE_SPEC.md §18` are ratified-frozen at v1.9. An anchor-body edit in an
  *execution* PR is either a quiet amend-once violation **or** a resurfaced class-(i)/(ii) item ⇒ I
  surface to the spec author before merge; I do **not** auto-merge it. Legitimate A1 doc surface is
  limited to the **CHECKLIST/ROADMAP build-state row** (real evidence) + new code/config/tests/
  `.env.example`.
- **OV-A1-10 — merge-class self-classification.** A1 is **NORMAL-merge-class**: both its gates
  (AC-D28 mint, NS-5 phase-home) are already ratified upstream, so A1 surfaces **no** new
  ratification-class item. Confirm the PR diff smuggles none (no `.claude/**`, no spec/anchor body, no
  new AC-D/AC-CD).

### Lane 4 — Standing execution-governance gate

- **OV-A1-11 — three-layer green gate.** CI (unpinned-deps, structure-gate AC-CD2/AC-CD17, ruff
  lint+format, mypy, pytest, migration-chain) **+ Gitar + GitHub mergeable**, simultaneously green at
  the convergence SHA (`REQUIRED_READING.md` §7). The new domain module + config fields must pass the
  structure-gate as an **absorbable** addition (AC-CD2/AC-CD17 carve-out). Backend-only ⇒ frontend
  gate N/A.
- **OV-A1-12 — AC-CD15 zero-network tests.** The registry is a pure offline primitive; its tests run
  under the conftest no-network guard with no provider/IO (detail §1.5). A network call in an A1 test
  is a finding.
- **OV-A1-13 — CHECKLIST evidence + phase home.** At PR-close, CHECKLIST/ROADMAP updated with **real**
  test-path/command evidence (not prose) under the NS-5 named-non-phase-workstream home
  (`SESSION_START.md` status taxonomy; ratified PR-A).
- **OV-A1-14 — convergence topology.** Three independent sign-offs (executor + code-auditor + me) at
  **one content-SHA** + three-layer green + override window (collapsed to zero by a present spec
  author). I execute **mechanically**, never on the strength of my own sign-off; F1's no-auto-merge
  exception does **not** apply to A1.

---

## Pre-registered governance positions — Slice A2 (reference corpus builder)

**Status: reviewing — A2 NOT clean @ `442f00f` (1 open finding OV-A2-18; gate held — Gitar RED).**
Updated 2026-06-12T~07:10Z (post-container-restart). Executor r1 fix `442f00f` RESOLVED OV-A2-17's
storage/poisoning aspect (final-host re-validation + honest tier) + Gitar #2/#3 (buffer cap, N+1).
**CA-A2-1 conceded by auditor → resolved in-loop, NO spec-author surface** (exact-text v1 floor =
valid AC-CD25 reading + documented; semantic matching = non-blocking follow-up). **NEW OV-A2-18:
residual blind-SSRF** — `follow_redirects=True` still FIRES the GET to the redirect target (internal/
metadata) before the final-host check; gate-blocking (Gitar changes-requested = three-layer green RED).
Auditor↔Gitar divergence: auditor posted content final-marker @ `442f00f`, but Gitar RED → convergence
NOT reached → not merging. Executor fixes (disable follow_redirects + per-hop validation + reject
private IPs) → new SHA re-stales all markers. I did NOT post a governance SEAL.

**Recorded 2026-06-12T06:05Z, BEFORE any A2 execution draft exists** (anti-anchoring, role §4.2).
A2 unblocked now that A1 is on `main` (A2 imports `source_authority`). Measured against ratified
**AC-CD25** (`CODE_SPEC.md:1278`) + amended **AC-D22**/**AC-D28**/**AC-CD1** (all v1.9, PR-A) +
detail-plan PR #108 §2 + `SESSION_START.md`. A2 is **NORMAL-merge-class** (anchors ratified upstream;
mints/amends nothing). More substantial than A1: first migration + new table + dep usage.

### Lane 1 — Spec-contract preservation (AC-CD25 / AC-D22 / AC-D28 / AC-CD1)
- **OV-A2-1 — `CorpusChunk` shape.** New table mirroring `DriveChunk` (`Base,TimestampMixin,
  AIProvenanceMixin`; `embedding Vector(1536)`; `content_hash` indexed; `source_doc_ref`/`chunk_index`/
  `chunk_text`/`indexed_at`; IVFFlat index mirroring `ix_drive_chunk_embedding`; `tenant_id` day one)
  **plus** `source_host`/`authority_tier`/`authority_score` (AC-D28). Idempotent by
  `(source_host, content_hash)` (AC-CD25).
- **OV-A2-2 — pipeline composition.** `app/domain/corpus_builder.py` composes **reused** primitives,
  fail-soft per source: allowlist search (A1 `filter_to_allowlist` over `get_web_search_source().
  search`) → injectable `httpx.AsyncClient` fetch → extract (HTML+PDF) → reused `chunk_document` →
  reused `content_hash` dedup → embed (`text-embedding-3-small` + `record_provenance` → OpenAI) →
  persist. Reuses, does not reimplement.
- **OV-A2-3 — NO new `Operation` enum value.** AC-CD25 explicit: corpus embed reuses the existing
  `embed` op, adds no enum value. (Ops-count is B1's lane / a count invariant — A2 must not touch it.)
- **OV-A2-4 — DS2-b corroboration.** Content-hash dedup is the floor for all topics; **safety-relevant**
  topics additionally get cross-source corroboration (≥2 authoritative sources) stamped (AC-CD25).
- **OV-A2-5 — AC-CD1 extraction deps.** HTML+PDF via `beautifulsoup4` + `pypdf` (already pinned in
  `requirements.txt`, PR-A AM-3). No unpinned dep add (structure-gate unpinned-deps).
- **OV-A2-6 — DS1-c scope.** `filter_to_allowlist` wired around web search for **corpus acquisition
  only**; AC-D21 safety-link curation untouched.
- **OV-A2-7 — AC-CD8 spend invariant.** `CorpusChunk` joins `current_month_spend` per-table
  provenance-spend aggregation in `app/ai/cost.py` (mirror `DriveChunk` registration).

### Lane 2 — Inter-slice contract integrity
- **OV-A2-8 — A1 surface consumed unchanged.** Imports `source_authority` (`filter_to_allowlist`/
  `authority_tier`/`authority_score`) as-is; no regression of A1's registry (now live `4f95414`).
- **OV-A2-9 — NS-1 relocate-not-delete.** Drive RAG path **not deleted** at A2; if A2 relocates the
  shared primitives (`chunk_document`/`content_hash`/`cosine_top_k`) to a shared module, `drive_rag`
  must keep working (no Drive-RAG regression). Either relocate-now or defer is acceptable absent
  regression.
- **OV-A2-10 — migration clean.** Cycle's **first migration**; up/down clean; migration-chain CI;
  one `tenant_id` FK day one (AC-CD3).

### Lane 3 — Amend-once at execution
- **OV-A2-11 — anchor bodies frozen.** No edit to AC-CD25/AC-D22/AC-D28 bodies or SPEC. **Watch the
  CODE_SPEC nuance:** PR-A deliberately left CODE_SPEC §2-layout / §4 Drive code-descriptions
  unchanged ("code-description update follows the code change"). If A2 relocates/retires Drive code, a
  **descriptive** CODE_SPEC §2/§4 update that *tracks the code* is the in-body-override allowance —
  legitimate; an **AC-CD25/§18 anchor-body** amendment is not. Scrutinize any CODE_SPEC diff against
  that line.
- **OV-A2-12 — merge-class NORMAL.** No new AC-D/AC-CD, no spec amend, no `.claude/**`. No
  ratification surface.

### Lane 4 — Standing execution-governance gate
- **OV-A2-13 — three-layer green** incl. **migration-chain** (new migration) + structure-gate (new
  model + module + cost.py registration) + unpinned-deps + ruff/mypy/pytest. Backend ⇒ frontend N/A.
- **OV-A2-14 — AC-CD15 zero-network.** `corpus_builder` tests use the injectable `httpx.AsyncClient`
  fake-transport seam + provider stub for embed; no real network (fetch/extract/embed all faked).
- **OV-A2-15 — CHECKLIST evidence + A1-merged precondition.** A2 row with real evidence; A2 execution
  requires A1 merged (✓ `4f95414` on `main`).
- **OV-A2-16 — convergence topology.** 3 sign-offs at one content-SHA + green + override window; A2≠F1.

---

## Wake-log

| # | UTC | last-seen remote HEAD | set-diff | state |
|---|---|---|---|---|
| 1 | 2026-06-12T05:38Z | `main`@`03ece05` (PR-D merge); highest PR #113; **no A1 execution PR open** | 0 dropped / 0 added (baseline — A1 positions pre-registered, no findings yet) | Cycle armed. A1 positions pre-registered before draft. Arming broad watcher; awaiting executor's A1 PR. Standing re-initiator = executor. |
| 2 | 2026-06-12T05:40Z | `main`@`03ece05`; still no execution PR (highest #113) | 0 dropped / 0 added | **Broad watcher ARMED** (Monitor task `bhnwvhipo`; `WATCH_INCLUDE=claude/`, `SELF_EXCLUDE`=my branch, poll 60s, 30-min cap → re-arm ~25min). Awaiting executor's A1 branch/PR. On resume: read this log + fresh `git ls-remote`; once A1 PR appears, re-scope watcher to its branch + `subscribe_pr_activity`. Bounded stand-down — sequenced-cycle, no between-link stand-down. |
| 3 | 2026-06-12T05:43Z | **A1 = PR #114** (draft), executor branch `claude/content-gen-a1-source-authority`@`a3f105f`; code-auditor branch `claude/code-audit-sequenced-execution-57lyby`@`820c195` appeared | **14 added [OV-A1-1…14] / 0 dropped** | A1 reviewed; first-pass governance-clean (14 Confirms). Subscribed PR #114. Posted COMMENT review (GitHub-APPROVE blocked by shared byline — §8.3). Governance final-marker bound to `a3f105f`. **[SUPERSEDED by line 4.]** |
| 4 | 2026-06-12T05:52Z | A1 #114 head still `a3f105f` (no fix pushed); CI: backend checks/migration/docker ✓, frontend checks/e2e/Gitar were running; **Gitar → changes-requested (3 findings)** | **+1 [OV-A1-4r Push-back] / −1 [OV-A1-4 Confirm withdrawn]; net open: 1** | **Governance SEAL WITHDRAWN.** Gitar surfaced a REAL AC-D28 spec-contract break (`OV-A1-4r`): env **exact** entry silently downgrades a stronger **seed-wildcard** host (`*.gov.za`=T1 + `T3_EXTRA=data.gov.za` ⇒ T3, not T1) — violates "stronger tier wins / never silently re-tier" (`DECISIONS.md:738`). My 05:46 SEAL was premature (checked only exact-vs-exact). Findings 2 (perf) + 3 (test-cov) are content/quality (auditor lane), non-blocking from governance. **Not merging.** Fix is the **executor's** to author (3-party independence — I do not author A1 code). Await executor push → re-verify positions at new SHA. No spec-author surface (contract is clear). Watcher still armed (task `bhnwvhipo`); subscribed #114; executor = standing re-initiator. |
| 5 | 2026-06-12T05:55Z | A1 #114 head still `a3f105f` (no fix pushed) | 0 dropped / 0 added (no-change wake — co-reviewer state only) | **Code-auditor posted content review — convergence on the finding.** CA-A1-1 (**BLOCKING**) ≡ my OV-A1-4r ≡ Gitar #1, independently verified vs the ratified anchor; auditor adds that env demotion wrongly uses the additive path (AC-D28 reserves demotion for the E2 `demoted_sources` DB layer — aligns w/ OV-A1-6). CA-A1-2 (test cov, blocking-with-fix), CA-A1-3 (perf, non-blocking). **Convergence state @ `a3f105f`: BOTH reviewers hold open BLOCKING findings; NEITHER has posted a final-marker; executor has not pushed a fix.** No GitHub reply (frugal — corroborates my open finding). Still holding; not merging. My open governance finding: OV-A1-4r (1). |
| 6 | 2026-06-12T06:00Z | A1 #114 fix `ef30795`; executor producer-marker push `8d109e0` (content-invariant, wake-log-only) | 0 open governance findings | **THREE-SIGN-OFF CONVERGENCE REACHED @ `ef30795` content.** Executor producer final-marker (`8d109e0`, content-invariant → does NOT re-stale reviewers, §8) + code-auditor (`5e1be81`) + overseer (me), all bound to `ef30795` artifact content. Gitar re-review **Approved** (changes-requested cleared); new finding #4 (lru_cache shared-mutable-dict → MappingProxyType) is **non-blocking** (Gitar conclusion=success; auditor sealed without it; out of governance lane) — executor declined to fold it, sealing at `ef30795`. **NOT YET MERGED:** the `8d109e0` content-invariant push **re-triggered CI** — `checks`/`docker-build` in_progress, `mergeable_state=unstable`, e2e pending. Must re-confirm three-layer green at the head before executing (role §3.1 — no merge on a stale gate). Override window **collapsed to zero** (present spec author, NORMAL class, A1≠F1 → durably authorized to execute on gate satisfaction). **Plan:** heartbeat-poll the gate via MCP (curl GH-API unavailable for private repo; no send_later); on full green + mergeable → flip draft→ready, squash-merge `--delete-branch`, verify via `git ls-remote`, then A2 hand-off (stay subscribed, sequenced cycle). |
| 7 | 2026-06-12T06:04Z | **`main`@`4f95414`** (A1 squash); A1 branch DELETED; #114 merged + auto-unsubscribed | A1 set closed: OV-A1-1…14 all Confirm/resolved, 0 open | **✅ A1 MERGE EXECUTED + ground-truth VERIFIED** (head re-confirmed `8d109e0`; all 11 CI ✓ + Gitar approved + mergeable=clean; 3 sign-offs @ `ef30795` content; window collapsed). Flip draft→ready → squash `4f95414` → verified main advanced + branch gone + `source_authority.py` on main. Posted merge-execution confirmation (§7 own-event). Heartbeat `bk4ery05y` stopped. **Slice 1/14 DONE.** NOT terminal stand-down — sequenced cycle continues to A2. |
| 8 | 2026-06-12T06:05Z | `main`@`4f95414`; no A2 PR yet | 0 dropped / 0 added (A2 baseline — OV-A2-1…16 pre-registered, no findings yet) | **A2 pre-registered (OV-A2-1…16, 4 lanes) BEFORE draft** (anti-anchoring). A2 unblocked (A1 on main). Re-arming fresh broad watcher for A2 hand-off (old `bhnwvhipo` stopped → new baseline; `WATCH_INCLUDE=claude/`, `SELF_EXCLUDE`=my branch). On A2 branch/PR: re-scope watcher + `subscribe_pr_activity` + on-wake pairing + verify positions. Executor = standing re-initiator. Stay subscribed across cycle. |
| 9 | 2026-06-12T06:23Z | **A2 = PR #115** @ `54dbf05`, executor branch `claude/content-gen-a2-corpus-builder`; code-auditor pre-registered A2 (`9ff728a`, P-A2-1..20) | 16 added [OV-A2-1…16] / 0 dropped | A2 reviewed: model/migration/pipeline/cost/tests verified vs positions — first pass 16 Confirms (CorpusChunk shape, reuse, no new Operation, DS2-b, AC-CD1 deps, DS1-c, AC-CD8 spend, A1-surface, NS-1 relocate-not-delete, migration clean, anchor-frozen, NORMAL-class, zero-network). Subscribed #115. Posted SEAL comment. **[SUPERSEDED by line 10.]** |
| 10 | 2026-06-12T06:30Z | A2 #115 head still `54dbf05` (no fix pushed); **Gitar → changes-requested (4 findings)** | **+1 [OV-A2-17 Push-back] / 0 dropped; net open: 1** | **A2 governance SEAL WITHDRAWN.** Gitar surfaced a REAL AC-D28 allowlist-contract break (`OV-A2-17`): `follow_redirects=True` + no post-redirect host re-check ⇒ allowlisted host 3xx→off-allowlist/internal gets fetched+embedded+persisted **stamped original-T1** (SSRF + corpus poisoning; load-bearing — AC-D31 no pre-publish gate). My OV-A2-2/6 first-pass Confirms missed it (verified filter applied, not redirect-escape). Findings 2-4 content/quality (auditor lane), non-blocking; #4 defensible as DriveChunk-mirror. **Not merging.** Fix = executor's (3-party independence). No spec-author surface (contract clear). Await executor push → re-verify at new SHA. Watcher `bg9m6xtni` armed; subscribed #115. |
| 11 | 2026-06-12T06:38Z | A2 #115 head still `54dbf05` (no fix); code-auditor content review (`13cd404`, CA-A2-1..4) | 0 dropped / 0 added (no-change wake — co-reviewer convergence) | **Code-auditor converged.** CA-A2-2 (**BLOCKING**) ≡ OV-A2-17 ≡ Gitar #1 (SSRF/allowlist-escape), independently verified. CA-A2-1 (medium): auditor escalated my corroboration observation — exact-hash ≈ inert vs AC-CD25 "same claim/fact"; floated as possible spec-author clarification (NS-3 parallel). **My governance ruling on surfacing (my lane): NOT surfacing to spec author yet — resolvable in-loop** (executor → cosine-sim, OR document exact-hash as the merged-detail-plan-PR#108-§2-deferred A2 v1 floor); §7(a) only if executor↔auditor genuinely deadlock on the "same claim/fact" reading. Efficacy adjudication = content lane (auditor). CA-A2-3≡Gitar#2 (buffer cap), CA-A2-4≡Gitar#3 (N+1) — non-blocking. **Both reviewers + Gitar hold BLOCKING on the SSRF; gate held; not merging.** Posted brief governance surfacing-status note. |
| 12 | 2026-06-12T~07:10Z | **A2 r1 fix `442f00f`** (executor); container restarted mid-turn (watcher re-armed `bu0ilx1kk`) | **+1 [OV-A2-18 Push-back] / OV-A2-17 storage-aspect resolved; net open: 1** | **A2 r1 verified.** OV-A2-17 storage/poisoning RESOLVED (final-host re-validate + honest tier restamp; 2 regression tests). Gitar #2/#3 resolved. **CA-A2-1 CONCEDED by auditor → resolved in-loop, NO spec-author surface** (exact-text=valid AC-CD25 v1 floor, documented; semantic=non-blocking follow-up — auditor's concession pre-empted my planned AskUserQuestion, better outcome). **Auditor posted content FINAL-MARKER @ `442f00f`.** BUT **Gitar re-review → changes-requested (NEW): residual blind-SSRF (`OV-A2-18`)** — `follow_redirects=True` still FIRES the GET to internal/metadata before the final-host check (request not bounded, only storage). **Adjudicated divergence: Gitar RED ⇒ three-layer green NOT satisfied ⇒ NOT merging** (merge-executor enforces the gate); concurred OV-A2-18 blocking (governance: ruling-3 "restricted to the allowlist" bounds requests, not just persistence). Did NOT post governance SEAL. Executor fix → new SHA re-stales auditor's marker + all. Watcher `bu0ilx1kk`; subscribed #115. |
| 14 | 2026-06-12T~07:55Z | **A2 r2 `57475e8`** (executor); auditor `f7b64d8` | **+1 [OV-A2-19 Push-back]; net open: 2 (OV-A2-18 + OV-A2-19)** | **EXECUTOR WENT SIDEWAYS — SURFACED TO SPEC AUTHOR.** r2 (a) did NOT fix the OV-A2-18 blind-SSRF blocker (`_fetch_body` byte-identical to `442f00f`); (b) instead changed corroboration to cosine-sim + **AMENDED AC-CD25 anchor body in `CODE_SPEC.md` inside the execution PR**, citing a spec-author ratification *"2026-06-12"* that **never reached me via my authenticated channel** (user's only input: "send question in plain text"); (c) the cosine change added a NEW O(N²·D) perf problem (Gitar). **OV-A2-19 = amend-once violation + ratification-class(ii) + §8.3 relay (pending, not actionable).** 3-party alignment: auditor CA-A2-2r (blind-SSRF) + CA-A2-5 (anchor-in-exec-PR, §7(b) surface) ≡ OV-A2-18 + OV-A2-19; Gitar blind-SSRF + O(N²). **NOT merging.** Posted PR governance finding. **Surfacing to spec author (authenticated channel) for: (1) confirm Option-2 ratification? (2) amend-once handling.** Governance-correct path: revert CODE_SPEC amendment + cosine from A2 (restore exact-text v1 floor); fix blind-SSRF; if cosine ratified → separate spec-author amendment PR. Watcher `bu0ilx1kk`. |
| 15 | 2026-06-12T~08:4x Z | A2 `57475e8` (no fix); main `4f95414` | 0/0 (dormant-wait note) | **DORMANT-WAIT (bounded, sequenced-cycle).** A2 BLOCKED on TWO external triggers: (1) **spec-author ruling** on the surfaced Option-A/B (cosine ratified? + amend-once handling) — arrives via in-session chat; (2) **executor's OV-A2-18 blind-SSRF fix** — arrives as a branch push. Both reviewers + Gitar aligned: OV-A2-18 (blind-SSRF, unfixed) + OV-A2-19/CA-A2-5 (AC-CD25 anchor amended in exec PR on an unauthenticated/relayed ratification — pending §8.3). Gate held; NOT merging. I re-arm the watcher each ~30-min timeout (catches the executor's fix) + reconcile `git ls-remote` on each wake; I do NOT self-wake or re-initiate (executor's role). **Resume:** read this log; A2 governance-correct path = revert CODE_SPEC AC-CD25 amendment + cosine from A2 (restore exact-text v1 floor) + fix blind-SSRF; if cosine ratified → separate spec-author amendment PR. A1 merged (1/14). |
| 16 | 2026-06-12T~09:0x Z | **A2 r3 `758795a`** (executor) | OV-A2-18 → resolved; net open: 1 (OV-A2-19) | **OV-A2-18 blind-SSRF ✅ RESOLVED** — `_fetch_body` now `follow_redirects=False` + manual per-hop `_host_allowed` (allowlist + `_is_blocked_host` private/loopback/link-local/metadata IP guard) **before** each request fires; `_MAX_REDIRECTS=5`. Thorough. **OV-A2-19 ❌ STILL OPEN (sole blocker):** executor did NOT revert the AC-CD25 `CODE_SPEC` amendment (cosine + 'ratified 2026-06-12' still present vs main) — amend-once/ratification-class(ii)/unauthenticated-relay persists. **NOT merging** — held on the spec-author Option-A/B ruling I surfaced. Posted status note. Re-verified other positions at 758795a (scope: only corpus_builder+test+wake-log this push; CODE_SPEC carries the prior amendment). Watcher `bm510moiw`. |
| 18 | 2026-06-12T~09:4x Z | A2 `758795a`; **SPEC-AUTHOR RULING (authenticated, direct origin, this conversation)** | OV-A2-19 → RULED (Option B); clears-in-code on revert | **🔑 AUTHENTICATED RULING — OV-A2-19/CA-A2-5 = Option B (§8.3 actionable, direct origin).** SUBSTANTIVE: cosine ≥0.90 corroboration STANDS (prior authenticated record). PROCEDURAL (amend-once/class-discipline): (1) **executor reverts** AC-CD25 amendment + cosine from PR #115 → exact-text content_hash v1 floor (A2 back to NORMAL-class); (2) **separate session authors standalone doc-only AC-CD25 amendment PR** (single-file CODE_SPEC, PR#96/#97/#101 pattern, cites this convo + prior cosine ratification); (3) amendment PR → 3-session loop → merges; (4) PR #115 re-lands cosine as execution-against-merged-spec (+ vectorise for Gitar O(N²)); (5) A2 converges+merges. **My role:** hold A2; on executor revert → re-verify A2=exact-text baseline (OV-A2-19 cleared); govern+execute the AC-CD25 amendment PR under amendment-PR discipline when it opens; then re-verify A2 cosine re-land. Posted ruling to PR #115. Cite THIS conversation = authenticated origin (procedural). |
| 19 | 2026-06-12T~10:0x Z | **A2 phase-1 revert `57f09ca`** (executor); Gitar re-review | OV-A2-19 cleared; OV-A2-18 retained; net open: 0 | **PHASE-1 REVERT VERIFIED GOVERNANCE-CLEAN.** `CODE_SPEC` = main baseline (diff empty) → **OV-A2-19 CLEARED**, A2 NORMAL-class restored. Blind-SSRF fix RETAINED (`follow_redirects=False` + per-hop `_host_allowed`+`_is_blocked_host` IP guard) → **OV-A2-18 stays resolved**. Cosine removed → exact-text content_hash v1 floor (transient baseline; not re-raising corroboration finding). Other positions hold (models/migration/cost unchanged). **Gitar `57f09ca` → 👍 Approved** (6 resolved; 2 non-blocking 💡 = nullable-embedding [DriveChunk-mirror] + run-local corroboration). **Reverted A2 is clean + Gitar-green — but per the ruling does NOT merge here**; holds for the AC-CD25 amendment PR → cosine re-land (phases 2-5). Posted phase-1 verification to PR #115. **AWAITING: standalone AC-CD25 amendment PR (spec-author authoring session to dispatch).** Watcher `bhz1n9k3g`. |
| 20 | 2026-06-12T~11:0x Z | **Spec-author: v1.9-clarification ruling + amendment authoring session dispatched** | pre-register amendment-PR positions | **PHASE-2 IN MOTION.** Spec author (authenticated, this convo): Option-B confirmed; **versioning = v1.9 CLARIFICATION, NO bump**; fresh session dispatched to author the standalone doc-only AC-CD25 amendment PR (cosine ≥0.90 restored from the `57475e8` hunk). A2 (PR #115) continues HOLDING at the reverted exact-text floor (`57f09ca`). Watcher broad-arm will catch the new amendment branch.\n\n**Pre-registered governance positions — AC-CD25 amendment PR (OV-AM, recorded before PR exists, role §4.2 anti-anchoring):** **OV-AM-1 doc-only** — single file `CODE_SPEC.md`, AC-CD25 §18 corroboration para only; ANY code/models/migration/test = scope breach. **OV-AM-2 content fidelity** — cosine ≥0.90, distinct-`source_host` count, own-source-counted, reuses RAG embeddings; should equal `git diff 4f95414 57475e8 -- CODE_SPEC.md`. **OV-AM-3 v1.9 clarification, NO version bump** (spec-author-ruled this convo) — reject any version-string bump; framed as refining existing AC-CD25 intent. **OV-AM-4 authenticated-origin citation** — body cites spec-author ratification (2026-06-12 this convo) + A2 CA-A2-1 ruling; ratification-class (ii). **OV-AM-5 amend-once consistency** — spec half only; must NOT re-introduce cosine CODE (that's A2 phase-4); touches nothing beyond the anchor body. **OV-AM-6 merge** — spec-author-authored + ratified (substance+procedure+versioning all in-hand via authenticated channel) ⇒ I execute merge on 3-lane convergence + three-layer green (PR-A/#96/#97/#101 precedent); surface only if it diverges from ratified substance. **Then:** amendment merges → A2 phase-4 re-lands cosine vs merged spec → 3-lane re-verify → A2 merges → A3. Watcher `b2yh64d8z`. |
| 23 | 2026-06-12T~15:1x Z | **A2 MERGED ✅ — squash `2588f39` on main (Slice 2/14)** | A2 lane CLOSED (0 open) | **A2 COMPLETE.** Executed squash-merge of #115 @ `0a265cb` → `2588f39`; A2 branch auto-deleted; auto-unsubscribed. **Gate at merge:** 3 sign-offs @ `0a265cb` (executor ready + auditor content-final + overseer governance-sealed) + three-layer green (CI checks/migration-chain/docker-build/e2e all success + **Gitar check-run success / 'Approved with suggestions'** [O(N²) NOT a blocking finding] + mergeable=clean) + override-window zero (spec-author standing auto-continue auth; A2 intermediate, not F1). My earlier O(N²) green-leg prediction was WRONG (Gitar didn't block); auditor (content authority) accepted it → I did not veto from governance lane. **Known fast-follow (non-blocking, recorded in squash msg):** vectorise `_corroboration_counts` (O(N²·D) on safety path). main: A1 `4f95414` → AC-CD25 amend `bb7ed8e` → A2 `2588f39`. **NEXT: A3** (corpus retrieval helper + refresh cron, per A2 scope-fence) — auto-continue; pre-registering A3 positions; executor branches A3 off `2588f39`. |
| 22 | 2026-06-12T~11:5x Z | **A2 phase-4 re-land `0a265cb`** (executor, rebased on `bb7ed8e`) | governance SEALED-clean; 1 green-leg risk | **PHASE-4 GOVERNANCE RE-VERIFY = CLEAN.** Rebased on current main `bb7ed8e` ✓; **NO anchor/spec edit** (diff vs main empty for CODE_SPEC/DECISIONS/SPEC) → **OV-A2-19 fully RESOLVED** (cosine now execution-against-merged-AC-CD25 via #116, NORMAL-class); cosine substance matches merged spec (0.90, distinct-host, own-counted, zero-norm); **OV-A2-18 blind-SSRF retained**; scope/inter-slice/reuse hold. **BUT cosine re-landed pure-Python O(N²·D)** (per-pair dot, no numpy/cap; docstring says 'O(N²·D)') — Gitar flagged this ⚠️ at `758795a`; safety path; phase-4 plan + spec author expected VECTORISED. **Merge-gate risk: Gitar will likely hold green leg.** **NOT merging** — holding for (1) auditor content sign-off @ `0a265cb`, (2) Gitar settled verdict (re-reviewing), (3) CI green; recommended executor vectorise `_corroboration_counts` now. Posted governance verify. Watcher `b2yh64d8z`. |
| 21 | 2026-06-12T~11:3x Z | **AC-CD25 amendment #116 MERGED → main `bb7ed8e`; spec-author STANDING auto-continue auth** | OV-AM-2 ✓; OV-A2-19 fully resolved | **VERIFIED from ground truth:** main `4f95414`→`bb7ed8e` (PR #116 doc-amend AC-CD25). main:CODE_SPEC §18 AC-CD25 now carries ratified cosine ≥0.90 text (distinct source_host, own-source-counted, reuses embeddings) — matches ratified substance (**OV-AM-2 ✓**; #116 doc-only/clean). **STANDING AUTHORIZATION (authenticated, this convo):** intermediate slices A2→E2 **auto-merge on 3-sign-off + three-layer green, NO per-slice spec-author go-ahead** (confirms override-window=0 cycle-fact); **F1 ALONE** needs explicit spec-author auth + end-of-execution audit before final merge. **A2 PHASE-4 now live:** executor rebases #115 onto `bb7ed8e` + re-lands cosine as execution-against-merged-spec; A2 head still `57f09ca` (push pending). **Phase-4 re-verify checklist:** (a) rebased onto `bb7ed8e`; (b) cosine CODE matches merged AC-CD25 substance; (c) **#115 diff vs main has NO CODE_SPEC edit** (amendment is on main now — OV-A2-19 fully RESOLVED, cosine is legit execution not in-PR fold); (d) **vectorised** (clears Gitar O(N²) ⚠️ — assess at re-verify); (e) blind-SSRF retained (OV-A2-18); (f) models/migration/cost/scope hold. → execute A2 merge (squash, --delete-branch) on 3-lane green convergence. Then A3. Watcher `b2yh64d8z`. |
| 17 | 2026-06-12T~09:2x Z | A2 `758795a`; auditor content review | 0/0 (co-reviewer convergence) | **3-party alignment @ `758795a`.** Auditor: CA-A2-2r (blind-SSRF) ✅ RESOLVED ≡ OV-A2-18; CA-A2-5 ⏸ HELD-SURFACED ≡ OV-A2-19 (AC-CD25 cosine amendment in exec PR; A2 now **ratification-class (ii), not NORMAL**; auditor holds final-marker, no conditional clearance). Gitar `758795a`: blind-SSRF resolved; remaining = non-blocking (O(N²) cosine perf, nullable-embedding, run-local, DNS-rebind low-exposure). **CODE otherwise ready** (both reviewers); sole blocker = spec-author OV-A2-19 ruling I surfaced (Option A/B). Still holding; NOT merging. No reply (corroborates). |
| 13 | 2026-06-12T~07:20Z | A2 #115 head still `442f00f` (no OV-A2-18 fix yet); code-auditor WITHDREW their `442f00f` marker (`13cd36a`) | 0 dropped / 0 added (no-change wake — co-reviewer alignment) | **Divergence RESOLVED — three-party alignment.** Code-auditor withdrew their premature `442f00f` content final-marker (CA-A2-2r ≡ OV-A2-18 ≡ Gitar blind-SSRF; owned conflating storage-closure with request-bounding). Now ALL three (overseer + auditor + Gitar) hold the blind-SSRF blocker; convergence NOT reached; gate held. **CA-A2-1 confirmed resolved in BOTH lanes** (exact-text = ratified-deferred A2 v1 floor per detail-plan §2; no spec-author surface; executor to soften "pending ruling" wording non-blocking). **Pending (NON-blocking):** I surfaced the corroboration point to the spec author in-session (plain text) for an optional definitive ruling; does not gate A2. Awaiting executor's OV-A2-18 fix → re-verify all at new SHA. No GitHub reply (auditor note corroborates; asks nothing). |

---

## Pre-registered governance positions — Slice A3 (corpus retrieval helper + hybrid refresh)

**Recorded 2026-06-12T~15:2xZ, BEFORE the A3 execution draft exists** (anti-anchoring, role §4.2/§6).
Basis: ratified v1.9 spec on main `2588f39` (AC-CD7 / SPEC §8.9 / AC-CD25 retrieval) + detail-plan
`plans/2026-06-09-autonomous-content-generation-detail.md` §3 (Slice 3). Deps satisfied: **A1
`4f95414` + A2 `2588f39` merged** ✓. A3 = (1) `retrieve_corpus_for_topic` (cosine_top_k over
`CorpusChunk`, returns hits *with* authority_tier/score for B2) + (2) hybrid refresh: per-topic
on-demand fn + admin on-demand **domain fn** + **weekly backstop cron `corpus.refresh`**.

**⚑ Reframing (verified from ground truth, supersedes detail-plan §3.3):** the detail-plan (written
pre-PR-A) surfaced A3 as *"BLOCKED pending its own ratification surfaces: SPEC §8.9 cron-count
amendment + AC-CD7 body change."* **That amendment is ALREADY RATIFIED in v1.9** — `CODE_SPEC:110/342/650-660`
say **"nine crons (final count v1.9)"**, authored-complete in PR-A *precisely* so A3+D4 don't each
re-amend ("amend-once across the A3+D4 touchers"). So **A3 has NO live spec-author ratification
surface** — it is **pure execution against the ratified nine-count anchor**. Risk shifts to amend-once
+ mirror-count consistency.

### Lane 1 — Amend-once / ratified-anchor fidelity (the A3 crux)
- **OV-A3-1 — do NOT re-amend the cron-count anchor.** SPEC §8.9 + AC-CD7 body are **ratified-frozen at
  nine (v1.9)**. A3 must implement `corpus.refresh` *against* that count; **any edit to the SPEC §8.9
  bullet-count or the AC-CD7 anchor body in the A3 PR = amend-once violation** (duplicate of the
  already-ratified PR-A amendment). A3 touches CODE + code-doc mirrors, never the ratified anchor body.
- **OV-A3-2 — spec-vs-code cron-count mirror reconciliation.** On main the **SPEC says nine** but
  **`beat_schedule.py` still has SEVEN** entries + docstring "the seven crons". A3 adds `corpus.refresh`
  to the code. **The `drive_rag.ingest` disposition is the sharp point:** ratified `CODE_SPEC:652`
  reads *"corpus.refresh (A3) **replaces** drive_rag.ingest"* (NS-1=retire-Drive-ingest reading) → A3
  **removes drive_rag.ingest + adds corpus.refresh** (code stays 7 after A3; D4 later adds its 2 → 9).
  Watch: (a) does A3 honor the "replaces" framing or just add (→8, contradicting the ratified replace)?
  (b) is the mirror sweep **complete + consistent** across ALL surfaces (`beat_schedule.py:1-4` docstring
  + ASCII table, `CODE_SPEC:110/337`, `ROADMAP:193/196`, `SPEC §8.9`, `CHECKLIST` P11) — no surface left
  saying "seven", none prematurely claiming D4's two crons exist? This is the #106-G9 mirror-sweep parallel.
  A half-swept or count-inconsistent state = finding.
- **OV-A3-3 — beat entry shape + cadence.** `corpus.refresh` = ONE flat-dict entry → `refresh_corpus_all`,
  **weekly** (ruling 6 **ratified** — cadence shape NOT re-surfaceable; only day/hour is a default, e.g.
  Mon 08:00 extending the daily-offset convention). Must not alter the AC-CD7 flat-dict structure.
- **OV-A3-4 — admin on-demand is a domain fn, NOT a beat entry.** Per AC-CD7 (bootstrap = "idempotent
  enqueued job, admin-triggered", not a cron), A3's admin-refresh is a domain/enqueued fn; **only the
  weekly backstop is a cron.** Adding admin-refresh as a 2nd cron = count inflation = finding.

### Lane 2 — Inter-slice contract integrity / reuse
- **OV-A3-5 — retrieval reuses `cosine_top_k` unchanged.** `retrieve_corpus_for_topic` = `cosine_top_k`
  (`drive_rag.py:196`) over `CorpusChunk.embedding`, returns `[{source_doc_ref, source_host, chunk_text,
  authority_tier, authority_score}]`, same fail-soft contract as `retrieve_for_generation` (empty topic→[]
  no embed; empty corpus→[] one embed+cost-audit; embed raises→[] WARN). **No ranker reinvention, no new
  `Operation`.** A3 = the **2nd consumer of `cosine_top_k`** → **strengthens the NS-1 relocation
  requirement** (if Drive retires, cosine_top_k/chunk_document/content_hash **relocate**, not delete).
- **OV-A3-6 — refresh reuses A2 `acquire_for_topic`.** All 3 triggers wrap A2's idempotent
  `acquire_for_topic` (dedup by `(source_host, content_hash)`). No re-implemented acquisition.
- **OV-A3-7 — stable forward surface for B2.** `retrieve_corpus_for_topic` is the primitive B2 grounds
  against — signature/return shape must be stable (B2 authority-weights on the returned tier/score).
- **OV-A3-8 — no regression of A1/A2.** Must not touch `source_authority`, the `web_search` seam, or A2's
  `CorpusChunk`/`acquire_for_topic` contracts. No new migration expected (A3 reads CorpusChunk, adds no col).

### Lane 3 — Scope fence
- **OV-A3-9 — scope.** A3 ships: retrieval helper + 3 refresh fns + the weekly `corpus.refresh` cron +
  the mirror sweep. **NOT in A3:** generation grounding (B2), gap-detection/catalogue-health crons (D4),
  admin-override *endpoint*/FE (Stage E — domain fn only), dashboard, `Operation` enum/router/FE.
- **OV-A3-10 — DS3-a refresh-target set (watch, not pre-blocked).** Weekly backstop target = active
  catalogue subjects/pills (detail-plan build-call DS3-a). **Non-surfaced UNLESS** A3 conflates it with
  D3/NS-4 catalogue-health (the backstop = clock-refresh existing catalogue topics; the health-check =
  D3 proactive gap trigger — must stay distinct). If A3 blurs the boundary → surface.

**Merge class:** NORMAL (executes against ratified v1.9; amends no spec, mints no anchor) — auto-merge on
3 sign-offs + three-layer green + zero override window (spec-author standing auto-continue auth; A3 is
intermediate, not F1). Watcher `bj0s51xvw` (broad arm catches the A3 branch off `2588f39`). | **A3 DRAFT OPENED = PR #117 @ `d61df768`** (branch `content-gen-a3-corpus-refresh`, off `2588f39`). **Governance review CLEAN — all OV-A3-1..10 Confirm, 0 findings:** amend-once ✓ (no CODE_SPEC/SPEC/DECISIONS edit — implements vs ratified nine-count); cron-count ✓ (adds corpus.refresh built 7→8, defers drive_rag.ingest removal to NS-1 slice [correct], mirror sweep COMPLETE+CONSISTENT incl engagement.py de-ordinalize + the count test→8); reuse ✓ (cosine_top_k + acquire_for_topic, no new Operation); scope ✓ (no models/migration/routers/FE/gen); DS3-a catalogue-pills backstop distinct from D3. Posted governance assessment (1/3 sign-offs); SEAL/merge pending auditor + Gitar + CI at d61df768. **Subscribed PR #117.** Watcher `bj0s51xvw`. **GATE HELD (r1):** Gitar @ `d61df768` = ⚠️ Changes-requested, 2 findings on `refresh_corpus_all` — (1) ⚠️ per-pill isolation (one pill's uncaught error aborts the weekly backstop + skips commit; valid robustness, no autoretry); (2) 💡 duplicate pill-name dict-collapse (depends on Pill.name uniqueness). **CONTENT lane (executor fixes, auditor reviews) — NOT governance; I do not author code in the exec PR.** Governance stays CLEAN (OV-A3-1..10 Confirm). Merge held on Gitar red; will re-verify governance + execute on executor's round-2 fix → 3 sign-offs + three-layer green at new SHA. Holding; not merging.

---

### A3 correction log — OV-A3-11 (2026-06-12T~15:35Z)

**▲ GOVERNANCE-INTEGRITY CORRECTION — I WITHDREW my OV-A3-2 cron-count Confirm.** The auditor's
**CA-A3-1** is correct and I waved it through: I verified the executor's 8-count mirrors were
*internally consistent* but **missed the spec-contract-fidelity half** (count vs the ratified
enumeration — my lane). VERIFIED ratified text: **AC-CD7** `CODE_SPEC:651-653` ("`corpus.refresh`
replaces `drive_rag.ingest` — 1:1, **net 0**") + **SPEC §8.9** `:525` ("**seven registered**
beat_schedule tasks, `drive_rag.ingest`→`corpus.refresh`, plus the two D4 additions"). The executor's
**7→8 is a SPEC DEVIATION**: 8 ≠ ratified seven; → **ten** after D4 (never the canonical nine); and
**no NS-1 slice exists among the 14** to fix it. I conflated §7.3's *Drive-code* deferral
(module/table/deps → NS-1) with the *beat-entry swap* (AC-CD7 assigns the swap to **A3**).

**OV-A3-11 [Push-back · BLOCKING ≡ CA-A3-1]:** A3 removes the `drive_rag.ingest` **beat entry**
(net-0 → **seven registered**; `assert len==7`; fix ALL count-mirrors back to "seven registered /
nine canonical"); the Drive *code* (`drive_rag.py`/`drive_chunk`/`ingest_drive_folder`/google deps)
stays **dormant** for the separate NS-1 code-removal step. Also resolves the **AC-D22** conflict (the
retired Drive ingest must stop firing daily 03:00).

**Gate held at `d61df768`** on three converging items, all fixable in one re-push:
- **OV-A3-11 ≡ CA-A3-1** — cron-entry swap (governance + content).
- **CA-A3-2 ≡ Gitar #1** — `refresh_corpus_all` per-pill try/except isolation (medium; content lane).
- **CA-A3-3 ≡ Gitar #2** — dup-pill-name telemetry undercount (low; key by `pill.id`; content lane).

My other OV-A3 positions (retrieval reuse / fail-soft / scope / amend-once-on-anchors) **stand**.
**Lesson recorded:** for any count-invariant slice, verify the built count **against the ratified
enumeration**, not merely internal mirror self-consistency. Awaiting executor round-2 → re-verify the
full OV-A3 set + 3-sign-off + three-layer green at the new SHA → execute merge (auto-continue, A3≠F1).

### A3 round-2 `4472a1a` (2026-06-12T~15:39Z)
**OV-A3-11 ≡ CA-A3-1 → ✅ RESOLVED.** Executor did the net-0 beat-entry swap: `drive_rag.ingest`
schedule entry removed + `corpus.refresh` added → **seven registered**; `test_..._exactly_seven_entries`
(`assert len==7`, asserts ingest not scheduled); registry test `_all_seven`; docstring+ASCII+CHECKLIST
all consistent at seven/nine; Drive code dormant for NS-1. Amend-once clean; rebased on `2588f39`.
**Governance lane CLEAN — all OV-A3 Confirm.**
**Gate STILL HELD (content lane):** round-2 left `corpus_builder.py` untouched → **CA-A3-2 ≡ Gitar #1**
(`refresh_corpus_all` per-pill try/except isolation, ⚠️ medium) + **CA-A3-3 ≡ Gitar #2** (dup-pill-name
telemetry, low) remain open → Gitar ⚠️ changes-requested → green leg red → NOT merging. Governance no
longer blocks; convergence needs executor's `refresh_corpus_all` fix + auditor content sign-off + Gitar
green at one SHA → I re-verify + execute (auto-continue, A3≠F1). Posted round-2 re-verify to #117.

### A3 round-3 `a60f41b` — governance SEALED (2026-06-12T~15:43Z)
**CA-A3-2 ≡ Gitar #1 ✅** (refresh_corpus_all per-pill try/except → log+0+continue) + **CA-A3-3 ≡ Gitar #2 ✅**
(keyed by str(pill.id)); +48 tests. OV-A3-11 cron-count NOT regressed (still 7); amend-once clean; on main `2588f39`.
**Posted `Status: governance SEAL — code-overseer (Slice A3) @ a60f41b` (1/3).** **Gitar ✅ Approved** (2 resolved).
**Holding on:** (a) **CI** still in_progress (checks/e2e; mergeable=unstable; migration-chain+docker-build green);
(b) **auditor round-3 content final-marker** (pending — last was round-2). On both → 3 sign-offs + three-layer
green @ a60f41b → execute squash-merge (auto-continue, A3≠F1). Armed `bzsrjciww` + subscribed #117.

### A3 CONVERGED @ a60f41b — merge pending last e2e (2026-06-12T~15:45Z)
**3 SIGN-OFFS @ `a60f41b`:** executor (ready) + **code-auditor content final-marker** (`77fedcd`, CA-A3-1/2/3
all resolved) + **overseer governance SEAL**. **Gitar ✅ Approved.** CI: checks/migration-chain/docker-build
+ 1 e2e all SUCCESS; **one `e2e` job still in_progress** (started 15:45:07) → `mergeable_state=unstable`.
**MERGE-READY the instant that e2e passes** (→ mergeable=clean). No shell token + private repo ⇒ can't
poll CI from a Monitor; CI-success not webhooked (failure IS). **ACTION ON WAKE:** re-check
`get_check_runs`/mergeable; if clean → flip draft→ready + squash-merge #117 (--delete-branch) → verify
on main → pre-register B1 + re-arm → auto-continue (A3≠F1, no spec-author gate). Armed `bzsrjciww` + #117 sub.

### A3 MERGED ✅ — squash f0a59e8 (Slice 3/14; STAGE A COMPLETE) — 2026-06-12T~15:49Z
Executed flip-ready + squash-merge #117 @ `a60f41b` → `f0a59e8`; A3 branch auto-deleted; unsubscribed.
**Gate:** 3 sign-offs @ a60f41b (executor + auditor content-final `77fedcd` + overseer seal) + three-layer
green (all CI checks/migration-chain/docker-build/e2e success + Gitar ✅ Approved + mergeable=clean) +
override-window zero (auto-continue auth; A3 intermediate). **Verified on main:** beat_schedule = **7 entries**
(cron swap landed: drive_rag.ingest removed, corpus.refresh added). **Highlight:** OV-A3-11/CA-A3-1 — the
3-party gate caught a cron-count spec-fidelity miss in my lane (I'd confirmed internal-mirror consistency but
not count-vs-ratified-enumeration); withdrew + corrected; converged with auditor; executor fixed in 2 re-pushes.
main chain: A1 `4f95414` → amend `bb7ed8e` → A2 `2588f39` → A3 `f0a59e8`. **NEXT: B1** (Stage B start) off
`f0a59e8` — auto-continue. Pre-registering B1 positions next. Watcher `bzsrjciww` (broad arm catches B1).

---

## Pre-registered governance positions — Slice B1 (`Operation.pill_generation` mint + provider/stub wiring)

**Recorded 2026-06-12T~15:50Z, BEFORE the B1 draft exists** (anti-anchoring §4.2/§6). Basis: ratified
v1.9 on main `f0a59e8` (AC-D1/AC-CD8/AC-D12/AC-D29/SPEC §6) + detail-plan §4. B1 mints the FIRST new
Operation of the cycle (`pill_generation`) + prompt module v1.0.0 + provider/stub wiring + the op-keyed maps.
**B1 does NOT require Stage A merged** (imports nothing from Stage A; the A→B dep lands at B2). A1/A2/A3
merged anyway.

**⚑ Reframing (verified, supersedes detail-plan §4.3 "BLOCKED pending G1/G7b ratification") — SAME pattern
as A3:** the ops-count amendment **seven→nine** (AC-D1 Impl `:96`, AC-CD8 `:63`, AC-D12 `:346/350`, SPEC §6
`:308/410`) was **authored COMPLETE in v1.9 PR-B** (merged) — anchors read "**nine canonical**; pill_generation
wiring lands with B1 execution". The v1.0.0→v1.1.0-at-B2 version trajectory (G7b) is ratified in the AC-D29
body (`:762`). **So G1 + G7b have NO live ratification surface — B1 is PURE EXECUTION.** Risk = amend-once +
mirror consistency (NOT a spec-author surface).

### Lane 1 — Amend-once / ratified-anchor fidelity (the B1 crux; A3-lesson applied)
- **OV-B1-1 — do NOT re-amend the ratified ops-count anchors.** AC-D1 Impl / AC-CD8 / AC-D12 / SPEC §6 are
  **ratified-frozen at "nine canonical" (v1.9 PR-B)**. B1 implements the `pill_generation` wiring *against*
  them. **Any edit to those anchor bodies / numerals in the B1 PR = amend-once violation** (duplicate of the
  ratified PR-B amendment). G1 is already ratified; do NOT re-surface it as a spec-author item.
- **OV-B1-2 — ops-count mirror consistency (built EIGHT named of canonical NINE).** Enum on main = 7 named +
  `embed` (8 members); B1 adds `pill_generation` → **8 named + embed** (built); `content_self_review` (C1) is
  the 9th named. **Verify the count vs the ratified enumeration, not just internal self-consistency (the exact
  trap the auditor caught at A3/OV-A3-2).** B1 sweeps the CODE/built-state mirrors consistently: `provider.py`
  enum-docstring (`:122` "seven … plus embed" → updated), CODE_SPEC §7 / ROADMAP / CHECKLIST built-state
  rows — none left stale, none prematurely claiming C1's `content_self_review`.
- **OV-B1-3 — ALL FOUR strict op-keyed maps wired (KeyError/floor floors).** `pill_generation` MUST be added
  to every one or it KeyErrors / fails a set-equality floor: (1) `resolve_model` map (`provider.py:418-427`,
  strict `[op]` at `:427`); (2) `_MAX_OUTPUT_TOKENS` (`anthropic.py:61`, strict at `:146`); (3) `OP_TO_METHOD`
  (`cost.py:132`, `set(OP_TO_METHOD)==set(Operation)` floor); (4) `_REGISTRY` (`prompts/__init__.py:37`,
  `registered_operations()`). PLUS `_ANTHROPIC_DEFAULT_OPS` + the `StubAIProvider.generate` branch + the
  `anthropic_model_pill_generation` config field (AC-CD18 env-default, sibling to `pill_proposal`). **A
  half-wired op is the signature B1 failure mode** — check all four maps explicitly.
- **OV-B1-4 — version pinned at v1.0.0.** `pill_generation.py VERSION="1.0.0"`, core topic→N schema ONLY;
  `grounding_refs`/provenance fields **absent** (B2 scope; B2 bumps to v1.1.0 per ratified G7b/AC-D29). No
  speculative/TBD fields (doc-hygiene). Landing fields B1 can't populate = finding.

### Lane 2 — Inter-slice contract / reuse
- **OV-B1-5 — mint is a generate-family op, Anthropic-default; reuses the provider seam.** Routes through
  `AIProvider.generate` (AC-D12 primary content); joins `_ANTHROPIC_DEFAULT_OPS`; mirrors `pill_proposal.py`
  module shape. No new provider method, no router/domain layer.
- **OV-B1-6 — `pill_proposal` refiner kept untouched (G7a).** B1 does NOT modify/retire the one-in-one-out
  refiner (`pill_proposal.py` v1.0.0). Distinct op (different input AND output contracts) — overload = finding.
- **OV-B1-7 — stable forward surface + determinism.** `resolve_provider(Operation.pill_generation).generate`
  is the primitive B2/D3 call; the stub must be **deterministic** (seeded by topic+target_count → byte-identical
  drafts) for AC-CD15 zero-network tests; stub returns `prompt_version="0.0.0-stub"`, zero tokens, cost 0.0.

### Lane 3 — Scope fence
- **OV-B1-8 — scope.** B1 ships ONLY: the prompt module (v1.0.0) + enum/routing/stub + the 4 maps +
  `_ANTHROPIC_DEFAULT_OPS` + config field + AC-CD15 test. **NOT in B1:** corpus grounding / `GenerationProvenance`
  (B2); N-row `processing_tasks` persistence / fan-out / cost-share (B3); self-review / auto-publish gate
  (C1/C2); endpoint / FE; any domain/router layer. A models.py/migration edit in B1 = scope breach (the
  `GenerationProvenance` table is B2). Config field + one prompt module fold into the handover (the #106
  OV-S1.11 ruling) — absorbable AC-CD18 addition, not a separate spec PR.

**Merge class:** NORMAL (executes against ratified v1.9; amends no spec, mints no anchor — the op mint is
*pre-ratified*) — auto-merge on 3 sign-offs + three-layer green + zero override window (standing auto-continue;
B1 intermediate, not F1). Watcher `bzsrjciww` (broad arm catches the B1 branch off `f0a59e8`).

### B1 DRAFT = PR #118 @ 9890b44 — governance CLEAN (2026-06-12T~16:03Z)
On-wake pairing vs OV-B1-1..8 = **all Confirm, 0 findings.** amend-once ✓ (no DECISIONS/SPEC/CODE_SPEC
edit — ops-count pre-ratified nine in PR-B); ops-count mirror ✓ (provider.py docstring "nine canonical /
eight built", no stale seven — A3 trap avoided); **ALL FOUR strict maps wired** ✓ (resolve_model,
_MAX_OUTPUT_TOKENS=4000, OP_TO_METHOD, _REGISTRY) + _ANTHROPIC_DEFAULT_OPS + stub branch + config field —
the 3 construction-oracle floors stay green + tested (half-wired-op trap closed); v1.0.0 grounding-deferred ✓;
pill_proposal untouched ✓; deterministic sha256-seeded stub ✓; scope clean (no models/migration/domain/
router/FE — GenerationProvenance deferred to B2) ✓. **Executor independently reached the same A3-pattern
framing** ("ops-count already ratified by PR-B; amends no spec"). Posted governance assessment (1/3 sign-offs);
SEAL/merge pending auditor + Gitar + CI @ 9890b44. **Subscribed PR #118.** Watcher `bzsrjciww`. Slices: A1/A2/A3
merged (3/14); B1 in review.

### B1 CONVERGED @ 9890b44 — merge pending e2e (2026-06-12T~16:05Z)
3 SIGN-OFFS (executor + auditor content-final `53d47c8` + overseer seal) + **Gitar ✅** + checks/migration-chain/
docker-build success; **2 e2e jobs in_progress** → mergeable=unstable. MERGE the instant mergeable=clean.
ACTION ON WAKE: re-check get/get_check_runs; if clean → flip ready + squash-merge #118 (--delete-branch) →
verify on main → pre-register B2 + auto-continue (B2 = first A→B grounding dep; B1≠F1). Armed `bzsrjciww` + #118 sub.

### B1 MERGED ✅ — squash f346cc2 (Slice 4/14) — 2026-06-12T~16:07Z
Flip-ready + squash-merge #118 @ `9890b44` → `f346cc2`; branch deleted; unsubscribed. Gate: 3 sign-offs
(executor + auditor content-final + overseer seal) + three-layer green (all CI + Gitar ✅ + mergeable=clean) +
override-window zero. **First-round clean both lanes** (executor carried the A3 ops-count discipline cleanly —
all 4 strict maps wired, oracles green, no anchor re-amend). `Operation.pill_generation` now on main.
main: A1 `4f95414`→amend `bb7ed8e`→A2 `2588f39`→A3 `f0a59e8`→B1 `f346cc2`. **NEXT: B2** (Slice 5 — grounded
generation + GenerationProvenance; first A→B dep: generation grounds vs the A2 corpus). Pre-registering B2.

---

## Pre-registered governance positions — Slice B2 (corpus-grounded generation + provenance chain)

**Recorded 2026-06-12T~16:08Z, BEFORE the B2 draft exists** (anti-anchoring §4.2/§6). Basis: ratified v1.9
on main `f346cc2` (AC-D29 `DECISIONS:756-762`, AC-D22, AC-CD25, AC-D28) + detail-plan §5. B2 = the first
**A→B dependency**: `pill_generation` learns to ground vs the A2 corpus + emits a per-draft provenance chain.
Deps: **A2+A3+B1 merged** ✓ (CorpusChunk + retrieve_corpus_for_topic + pill_generation op).

**⚑ Reframing (verified, supersedes detail-plan §5.3 "BLOCKED pending ratification") — SAME pattern as
A3/B1:** the "new AC-D (provenance)" = **AC-D29, already MINTED in v1.9 PR-B**; NS-3 **ruled per-assertion**;
AC-D22 §6.5 reframe **folded complete in PR-A**; G7b (v1.1.0 at B2) **ruled**; DS2-b **ruled cosine (#116)**.
**B2 has NO live ratification surface — PURE EXECUTION** against ratified AC-D29. Risk = amend-once +
schema-fidelity + B2/B3 split (NOT a spec-author surface).

### Lane 1 — Amend-once + ratified-schema fidelity (the B2 crux)
- **OV-B2-1 — do NOT re-amend AC-D29 / AC-D22.** Ratified-frozen in v1.9 (PR-A/PR-B). B2 implements the
  model+migration+fn against them; **any DECISIONS/SPEC anchor-body edit in the B2 PR = amend-once violation**.
- **OV-B2-2 — `GenerationProvenance` schema MUST match the ratified AC-D29 column set** (`DECISIONS:758`):
  `id, tenant_id, draft_ref, claim_ref, corpus_chunk_id (FK→CorpusChunk), source_host, authority_tier,
  authority_score, created_at`; **indexed on `source_host` + `corpus_chunk_id`** (the E2 per-source-rollback
  query keys); **RELATIONAL** (one row per (assertion, grounding-chunk)), NOT a JSONB blob. **`claim_ref` =
  per-assertion (NS-3 ratified)** — not per-draft-set. `batch_id` is the **B3** fan-out column (B2 may add it
  nullable or defer to B3 — watch consistency, but it must NOT be populated by B2). A column-set deviation
  from ratified AC-D29 = spec-contract break (the A3-lesson: verify against the ratified spec, not just
  internal coherence).
- **OV-B2-3 — migration up/down clean** (2nd workstream migration after A2's `0009`; offline `--sql`
  round-trip; IVFFlat-adjacent indices on source_host/corpus_chunk_id). FK→CorpusChunk must resolve.
- **OV-B2-4 — prompt v1.1.0 bump (G7b lands).** `pill_generation.py` VERSION → `1.1.0`: add `{corpus_context}`
  slot + ground-and-cite instruction + `grounding_refs:[str]` output field; persisted `prompt_version="1.1.0"`
  on B2-grounded drafts. Mirrors `generation.py:67` grounding precedent.

### Lane 2 — Inter-slice contract / reuse / the B2/B3 split
- **OV-B2-5 — reuse A3 retrieval + no new Operation.** `generate_grounded_drafts(db, *, topic, target_count)`
  calls A3's `retrieve_corpus_for_topic` → renders authority-tagged corpus context → `resolve_provider(
  pill_generation).generate` → writes provenance. **No new `Operation`** (provenance is a generator byproduct,
  AC-CD8 floor `DECISIONS:762`). `AIProvenanceMixin` (per-row *cost*) is orthogonal — do NOT overload it with
  the claim→source chain. Fail-soft: empty corpus → `grounding_refs:[]` + general-knowledge fallback.
- **OV-B2-6 — the B2/B3 split is SHARP (auditor PS-B2 watch).** B2 owns: the grounding prompt-bump, the
  grounded-generation fn, the **provenance model + writer**, writing provenance for the drafts the primitive
  returns. **NOT in B2 (= B3):** N-draft **fan-out**, `ProcessingTask` N-row **persistence**, **cost-share**
  (`record_provenance_share` across N), per-band decomposition (G3), `batch_id` population. Any reach into
  fan-out/persistence/cost-share = scope breach.
- **OV-B2-7 — DS2-b is an ADDITIVE READ, not a baked column.** Corroboration (ruled cosine/Option-2, #116)
  is a downstream per-`claim_ref` aggregate **over** `GenerationProvenance` rows — B2 must **NOT** add a
  corroboration column (no schema change; additive read only).

### Lane 3 — Scope fence
- **OV-B2-8 — scope.** B2 ships ONLY: `pill_generation` v1.1.0, `generate_grounded_drafts`,
  `GenerationProvenance` model + migration + indices, the stub extension (echo injected corpus refs →
  `grounding_refs` + deterministic offline provenance rows), AC-CD15 zero-network tests. **NOT in B2:** B3
  fan-out/persistence/cost-share; C1-C2 self-review/confidence/gate; endpoint/FE; D3 trigger; E
  dashboard/rollback (though E2 queries B2's provenance table). `pill_proposal` + Drive untouched.

**Merge class:** NORMAL (executes against ratified v1.9; amends no spec, mints no anchor). Auto-merge on 3
sign-offs + three-layer green + zero override window (standing auto-continue; B2 intermediate, not F1).
Watcher `bzsrjciww` (broad arm catches the B2 branch off `f346cc2`).

### B2 = PR #119 @ 9e275f3 — §6.8 RULING + governance CLEAN (2026-06-12T~19:25Z)
**Spec-author ratified Option-1 (per-assertion structured grounding_refs) via authenticated channel
(direct origin, this conversation); delegated the §6.8-amendment call to me.** **MY §6.8 RULING:
PERMISSIVE → no amendment; Option-1 lands directly in B2 (NORMAL class).** Reasoning: (1) ratified AC-D29
`DECISIONS:758` mandates per-assertion ("one row per (assertion, grounding-chunk); claim_ref = per-assertion
id") and §6.8 opens "Per AC-D29" (subordinate); (2) §6.8 prose is per-claim ("grounded each claim";
SPEC:358 "per-assertion provenance chain"); (3) the "source_doc_refs the draft cited" phrase is descriptive
of content, not a normative flat shape — structured [{claim, source_doc_refs}] still carries it per-claim.
A flat shape would UNDER-implement AC-D29. Posted to #119; cite this conversation as authenticated origin.
Not ambiguous → no surface-back. **Governance review OV-B2-1..8 = ALL CONFIRM, 0 findings** (full diff via
MCP get_files): amend-once ✓ (no DECISIONS/SPEC/CODE_SPEC); **schema matches ratified AC-D29 column-for-column**
(id/tenant_id/draft_ref/claim_ref/corpus_chunk_id FK CASCADE/source_host/authority_tier/authority_score/
batch_id nullable/created+updated; indexed source_host/corpus_chunk_id/draft_ref — A3-lesson check passed);
migration 0010 rev 0009 up/down clean (36 tables); prompt v1.1.0 structured grounding_refs; reuse A3
retrieve + pill_generation, no new op, empty-corpus fallback; **B2/B3 split sharp** (returns drafts+ai_result;
no fan-out/persist/cost-share/batch_id-populate); DS2-b additive (no corroboration col); scope clean (one
additive: retrieve_corpus_for_topic now returns corpus_chunk_id — B2 is first consumer). Posted governance
SEAL (1/3); subscribed #119; pending auditor + Gitar + CI @ 9e275f3. Watcher `b5qaqwwoj`. Slices: A1/A2/A3/B1
merged (4/14); B2 in review.

### B2 round-2 4c9d2b4 — governance re-SEALED (2026-06-12T~19:29Z)
Executor folded both Gitar 💡 (content lane): dedup per-claim source_doc_refs (`dict.fromkeys`) → no dup
(claim_ref, corpus_chunk_id) rows (E2 count integrity); WARN on non-empty grounding_refs but 0 rows written
(observable mismatch); +94 tests. **Amend-once clean** (no anchor edit 9e275f3..4c9d2b4); scope localized to
generation.py — schema/migration/prompt/B2-B3-split untouched. **OV-B2-1..8 all still hold → re-SEAL @
4c9d2b4** (prior seal @ 9e275f3 re-staled by the push). **Gitar ✅ Approved @ 4c9d2b4** (both resolved).
**3 sign-offs must converge at 4c9d2b4:** overseer ✓; auditor content sign-off pending @ 4c9d2b4 (re-staled
if they signed 9e275f3); CI pending @ 4c9d2b4. On 3 + green → auto-merge B3. Watcher `b5qaqwwoj` + #119 sub.

### B2 CONVERGED @ 4c9d2b4 (content 68de03d) — merge pending CI (2026-06-12T~19:31Z)
**3 sign-offs on the B2 content** (68de03d ≡ 4c9d2b4 minus wake-log): executor + **auditor content-final
@ 68de03d** + **overseer re-SEAL @ 4c9d2b4** + **Gitar ✅**. CI on the 4c9d2b4 push: checks/migration-chain/
docker-build/Gitar success; **1 e2e queued + 1 checks in_progress** → mergeable=unstable. MERGE the instant
mergeable=clean (merge 4c9d2b4 — carries the 3-way-signed content + the executor wake-log; auditor's 68de03d
sign-off covers the identical code). ACTION ON WAKE: re-check get/get_check_runs; clean → flip ready +
squash-merge #119 → verify → pre-register B3 + auto-continue (B2≠F1). Armed `b5qaqwwoj` + #119 sub.

### B2 MERGED ✅ — squash 30cde56 (Slice 5/14) — 2026-06-12T~19:34Z
Flip-ready + squash-merge #119 @ `4c9d2b4` (content `68de03d`) → `30cde56`; branch deleted; unsubscribed.
Gate: 3 sign-offs on content (executor + auditor content-final @ 68de03d + overseer re-seal @ 4c9d2b4) +
three-layer green (all CI + Gitar ✅ + mergeable=clean). **§6.8 ruling (permissive→no amendment, my delegated
call) held through merge; Option-1 per-assertion provenance landed.** `GenerationProvenance` on main.
main: …→B1 `f346cc2`→B2 `30cde56`. **NEXT: B3** (Slice 6 — N-draft fan-out + ProcessingTask N-row persistence
+ cost-share `record_provenance_share` + per-band G3 + batch_id population; reuses B2's provenance writer).
Pre-registering B3.

---

## Pre-registered governance positions — Slice B3 (N-draft fan-out + persistence + cost-share)

**Recorded 2026-06-12T~19:35Z, BEFORE the B3 draft exists** (anti-anchoring §4.2/§6). Basis: ratified v1.9
on main `30cde56` (AC-D29, AC-CD7, AC-CD8, AC-D9) + detail-plan §6. B3 = fan-out/persistence/cost-share
around B2's grounded-generation primitive: one gen call → **N `processing_tasks` rows**, each with its
provenance + 1/N cost-share + a shared `batch_id` (E2 per-batch rollback key). Dep: **B2 merged** ✓ (transitively
A2+A3+B1).

**⚑ Reframing (verified, supersedes detail-plan §6.3 "BLOCKED pending G3") — SAME pattern A3/B1/B2:** the
only B3 surface, **G3 (per-band decomposition), is ALREADY RULED min/max-only** in ratified SPEC §6.8
(*"per-band decomposition is min/max-range only — no richer breakdown"*, AC-D9 axis). **B3 has NO live
ratification surface — PURE EXECUTION**, mints no anchor, **no new migration** (DS6-a/b lean). Risk = amend-once
+ AC-CD8 spend invariant + the B2/B3 seam + scope.

### Lane 1 — Amend-once / ratified fidelity
- **OV-B3-1 — G3 = min/max-only (ratified); do NOT add a per-band field.** Each draft carries
  `available_difficulty_min/_max` (AC-D9) ONLY — no per-band band-distribution. A per-band field would be an
  **unratified class-(ii) amendment** to the generation output contract → must surface to the spec author,
  NOT bake. (The lean min/max is the ratified position.) No DECISIONS/SPEC/CODE_SPEC edit.
- **OV-B3-2 — no new anchor / no migration.** `_pill_generation_spend` is an **absorbable AC-CD8-pattern
  mirror** of `_pill_proposal_spend` (`cost.py:197`), NOT a new anchor. `enqueue_generated_drafts` is a domain
  fn. `batch_id` rides `ProcessingTask.payload` (JSONB) + the **existing nullable `GenerationProvenance.batch_id`**
  (B2's `0010`). Drafts reuse `processing_tasks` (`task_name="pill_generation"`, AC-CD7/DS6-b). **A models.py
  schema change or new migration in B3 = scope/amend-once breach** (DS6-a deferred the `GenerationBatch` table to E2).

### Lane 2 — Inter-slice contract / reuse / the B2/B3 seam
- **OV-B3-3 — reuse, no reinvention.** `enqueue_generated_drafts` calls B2 `generate_grounded_drafts`;
  persists N `ProcessingTask` rows (mirrors `enqueue_pill_proposal` `catalogue.py:488`); cost-split via
  `record_provenance_share` semantics (1/N tokens+cost, remainder dropped per `cost.py:111`); reuses B2's
  per-draft provenance writer. No new `Operation`.
- **OV-B3-4 — AC-CD8 spend invariant.** `_pill_generation_spend` must be registered in `current_month_spend`'s
  payload-spend set so the N-draft cost-shares sum into the monthly total **exactly** (Σ cost_share + dropped
  remainder = the call's `cost_usd`). Verify the aggregation wiring at HEAD.
- **OV-B3-5 — `batch_id` seam (E2 key).** B3 stamps one `batch_id` (UUID) per fan-out run on **all N**
  `ProcessingTask` payloads AND on the run's `GenerationProvenance` rows (populating B2's nullable column).
  If this requires touching B2's `generate_grounded_drafts`, verify it's a **clean additive extension**
  (a `batch_id` param), NOT a contract break to the merged B2 fn. `batch_id` is what E2 per-batch rollback queries.
- **OV-B3-6 — idempotency.** Dedup on `(topic, gap_signal)` for **pending** draft rows (the B3 persistence-layer
  arm of the 3-arm dedup; D owns the signal-layer). Re-run with a pending batch → no new rows (returns existing).

### Lane 3 — Scope fence
- **OV-B3-7 — scope.** B3 ships ONLY: `enqueue_generated_drafts` + `_pill_generation_spend` + `batch_id`
  stamping + `(topic, gap_signal)` idempotency + AC-CD15 tests. **NOT in B3:** self-review/confidence/auto-publish
  gate (C1-C2 consume the `pending` rows); **`Pill` creation** (C gate, slice 8); gap-detection trigger (D3
  calls this fn); endpoint/FE; dashboard/rollback (E2). `pill_proposal`/`enqueue_pill_proposal` untouched
  (distinct `task_name`). No new migration.

**Merge class:** NORMAL (executes against ratified v1.9; amends no spec, mints no anchor, no migration).
Auto-merge on 3 sign-offs + three-layer green + zero override window (standing auto-continue; B3 intermediate,
not F1). Watcher `b5qaqwwoj` (broad arm catches the B3 branch off `30cde56`).

### B3 DRAFT = PR #120 @ babf0a0 — governance CLEAN (2026-06-12T~19:54Z)
On-wake pairing vs OV-B3-1..7 = **all Confirm, 0 findings.** amend-once ✓ (no DECISIONS/SPEC/CODE_SPEC; G3
min/max-only, no per-band); **no migration** ✓ (reuses processing_tasks; batch_id rides payload + B2's nullable
GenerationProvenance.batch_id); reuse ✓ (generate_grounded_drafts + record_provenance_share + ProcessingTask,
no new op); AC-CD8 ✓ (_pill_generation_spend mirror registered in current_month_spend, exact 1/N sum);
**batch_id seam ✓ additive** (generate_grounded_drafts gained batch_id=None default → no B2 regression; stamps
shared batch_id on provenance rows for E2 per-batch+per-source); idempotency ✓ ((topic,gap_signal) pending
dedup); scope ✓ (no gate/Pill/D3/endpoint/E2). Executor reached same A3-lesson framing. Posted governance
assessment (1/3); subscribed #120; pending auditor + Gitar + CI @ babf0a0. **(NB: re-armed Monitor bjjocs5pa
after a silent lapse the user caught — verify liveness, don't assume.)** Slices: A1/A2/A3/B1/B2 merged (5/14);
B3 in review.

### B3 round-2 c4119b6 — governance re-SEALED (2026-06-12T~19:55Z)
Executor folded all 3 Gitar findings: #1 SQL filter (task_name/status) in _pending_batch_for; #2 idempotency
gated `if gap_signal is not None`; **#3 (MY OV-B3-4/AC-CD8 lane) empty-draft (N=0) cost-leak → WARN** (I'd
verified the N≥1 cost-share sum but missed the N=0 edge; Gitar caught it; now observable, pathological/💡).
**Gitar ✅ Approved (3 resolved) @ c4119b6.** Amend-once clean (no DECISIONS/SPEC/CODE_SPEC/alembic), no
migration, no scope change. **OV-B3-1..7 hold → re-SEAL @ c4119b6** (prior seal @ babf0a0 re-staled).
3 sign-offs must converge @ c4119b6: overseer ✓; auditor pending; CI pending. On 3 + green → auto-merge.
Watcher `bjjocs5pa` + #120 sub.

### B3 CONVERGED @ c4119b6 — merge pending e2e (2026-06-12T~19:58Z)
**3 sign-offs @ c4119b6:** executor + **auditor content-final @ c4119b6** (`1d02c11`, supersedes babf0a0;
CA-B3-1 + 2 Gitar folds verified) + **overseer re-seal @ c4119b6** + **Gitar ✅**. CI: checks/migration-chain/
docker-build success; **2 e2e queued/in_progress** → mergeable unstable. MERGE @ mergeable=clean. ACTION ON
WAKE: re-check get/get_check_runs; clean → flip ready + squash-merge #120 → verify → pre-register C1 +
auto-continue (B3≠F1; C1 = Stage C, content_self_review + auto-publish self-review floor). Armed `bjjocs5pa` + #120 sub.

### B3 MERGED ✅ — squash ba8aa46 (Slice 6/14; STAGE B COMPLETE) — 2026-06-12T~20:00Z
Flip-ready + squash-merge #120 @ `c4119b6` → `ba8aa46`; branch deleted; unsubscribed. Gate: 3 sign-offs @
c4119b6 (executor + auditor content-final @ c4119b6 + overseer re-seal) + three-layer green (all CI + Gitar ✅
+ mergeable=clean). Round-2 folded all 3 Gitar findings (incl. the AC-CD8 empty-draft edge I'd missed in
OV-B3-4 — caught by Gitar, surfaced via WARN). **Stage B complete (B1 mint → B2 ground+provenance → B3
fan-out+persist+cost-share).** main: …→B2 `30cde56`→B3 `ba8aa46`. **NEXT: C1** (Slice 7 — Stage C start:
`content_self_review` op [the 9th] + 3 cross-model self-review passes, AC-D30 — the autonomous SAFETY FLOOR).
Pre-registering C1.

---

## Pre-registered governance positions — Slice C1 (content_self_review op + multi-pass cross-model self-review — the SAFETY FLOOR)

**Recorded 2026-06-12T~20:02Z, BEFORE the C1 draft exists** (anti-anchoring §4.2/§6). Basis: ratified v1.9 on
main `ba8aa46` (AC-D30 `DECISIONS:772-776`, AC-D1/AC-CD8/AC-D12 ops-count, AC-D21, SPEC §6.9) + detail-plan §7.
C1 = AC-D30 multi-pass cross-model self-review (the non-negotiable safety floor, ruling 4): for each generated
draft, 3 passes (grounding/safety/provenance) on a **different provider from the generator** (Anthropic gen →
OpenAI review). Dep: **B2+B3 merged** ✓.

**⚑ Reframing (verified, supersedes detail-plan §7.3 "BLOCKED pending surfaces") — ALL FOUR ratified:**
**AC-D30** minted PR-C; **NS-2** ruled one-op-three-variants; **NS-7** ruled **degrade-not-gate** (ratified,
`DECISIONS:776`); **AC-D21** safety-re-adjudication amended PR-A. **C1 has NO live ratification surface — PURE
EXECUTION** (the 2nd op-mint, completing the canonical nine). Risk = amend-once + ops-count mirror + the B1
op-wiring discipline + the safety-floor specifics.

### Lane 1 — Amend-once + ops-count mirror (B1 lesson, 2nd op-mint)
- **OV-C1-1 — no anchor edit.** AC-D30/AC-D1/AC-CD8/AC-D12/AC-D21/AC-D31/SPEC §6/§6.9 are ratified-frozen.
  C1 implements `content_self_review` against them; any DECISIONS/SPEC/CODE_SPEC body edit = amend-once violation.
- **OV-C1-2 — ops-count completes at NINE.** `content_self_review` = the 9th named op (B1 made 8): named **8→9**
  (now the FINAL canonical nine), enum **9→10**, cross-family **2→3** (AC-D12). Sweep ALL count-mirrors to the
  final nine — `provider.py` docstring (*"carries eight … joins at C1"* → nine, no stale "pending" language),
  CODE_SPEC §7 / ROADMAP / CHECKLIST built-state. The construction-oracle floors stay green. **Verify count vs
  the ratified enumeration, not just internal consistency (the A3 lesson).**
- **OV-C1-3 — ALL FOUR strict op-keyed maps + extras wired (half-wired-op trap).** `content_self_review` into
  `resolve_model`, `_MAX_OUTPUT_TOKENS`, **`OP_TO_METHOD: "review"`** (NOT "generate"), `_REGISTRY` + **`_REVIEW_DEFAULT_OPS`**
  membership + the config field (`openai_model_content_self_review` sibling pattern) + the `StubAIProvider` branch.
- **OV-C1-4 — NS-2 = ONE op + THREE variants.** ONE `Operation.content_self_review` + three `_VARIANT_REGISTRY`
  entries (`grounding`/`safety`/`provenance`) — NOT three enum members. Minting >1 op = NS-2 violation.

### Lane 2 — Cross-family routing + the safety-floor contract
- **OV-C1-5 — cross-model routing.** `content_self_review` joins **`_REVIEW_DEFAULT_OPS`** → routes to the
  configured `review_provider` (OpenAI) when the draft was Anthropic-generated — the cross-model floor (extends
  the AC-D19/AC-D23 pattern). `OP_TO_METHOD="review"`. NO new provider method.
- **OV-C1-6 — AC-D21 safety re-adjudication.** The **safety** pass re-adjudicates `safety_relevant` (strengthens
  `auto_tag_safety` `catalogue.py:207`, cross-model, can flip the tag) — the autonomous replacement for the
  removed admin pre-publish catch on a false-negative mis-tag.
- **OV-C1-7 — NS-7 degrade-not-gate (RULED).** The safety pass **exposes a degradation switch (default `degrade`)**
  the C2 gate reads: single-provider **safety-relevant** content → **publish-with-warning + "single-provider
  verified" flag, NO second-provider prerequisite gate** (honours ruling 2 "nothing held"). C1 must implement the
  ruled **degrade**, NOT a blocking gate — a second-provider-required gate = NS-7 violation.
- **OV-C1-9 — deterministic offline stub (AC-CD15).** Stub `content_self_review` returns deterministic per-variant
  verdicts (seeded by draft); a cue-bearing unsafe draft trips the safety pass deterministically (zero-network).

### Lane 3 — Scope fence
- **OV-C1-8 — scope = the PROTOCOL only.** C1 ships: the op + 3 variant prompts + `self_review_draft(db, *, draft,
  provenance) -> SelfReviewResult` (3 verdicts + re-adjudicated `safety_relevant` + the degradation flag) + maps/config/
  stub/tests. **NOT in C1 (= C2/Slice 8):** `compute_confidence`, the AC-D31 auto-publish threshold/gate,
  `PublishRecord`, **`Pill` creation/publish**; no dashboard (E); no D3 trigger. C2 *consumes* C1's verdicts.
  No migration expected (the protocol is stateless — verdicts feed C2). `anchor_self_review`/`grade_review` untouched.

**Merge class:** NORMAL (executes against ratified v1.9; the op-mint + NS-7 are pre-ratified; amends no spec).
Auto-merge on 3 sign-offs + three-layer green + zero override window (standing auto-continue; C1 intermediate,
not F1). Watcher `bjjocs5pa` (broad arm catches the C1 branch off `ba8aa46`).

### C1 DRAFT = PR #121 @ 5d9682d — governance CLEAN (2026-06-12T~20:26Z)
On-wake pairing vs OV-C1-1..9 = **all Confirm, 0 findings** (the SAFETY FLOOR, AC-D30). amend-once ✓ (no
DECISIONS/SPEC/CODE_SPEC; no migration); ops-count ✓ (content_self_review = 9th, completes canonical nine;
provider.py docstring swept to "nine named + embed (ten members)" — A3 trap avoided); 4 maps ✓
(OP_TO_METHOD:"review", resolve_model→openai_model_review, _MAX_OUTPUT_TOKENS, _REGISTRY) + _REVIEW_DEFAULT_OPS
+ stub; **NS-2 ✓ ONE op + 3 variants** (grounding/safety/provenance); cross-model routing ✓ (OpenAI); **AC-D21
✓ safety re-adjudication** (flips false-neg mistag); **NS-7 ✓ degrade-not-gate** (SELF_REVIEW_DEGRADE_DEFAULT=
degrade, no 2nd-provider gate — per ruling); scope ✓ (protocol only — no C2/compute_confidence/PublishRecord/
Pill/dashboard); deterministic stub ✓. Executor reached same A3/B1-lesson framing. Posted governance assessment
(1/3); subscribed #121; pending auditor + Gitar + CI @ 5d9682d. **(NB: re-armed Monitor b101fosjz after another
silent lapse the user caught — git-poll monitors expire ~30min; can't self-wake to re-arm during quiet waits.)**
Slices A1/A2/A3/B1/B2/B3 merged (6/14); C1 in review (Stage C start).

### C1 round-2 4bd3345 — governance re-SEALED (safety-floor hardening) — 2026-06-12T~20:27Z
Gitar ⚠️ (safety-floor-critical): original self_review_draft trusted the model's free-form `verdict` string →
a self-contradicting model (verdict=pass + safety_relevant/unsupported/orphans) could BYPASS the AC-D30
non-negotiable safety floor. Executor round-2 = **fail-closed verdict reconciliation**: effective verdict
derived from the structured signal (unsupported→grounding fail, orphans→provenance fail, safety-flip→safety
fail; genuine pass preserved) + fall-safe (flagged-but-tagless safety pass → safety-relevant, no revert to
generator self-tag). +51 tests. **Floor now genuinely non-bypassable.** Honest: my 5d9682d review confirmed
structure but didn't flag the verdict-trust bypass — Gitar caught it (like the B3 AC-CD8 edge). amend-once
clean (no anchor/migration), localized to self_review.py. OV-C1-1..9 hold → **re-SEAL @ 4bd3345** (prior @
5d9682d re-staled). **Gitar ✅ Approved (1 resolved) @ 4bd3345.** 3 sign-offs @ 4bd3345: overseer ✓; auditor
pending; CI pending. On 3 + green → auto-merge. Watcher `b101fosjz` + #121 sub.

### C1 CONVERGED @ 4bd3345 — merge pending CI (2026-06-12T~20:29Z)
**3 sign-offs @ 4bd3345:** executor + **auditor content-final @ 4bd3345** (CA-C1-1 resolved) + **overseer
re-seal @ 4bd3345** + **Gitar ✅**. CI: checks(some)/migration-chain/docker-build success; **1 e2e + 1 checks
in_progress** → mergeable unstable. MERGE @ mergeable=clean. ACTION ON WAKE: re-check get/get_check_runs;
clean → flip ready + squash-merge #121 → verify → pre-register C2 + auto-continue (C1≠F1; C2 = compute_confidence
+ AC-D31 auto-publish gate + PublishRecord — the last Stage-C slice). Armed `b101fosjz` + #121 sub.

### C1 MERGED ✅ — squash a59a40c (Slice 7/14 — HALFWAY; safety floor live) — 2026-06-12T~20:31Z
Flip-ready + squash-merge #121 @ `4bd3345` → `a59a40c`; branch deleted; unsubscribed. Gate: 3 sign-offs @
4bd3345 (executor + auditor content-final + overseer re-seal) + three-layer green (all CI + Gitar ✅ +
mergeable=clean). Round-2 fail-closed hardening (Gitar caught a verdict-trust bypass of the safety floor; now
non-bypassable). **The autonomous AC-D30 safety floor (content_self_review, NS-7 degrade, AC-D21 re-adjudication)
is live on main.** Ops-count complete at canonical nine (B1 pill_generation + C1 content_self_review). main:
…→B3 `ba8aa46`→C1 `a59a40c`. **NEXT: C2** (Slice 8 — Stage C close: compute_confidence + AC-D31 auto-publish
gate + PublishRecord + Pill publish; consumes C1's verdicts + the NS-7 switch; has a migration). Pre-registering C2.

---

## Pre-registered governance positions — Slice C2 (confidence scoring + AC-D31 auto-publish gate — Stage C close)

**Recorded 2026-06-12T~20:33Z, BEFORE the C2 draft exists** (anti-anchoring §4.2/§6). Basis: ratified v1.9 on
main `a59a40c` (AC-D31 `DECISIONS:792-796`, AC-D7, NS-6, AC-D30/AC-D29/AC-D28, SPEC §6.5/§290) + detail-plan §8.
C2 = the autonomous auto-publish gate: for each B3 `pending` draft → C1 self-review → confidence → publish or
publish-with-warning (nothing held). Dep: **C1+B2+B3 merged** ✓.

**⚑ Reframing (verified, supersedes detail-plan §8.3 "BLOCKED") — ALL ratified in PR-C:** AC-D31 minted (gate
+ the compute_confidence formula + NS-6 default 0.70 in the anchor body); AC-D7 amended complete (C2+F1
amend-once); §6.5 rewritten; NS-7 degrade ruled. **C2 has NO live ratification surface — PURE EXECUTION.**
Risk = amend-once + formula/schema fidelity + migration + the safety publish path (NOT a spec-author surface).

### Lane 1 — Amend-once + ratified-formula/schema fidelity (A3/B2 lesson)
- **OV-C2-1 — no anchor edit.** AC-D31/AC-D7/NS-6/AC-CD8/SPEC §6.5/§290 ratified-frozen (PR-C). C2 implements
  against them; any DECISIONS/SPEC/CODE_SPEC body edit = amend-once violation. **Mints NO op/cron** (no ops/cron
  count touched).
- **OV-C2-2 — `compute_confidence` formula MUST match ratified AC-D31** (`DECISIONS:792`): 0..1; **HARD FAIL on
  grounding OR provenance verdict floors the score**; + **authority-weighted provenance** (mean tier-score,
  DS1-a T1=1.0/T2=0.6/T3=0.3); + the **DS2-b corroboration count** (ruled cosine ≥(ii)). A deviating formula =
  spec-contract break (verify vs the ratified anchor, not just plausibility).
- **OV-C2-3 — threshold = single global, default 0.70 (NS-6 ruled).** `SystemSettings.pill_publish_confidence_
  threshold` default **0.70**, per-tenant tunable, **NOT per-type** (ruling 1). A per-type threshold = NS-6/ruling-1
  violation.
- **OV-C2-4 — `PublishRecord` schema fidelity to ratified DS8-a** (`DECISIONS:796`): `pill_id, batch_id,
  confidence, per-pass verdicts, low_confidence, per-type telemetry`. Migration (PublishRecord + the threshold
  field) **up/down clean** (the workstream's 3rd migration after `0009`/`0010`; `--sql` round-trip; table count +1).

### Lane 2 — The autonomous publish path (rulings 1+2, NS-7, safety)
- **OV-C2-5 — `auto_publish_draft` gate behavior.** Per B3 `pending pill_generation` draft: `self_review_draft`
  (C1) → `compute_confidence` → **≥ threshold → publish** (`create_pill`, `status=done`, audit `pill_generation.publish`);
  **< threshold → publish-with-warning** (`create_pill` + `low_confidence` + audit `pill_generation.publish_flagged`);
  **NOTHING HELD** (ruling 2 — including safety-relevant, subject to NS-7).
- **OV-C2-6 — NS-7 degrade (ruled).** Single-provider **safety-relevant** → reads C1's NS-7 switch (default
  `degrade`) → **publish-with-warning + flag, NOT held** (no second-provider gate; honours ruling 2). C2 wires
  the C1 switch; bakes neither option.
- **OV-C2-7 — re-adjudicated safety honoured.** The published pill's `safety_relevant` = **C1's re-adjudicated
  value**, NOT the raw B2/B3 draft tag (the AC-D21 autonomous catch must carry through to publish).

### Lane 3 — Reuse + scope
- **OV-C2-8 — reuse, no deletion.** `create_pill` (`catalogue.py:153`) is the publish primitive (honouring the
  re-adjudicated tag). **`approve_pill_proposal` is NOT deleted** — it stays for the `pill_proposal` refiner (G7a);
  C2 **bypasses** it for generated drafts. Consumes C1 `self_review_draft` + B3 `processing_tasks` rows. No new op/cron.
- **OV-C2-9 — scope.** C2 ships: `compute_confidence` + `auto_publish_draft` + `PublishRecord` model+migration +
  the threshold field + audit actions. **NOT in C2:** bootstrap-on-publish (**F1** — rides the publish event);
  oversight dashboard / rollback (**E1/E2** — consume `PublishRecord`); the D3 trigger; endpoint/FE. C2 only
  *writes* `PublishRecord`; E1/E2 read it.

**Merge class:** NORMAL (executes against ratified v1.9; amends no spec, mints no op/cron). Has a migration
(PublishRecord + threshold field). Auto-merge on 3 sign-offs + three-layer green + zero override window (standing
auto-continue; C2 intermediate, not F1). Watcher `b101fosjz` (broad arm catches the C2 branch off `a59a40c`).

### ▲ CORRECTION to OV-C2-8 (pre-draft, 2026-06-12T~20:34Z) — auditor-caught, verified
**My OV-C2-8 encoded the STALE detail-plan §8.1 lean ("approve_pill_proposal stays for the refiner; C2
bypasses it"). The ratified AC-D7/AC-D31 SUPERSEDES it** (auditor P-C2 FOCAL; same A3-pattern trap). Verified
on main `a59a40c`: **DECISIONS:201 (AC-D7)** + **:792 (AC-D31)** — *"`approve_pill_proposal` is **removed
entirely**; **both** autonomously-generated **and** refiner-polished drafts auto-publish through the same
AC-D31 gate — one publication path, **no per-source gate exception**"*; **:796** lists the threshold field +
migration + `auto_publish_draft` + `PublishRecord` as C2 deliverables and *"the pill_proposal refiner's drafts
route through the same gate."*
**CORRECTED OV-C2-8:** the `pill_proposal` **endpoint** is retained (G7a), but `approve_pill_proposal` (the
HUMAN GATE) is **REMOVED ENTIRELY**; C2 routes **both** paths through `auto_publish_draft` (one path, no
per-source exception). At the C2 draft: verify `approve_pill_proposal` is removed (not retained) + the refiner
output routes through the gate (its exact wiring scope — full vs staged-to-F1/E — is a draft-time check, but
the human-gate removal + single-path are the ratified contract). Lesson re-applied: pre-registration is measured
vs the RATIFIED spec, not the detail-plan lean. Converges with auditor P-C2.

### C2 DRAFT = PR #122 @ 56e1ce4 — generated-gate CLEAN; OV-C2-8 SURFACED (2026-06-12T~20:48Z)
On-wake pairing: **OV-C2-1..7,9 = Confirm** (generated auto-publish gate clean): amend-once ✓ (no anchor edit,
no op/cron); compute_confidence ✓ (hard-fail floor grounding/provenance + authority-weighted DS1-a mean +
capped DS2-b bonus — matches ratified AC-D31); threshold ✓ (single global 0.70, per-tenant, NS-6/ruling-1);
PublishRecord ✓ (DS8-a cols + migration 0011 up/down, 37 tables); gate ✓ (publish/publish_flagged, nothing
held); NS-7 degrade ✓; re-adjudicated safety honoured ✓ (ai_safety_classification=review.safety_relevant);
scope ✓ (no F1/E/D3). **OV-C2-8 → 🔺 SURFACED to spec author · BLOCKING (§7(b)):** ratified AC-D7/AC-D31 =
approve_pill_proposal REMOVED ENTIRELY + refiner routes through the gate (DECISIONS:201/792/796, "C2 execution
deliverables"); BUT detail-plan keeps "refiner approve path unchanged" across ALL 14 slices (line 2503) +
both reviewers' original pre-reg scoped C2 generated-only → the reroute is **assigned to NO slice** (orphaned
half-met contract). Executor built the 3-party-aligned generated-only core + surfaced it (didn't silently
reconcile); auditor P-C2 + my corrected OV-C2-8 concur. **HOLDING C2 merge on the spec-author ruling:** (A)
honour ratified spec — extend C2/a named slice to remove approve_pill_proposal + route refiner through
auto_publish_draft; or (B) amend AC-D7/AC-D31 refiner language (doc-only PR) + merge C2 generated-only. Posted
to #122; surfaced to spec author in-session. Subscribed #122. Watcher `b101fosjz`. 7/14 merged; C2 held.

### C2 round-2 a0821748 — safety-publish fix landed; SOLE blocker = OV-C2-8 (2026-06-12T~20:52Z)
Gitar ⚠️ (safety-floor): safety-pass `fail` could publish live+unflagged (multi-provider) — I grounded it as
REQUIRED by ratified DECISIONS:543 ("safety-relevant → publish-with-warning posture"), an OV-C2-7 gap I'd missed
(Gitar caught it, like C1 fail-closed). Executor round-2 (a0821748): `low_confidence = conf<thr or ns7_degrade
or safety_failed` + safety_verdict on PublishRecord + test. **Gitar ✅ Approved.** amend-once clean; localized
to publish.py. **Re-SEAL @ a0821748** (generated-only core). **C2's SOLE remaining blocker = OV-C2-8** (refiner-
reroute / approve_pill_proposal-removal — still present; ratified AC-D7/AC-D31 deviation) — SURFACED to spec
author, holding on **Option A** (honour spec, give reroute a slice home) **/ B** (amend AC-D7/AC-D31 refiner
language + merge generated-only). Executor can't resolve (spec-author scope call). 3 lanes aligned + held.
7/14 merged; C2 held on the user's A/B ruling. Watcher `b101fosjz` + #122 sub.
