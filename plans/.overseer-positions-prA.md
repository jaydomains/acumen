# Plan-overseer — pre-registered governance positions: PR-A (Corpus & authority foundation)

**Role:** plan-overseer (governance lane + merge-executor) · `.claude/roles/plan-overseer.md`
**Cycle:** sequenced ratification cycle — 4 amendment PRs (PR-A → PR-B → PR-C → PR-D)
**Pre-registered (timestamped):** 2026-06-10T10:46:39Z
**Pre-registered BEFORE reading any PR-A draft** — none exists yet (§4.2 anti-anchoring; this is
the cleanest possible anti-anchor: there is no draft to inherit blind spots from).

**Source of the requirements (NOT a draft):** `plans/2026-06-08-amendment-cycle-extraction.md` §C
(PR-A scope) + `plans/REQUIRED_READING.md` §7 (ratification scope-classes) + the governance lane of
the role file. The extraction doc itself reads as a **relay** (its own §D: "the merged detail-plan
reads as a relay downstream (OV-2) — not actionable on its own"); it is a requirements source for my
positions, **never** a ratification.

---

## Ground truth at pre-registration (verified, not recalled)

- `git ls-remote` + `list_pull_requests(state=open)` → **zero open PRs.** PR-A/B/C/D do not exist.
- PR #108 (the detail-plan, `plans/**`-only, normal-class) is **closed & merged** (`f2cae86`,
  merged 2026-06-10T06:20:56Z by `jaydomains`). It **amended no spec/anchor**.
- `origin/main` HEAD = `0783331`. Anchors at HEAD: AC-D1…AC-D27 (next mint AC-D28); AC-CD1…AC-CD24
  (next mint AC-CD25). No PR-A triage doc exists (the reading list's "PR-A triage" has not landed).
- **Conclusion:** the cycle is at its *pre-authoring gate*, not in-flight. The planner (reviewer-side
  asymmetry — not me) authors PR-A, and only **after** the §D ratification surface is authenticated.

---

## A. The merge-eligibility gate I will enforce on PR-A (the load-bearing position)

PR-A's amendments are, by `REQUIRED_READING.md` §7, **ratification scope-class (i) AND (ii)**:
mint of AC-D28 + AC-CD25 (i/ii); body changes to AC-D21 / AC-D22 / AC-D23 (i/ii); AC-CD1 dep-add
(ii); SPEC §7.3/§7.4/§8.9 + AC-CD7 cron-count (ii). Therefore:

- **POS-A0 — PR-A does NOT auto-merge on the three-sign-off gate.** It requires **explicit,
  authenticated, current** spec-author ratification of the §D surface **before** the planner authors,
  and again confirmed at execution time (§8.3, §7f). A blanket "I approve the plan" is **not**
  ratification of any embedded mint/body. *This is the position the whole cycle turns on.*
- **POS-A0b — relay/inferred/superseded ratifications are PENDING, not actionable** (PR #104/#105
  lesson, cited in dispatch). A ratification recited in a peer's PR comment, in the merged detail-plan
  §1, in the extraction doc §D, **or paraphrased in the dispatch prompt narrative**, carries the same
  shared byline a genuine ruling would — it does not authorize a merge. Only the **in-session human
  channel**, stating the exact body/ID/value, counts. I confirm through that channel before executing.

## B. Anchor-system integrity (governance lane)

- **POS-A1 — exact bodies + immutable IDs are the spec-author's to set, not the planner's.** The four
  AC-D mints (source-authority→**AC-D28**, provenance→PR-B, self-review→PR-C, auto-publish→PR-C) and
  two AC-CD mints (corpus-builder→**AC-CD25**, oversight→PR-D) need their *exact body text + ID*
  ratified. PR-A carries AC-D28 + AC-CD25. Position: verify the body+ID landed in PR-A is byte-for-byte
  the ratified one.
- **POS-A2 — count-header + numeral-sweep integrity.** `DECISIONS.md:7` AC-D27→**28** count header +
  the "AC-D1 through AC-D27" numeral sweep, and `CODE_SPEC.md` §18 index AC-CD24→**25**, must update
  consistently. IDs are **never reused/renumbered; bodies amended in place** (`REQUIRED_READING.md`
  §4). A header that disagrees with the body set is an integrity finding.

## C. Amend-once / no-partial-fold (planner.md §7) — within PR-A

- **POS-A3 — shared anchors land COMPLETE, once.** AC-D21 COMPLETE [A2+C1+E2]; AC-D22 COMPLETE
  [A2+B2]; AC-D23 COMPLETE [A2 + B2/C1 + F1]; §8.9/AC-CD7 cron-count COMPLETE [A3+D4]. Landing only
  A2's slice of a body and "topping it up" in a later PR is the **silent-partial-fold** §7 failure.
  Position: verify each shared body in PR-A is whole (carries the forward C1/E2/B2/F1/D4 substance),
  not a narrow edit.

## D. Cross-PR coordination (the dispatch's explicitly load-bearing items)

- **POS-A4 — AC-D23 (PR-A) ↔ AC-D7 (PR-C) must tell ONE approve→publish story.** AC-D23's
  bootstrap-on-**publish** reframe (AM-6, PR-A) and AC-D7's approve-gate **removal** (AM-12, PR-C) are
  siblings split across PRs. Position: verify PR-A's AC-D23 wording is forward-consistent with PR-C's
  planned AC-D7 change; a drift between them recreates the §8-inconsistency the amendment exists to
  prevent. *(Dispatch: "AC-D23 in PR-A cross-refs AC-D7 in PR-C — verify both.")*
- **POS-A5 — amend-once ACROSS PRs: PR-A must NOT touch a later PR's anchor.** PR-A must not amend
  AC-D7 (PR-C), AC-D1 *Implications* / AC-CD8 numeral (PR-B AM-8), or the provenance / self-review /
  auto-publish AC-Ds (PR-B/PR-C). Position: grep PR-A's diff for any out-of-scope anchor touch — a PR-A
  edit to an anchor PR-(A+M) is meant to extend is the cross-PR amend-once violation the dispatch flags.
- **POS-A6 — mutual cross-references between PRs must be consistent.** The "nine" ops-count in PR-B
  (AM-8) references an op whose anchor (self-review) lands in PR-C; AC-D22's Drive-retirement (PR-A)
  couples NS-1 which sets PR-A's own cron count. Position: track these echoes; a number/reference in
  one PR that names an artifact another PR defines must match across both.

## E. Sequencing rulings that are PRECONDITIONS to PR-A authoring (surface if unmet)

- **POS-A7 — DS13-a must be ruled BEFORE PR-A is authored.** It decides whether AM-1/AC-D28 is
  multi-slice [A1+E2] — i.e. whether PR-A must carry the E2 DB source-override layer. It surfaces at E2
  (PR-D) but **binds A1 (PR-A)**. If unruled at PR-A authoring, AM-1's shape is undetermined →
  **ungovernable (§7e)** → I surface, I do not review or merge a PR-A whose AM-1 shape is unfixed.
- **POS-A8 — NS-1 must be ruled WITH the cron amendment.** NS-1 (retire Drive ingest code vs keep
  dormant) sets AM-7's cron-count number (**nine vs ten**) and AC-D22's shape. An unresolved NS-1 makes
  the §8.9/AC-CD7 count **unverifiable**. Position: the count in PR-A must trace to a ratified NS-1.
- **POS-A9 — NS-5 (post-P11 ROADMAP phase home).** Gates every slice's execution-close; should be
  ruled before the first execution PR. Doesn't block PR-A's *amendment* merge per se, but I surface it
  if it lands in PR-A's ROADMAP/CHECKLIST edits unruled.

## F. Recursive consistency / transportability (governance lane proper)

- **POS-A10 — invariant-bearing lines move together.** The extraction doc ground-checked "seven
  crons" (CODE_SPEC:110/337/632) and "seven AI operations" (CODE_SPEC:302/304, SPEC:296) as live
  invariants. PR-A (+ PR-B) change these counts. Position: verify every invariant-bearing line moves
  consistently — a count updated in one location and not another is the silent §8-drift partial-fold.
- **POS-A11 — three-layer green gate + override window are baked; I execute mechanically.** PR-A
  merges only on three independent sign-offs at one SHA + three-layer green gate (CI + Gitar +
  GitHub-mergeable) + the 24h override window (collapsed to zero **only** by a *present* spec author who
  ratifies through the authenticated channel — not by the dispatch's general assertion) + the §8.3
  ratification of POS-A0. I never merge on my own sign-off; execution is the hand, not the judge.

---

## Disposition at pre-registration

Positions pre-registered. **No PR-A draft to review** — the planner has not authored it, and per the
extraction doc §D it cannot be authored until the §D ratification surface is authenticated through the
in-session channel. As a reviewer-side role I do **not** re-initiate (that is the planner's role,
§4.10) and I do **not** merge on faith. I stand down **bounded** with this durable record, awaiting an
external trigger: either the spec-author's authenticated ratifications + the planner authoring PR-A,
or a re-dispatch. Watcher posture and the surfaced gate are recorded in
`plans/.wake-log-prA-plan-overseer.md`.
